# 2026-07-12

## 抓大鹅 (zhuada-e) 玩法与平衡性评审
- 定位：apps/zhuada-e，3D 物理三连消除（类似羊了个羊）+ 大鹅收集。核心玩法在 src/logic/engine-zhuada.ts，引擎/状态机在 guest-engine.ts，曲线在 game-rules.ts，场景/大鹅在 scenes.ts。
- 关键发现：
  1. 无计时模式道具返还饥饿——加时里程碑只在 timedMode 发放，无计时玩家中段拿不到移出/撤回返还（反向经济，P0）。
  2. L1→L2 难度悬崖（物品 18→84、种类 3→7），新手最大流失点（P0）。
  3. 大鹅纯装饰无机制价值；无每日/赛季循环 → 长期留存弱。
  4. 做得好：多重-of-3 可解性、侧架跨区匹配、物品流分包防埋死、L1 数学胜利地板、连击窗口 2200ms 手感优先。
- 交付：docs/zhuada-e-game-design-analysis.md（含难度曲线图、平衡性电子表格、6 条改进提案 R1-R6 + 优先级）。
- 建议：P0 先改 R1（untimedRefund 改发移出/撤回）+ R2（L2 降到 5 类或插过渡关）。

## 已实施 P0（R1 + R2）
- R1：game-rules.ts 给 MilestonePlan 加 untimedRefund 字段（默认 "remove"）；guest-engine.ts 的 add-time 里程碑分支在 untimed 模式改发 +1 移出（原死资源）。
- R2：LEVEL_CURVE L2 kinds 7→5（物品 84→60），未插新关（避免破坏存档序号/场景映射）。
- 验证：tsc --noEmit 0 错误；game-rules.test.ts 6 项（含新增 untimedRefund 契约测试）、guest-engine.test.ts 36 项全过。
- 文档加 §8 Changelog 并同步"待办→已做"状态。
- 未做（待下一轮）：R3 大鹅被动加成 / R4 每日+连签 / R6 连击 Frenzy。

## 已实施 R5（智能提示）
- 新增 src/logic/hint-plan.ts（纯函数 computeHintPlan 返回 {kind, needFromBox}）+ src/logic/hint-plan.test.ts（6 用例契约）。
- ZhuaDaScene.ts：triggerHint 改用 computeHintPlan；新增 pulseHintGroup（一次高亮盒内最多 needFromBox 个同类物品，按高度优先）+ 可复用 pulseVisual；加 hintPulsing 守卫集防组脉冲与 pending 脉冲冲突；埋藏物品仍走 pendingHintKind 回退。
- 约束：托盘/侧架物品由 React HUD 渲染、不在 3D 场景，R5 只高亮盒内可拾取成员（即玩家要点的目标）。完整"揭示堆下分布"留待后续。
- 验证：tsc --noEmit 0 错误；zhuada-e 全部逻辑测试 158 项全过（含新增 6）。

## 已实施 R6（连击 Frenzy）
- 设计：combo≥5 触发**狂潮**，发 2 发"自动吸附"充能（FRENZY_TRIGGER_COMBO=5、FRENZY_CHARGES=2，均 [PLACEHOLDER] 待 playtest）；之后每次消除消耗 1 充能、从盒内自动吸附 1 个刚消除的同色进托盘（省点击+爆发）。无副本/托盘满则回退充能，绝不堵托盘/递归/破可解性；长链再冲 5 重新发。
- 实现：guest-engine.ts 加 applyFrenzyPull 闭包（取盒内副本置托盘，失败回退；不碰 combo/score、不递归）；extract 匹配分支做充能/消耗；胜利判定改读实时可观察量（吸附吸空盒也能判胜）。main.tsx 加 frenzyCharges/frenzyFx 可观察量并接入引擎+state。PlayArea.tsx HUD 紫罗兰"狂潮 xN" + 一次性"FRENZY!"爆发闪层；PlayArea.scss 用 --goose-motion-* 缓动并尊重 prefers-reduced-motion；messages.ts 加 frenzyLabel/frenzyBurst。
- 关键坑：胜利判定原本用闭包开头捕获的 list，会漏算 Frenzy 吸附；必须读 live observables。motion-quality 测试禁止裸 ease-out，故爆发用 var(--goose-motion-celebrate)。
- 测试影响：原"全波次抽干只在全空判胜"的 14 个关卡测试用贪婪"只看盒内"解算器，会被 Frenzy 残留托盘组卡住且 extracted===expectedTotal 不再成立。已升级为"先收尾托盘/侧架残留组"的合格解算器 + 放宽 extracted<=expectedTotal。新增 R6 Frenzy 3 用例（发充能+爆发 / 武装匹配吸附扣充能 / 耗尽后重发）。
- 验证：tsc --noEmit 0 错误；eslint 干净；run-tests 全部逻辑测试 150 项全过（guest-engine.test.ts 147→150）。
- 文档：docs/zhuada-e-game-design-analysis.md §5 R6 改写、§7 优先级表标 ✅、§8 加 R6 实施段 + 顶部状态行更新；待办移除 R6。
- 剩余未做：R3 大鹅被动加成 / R4 每日+连签。

## 已实施 R4（每日挑战 + 连签）
- 设计：填补设计支柱 4"长期有理由回来"。每日签到（独立 key `zhuada-e:daily`，刻意不进 GooseProgress v3 以免扰动迁移守卫）+ 连签递增（断签>1天重置为1，损失厌恶）+ 7天里程碑（额外+3全道具 + 一次性爆发）+ 每日挑战（日期种子确定性布局 Lv.6）。
- 实现：`daily-reward.ts`（纯函数+存储：todayKey/prevDayKey/dayDiff/dateSeed/computeDailyView/claimDailyReward/load/save）。guest-engine 加 dailyState/dailyClaimable/dailyGrants/dailyMilestoneFx 四个可观察量；enter() 注入当日视图；enterLevel 在基础道具上叠加 dailyBonus；新增 claimDaily() 与 startDailyChallenge()（日期种子不混 runNonce/Date.now，否则同天布局逐会话漂移）。main.tsx 接线 + claimDaily/startDaily 动作。PlayArea.tsx 放 goose-daily 区块（徽章+领取卡+挑战按钮，置于 theme-picker 之后以保无障碍 Tab 顺序先落到 theme-card）+ 里程碑爆发；locale 中/英补全；SCSS 用共享 --goose-motion-quick（禁裸 ease，否则 motion-quality 测试挂）。
- 关键坑：每日奖励是"按天 perk"覆盖式写入有上限，不是储蓄罐——避免连签滚雪球。原 powerups 每局重置，故 dailyBonus 在每局基础发放上叠加，天然跨局生效。
- 验证：tsc 0 错误；eslint 干净；daily-reward.test.ts 8 用例 + guest-engine.test.ts 新增 R4 4 用例；逻辑测试 154 项全过（PlayArea.accessibility 1 例因 Tab 顺序先落 daily 按钮而失败→把 daily 区块移到 theme-picker 之后修复）。
- 文档：docs/zhuada-e-game-design-analysis.md §5 R4 改写(✅)、§7 表 R4 标✅、§8 加 R4 实施段 + 顶部状态行更新；支柱4 状态改"R4已成立"。
- 剩余未做：R3 大鹅被动加成。

## 已实施 R3（大鹅被动加成）
- 设计：填补"大鹅纯装饰无机制价值"问题（GDD §5）。6 只限定大鹅改为各解锁一个温和永久增益（endowment + 沉没成本），增益温和、仅作"努力可见"、不破坏可解性。
- 实现：`src/logic/goose-passive.ts`（纯函数）：GoosePassive 接口 + GOOSE_PASSIVES（6 场景→独立增益映射）+ GOOSE_PASSIVE_LIMITS（maxShakeCdReductionMs=3000、maxComboWindowDeltaMs=4000 防滚雪球）+ EMPTY_GOOSE_PASSIVE；computeGoosePassive(geese) 道具类累加、标量类 clamp 到上下限、阈值乘数相乘、未知 id 容错忽略；goosePerkKey(sceneId) 查加成文案 key。
- 映射：0花园+1提示 / 1果园+1移出 / 2池塘 shakeCd −1000 / 3农场 comboWindow +200 / 4雪原+1撤回 / 5夜市 milestoneThresholdScale ×0.9。
- game-rules.ts：`milestonesFor(spec, thresholdScale=1)`（默认 1 向后兼容），hintStep/addTimeStep 乘 scale。
- guest-engine.ts：`enterLevel`/`restoreRun` 重算 goosePassive 与 per-run `comboWindowMs = COMBO_WINDOW_MS + delta`；powerups 在基础发放叠加 extra*（提示/移出/撤回）；milestonesFor 注入 scale；`extract` 连击判定与归零用 comboWindowMs；`shake` 冷却 `Math.max(SHAKE_CD_MIN=2000, SHAKE_CD_MS + delta)`（clamp 下限）。
- UI：PlayArea.tsx 收藏册 unlocked 卡片渲染 `goosePerkKey` 文案（IIFE）；PlayArea.scss `.goose-collection__perk`（10.5px、品牌色、无裸 ease）；messages.ts 中/英 6 条 perk 文案。
- 关键决策：增益温和互不冲突；阈值缩放用乘数（非加减）避免叠加失真；未知场景 id 容错忽略而非抛错；连击窗口用 per-run 变量替代全局常量，确保农场鹅生效且不影响其他模式。
- 验证：tsc 0 错误；eslint 干净；新增 goose-passive.test.ts 10 例 + guest-engine.test.ts R3 段 4 例 + game-rules.test.ts R3 段 2 例；全量测试 198 项全过。
- 文档：docs/zhuada-e-game-design-analysis.md §5 R3 改 ✅、§7 表 R3 标✅、§8 Changelog 顶部状态行更新为"全量测试 198 项全过"、支柱4 改"支柱成立"、删除 R3 待办。

## 已实施 R7（第二篇章内容扩展）
- 设计：承接用户"扩展内容"指令，经 AskUserQuestion 确认=3 个新场景（第二篇章：火山/云端/深海）+ 引入**新加成类型**（不复用 R3 已有 6 类）。填补"内容深度/长期留存"支柱。
- 数据（scenes.ts）：SCENES 数组末尾追 3 个 SceneTheme——id6 火山(levels [16,18], party hat, 红橙)、id7 云端(levels [19,21], beanie, 蓝白)、id8 深海(levels [22,24], cap, 青黑)。各自 palette / kindPool(12 id 重排) / goose(GooseVariant 图元拼装，无新 3D 模型)。nameKey/gooseNameKey 指向新增文案 key。
- 曲线（game-rules.ts）：TOTAL_LEVELS 15→24；LEVEL_CURVE 追 L16–L24：kinds 恒 12、perKind 12→16 渐增（L16=12/L17=13/L18=14/L19=14/L20=15/L21=15/L22=15/L23=16/L24=16）、timeMs 按公式推（L16=660000 与 L15 持平做**软桥接**，L24=880000 峰值）、boxSize 12。顶部注释 "6 themed scenes"→"9 themed scenes"、峰值 "432 items"→"576 items (chapter 2 peak, L24)"、"only 40–54 live Cannon bodies" 保持单行连续（release-audit 硬性短语检查）。
- 新鹅加成（goose-passive.ts，3 个全新杠杆，与 R3 6 类不重叠）：GoosePassive 接口增 extraShuffle/scoreBonus/frenzyTriggerDelta；常量 GOOSE_PASSIVE_VOLCANO_SHUFFLE=1、GOOSE_PASSIVE_CLOUD_SCORE_BONUS=0.05、GOOSE_PASSIVE_ABYSS_FRENZY_DELTA=1；GOOSE_PASSIVE_LIMITS 增 maxScoreBonus=0.5、maxFrenzyTriggerReduction=2；GOOSE_PASSIVES 增 6:{extraShuffle:1}/7:{scoreBonus:0.05}/8:{frenzyTriggerDelta:1}（各带 perkKey）；computeGoosePassive 累加三新字段并 clamp(scoreBonus 0..0.5、frenzyTriggerDelta 0..2)。
- 引擎接线（guest-engine.ts）：声明区新增 `let frenzyTriggerCombo = FRENZY_TRIGGER_COMBO`；enterLevel/restoreRun 重算 `frenzyTriggerCombo = Math.max(3, FRENZY_TRIGGER_COMBO - goosePassive.frenzyTriggerDelta)`；道具发放 `shuffle: GRANT_SHUFFLE + bonus.shuffle + goosePassive.extraShuffle`；结算时间奖励 `timeBonus = round(leftSec * TIME_BONUS_PER_SEC * (1 + goosePassive.scoreBonus))`；消除计分 `gained = round((SCORE_PER_MATCH + (comboCount.get()-1)*COMBO_BONUS_PER_STEP) * (1 + goosePassive.scoreBonus))`；狂潮触发判 `comboCount.get() >= frenzyTriggerCombo`。
- 文案（messages.ts）：sceneVolcano/sceneCloud/sceneAbyss、gooseVolcano/gooseCloud/gooseAbyss、goosePerkVolcano(+1 shuffle)/goosePerkCloud(+5% score)/goosePerkAbyss(Frenzy −1 combo)，均中/英。
- 测试：新增 goose-passive.test.ts R7 段 5 用例（新鹅映射/9 鹅聚合/scoreBonus clamp/frenzyTriggerDelta clamp/perk key）；guest-engine.test.ts harness 暴露 score、新增 R7 段 4 用例（火山洗牌叠加进发放/云端分数倍率/无深海鹅 combo4 不触发狂潮/深海鹅 combo4 即触发）；progress.test.ts SCENES 期望 6→9；GooseChip.test.ts 肖像映射断言扩到 goose-00..goose-08；release-audit.mjs "432 items"→"576 items"。
- 关键决策：variety（kinds）不增只加深 perKind，避免 L1→L2 式悬崖；L16 与 L15 持平做软桥接；新加成严格不重叠 R3 6 类（否则会触发聚合冲突）；零新 3D 资源（图元+12 物品 id 复用），UI 动态引用 SCENES.length/TOTAL_LEVELS 自动适配无需改 UI。
- 验证：tsc 0 错误；release-audit 全过；全量 vitest **202 项全过**（goose-passive +5、guest-engine R7 +4、progress 改 6→9、GooseChip 扩 8）。
- 文档：docs/zhuada-e-game-design-analysis.md 6→9 场景、Progression 15关/6场景→24关/9场景、Retention Hook 补 R7、2 处"15 关"→"24 关"、新增 R7 提案章（火山/云端/深海 + 3 新杠杆 + 难度曲线 + 零新资源）、§7 优先级表 R7 ✅(P1)、§附录支柱4 加 R7、§8 Changelog 顶部状态行更新（全量 vitest 202 项）、新增 §8 R7 实施记录段、待办改"R1–R7 均已落地"并补新鹅 .webp 美术待生成说明。
- 待生成资源：新鹅肖像 goose-06/07/08.webp 由项目 prebuild 管线生成（映射正确、测试已更新，待美术资源生成）。

## 已实施 R8（平衡调参定稿）
- 问题：R1–R7 落地后，R6 Frenzy / R7 第二篇章鹅加成 / R4 每日经济 及 §6 中 `[PLACEHOLDER]` 数值缺"理由+区间+broken 判据"，无法进入 playtest 验收。
- 方法（用户选"平衡调参定稿"）：复用 tune.mjs 门禁，新增伴侣脚本 `scripts/balance-frenzy.mjs`（漂移防护：正则提取 TS 源码常量；蒙特卡洛遮挡 greedy + Frenzy 节奏模型 + 每日经济偿付）。覆盖三组杠杆：
  1. Frenzy 节奏：按 archetype 匹配节奏蒙特卡洛连击链。结论：吃力 0 触发/关、平均偶发、熟练频繁 = 干净技能表达杠杆；深渊鹅(门槛→4)只帮平均/熟练，吃力仍 0（安全）。
  2. 鹅加成胜率扫描：遮挡 greedy 下火山 +1 洗牌 +22.4pp、全收集 +37.8pp；满信息可解性全关 100%（不变式守住）、后期 96.7–99.9% <100%（张力保留）。
  3. 每日经济：上限 5 + 里程碑 3 → 最大发放 9/8，封顶防滚雪球；吃力仍有真实挑战（道具减负不包赢，缺口按设计）。
- 交付：GDD §9 调参决议表（每杠杆 出厂默认 / **Proposed** / 区间 / 理由 / broken-if）+ 数学目标曲线（计时预算/难度单调/里程碑/Frenzy 梯度）+ 模拟证据节选 + 结论。代码 `[PLACEHOLDER]` 标注保留，待真人 playtest 照 §9.1 验收后替换 Proposed 值。
- 关键坑：首版 balance-frenzy.mjs 的 playTrial 未尊重 `fullVision` 标志，导致"满信息可解性"误测为 17–42%（假阴性，误报门禁 BROKEN）。修 `windowOf = (n) => loadoutIn.fullVision ? n : exposedWindow(n)` 后重跑，满信息 24 关全 100%，COMPANION CHECKS PASS ✅。
- 验证：tune.mjs 全门禁通过（R7 后 24 关）；balance-frenzy.mjs 伴侣检查全过；tsc 0 错误（仅改注释 + 新增独立 .mjs，未动逻辑，无回归）。
- 文档：docs/zhuada-e-game-design-analysis.md §6 顶部加 §9 指针、新增 §9（R8 调参决议）、§8 Changelog 顶部状态行加 R8 + 新增 R8 实施段 + 待办改 R1–R8、③ 改"playtest 验收后替换 [PLACEHOLDER]"。

## 全链路状态（R1–R8 全部落地）
- P0：R1 无计时道具返还饥饿 ✅、R2 L2 难度悬崖 ✅
- P1：R4 每日挑战+连签 ✅、R5 智能提示 ✅、R6 连击 Frenzy ✅、R3 大鹅被动加成 ✅、R7 第二篇章内容扩展（3 新场景+9 关+3 新鹅+3 新加成类型）✅、R8 平衡调参定稿（§9 + balance-frenzy.mjs，[PLACEHOLDER]→可测假设）✅
- GDD docs/zhuada-e-game-design-analysis.md 持续更新（§5/§7/§8/§9），R1–R8 全部提案均已实施（逻辑 R1–R7 测试绿 202 项；R8 为设计交付，未改逻辑）。
- 后续可选项（需用户明确指示）：① **R8 已交付调参假设，下一步为真人 playtest 照 §9.1 区间与 broken-if 验收，通过后把 goose-passive.ts/guest-engine.ts/daily-reward.ts 的 [PLACEHOLDER] 替换为 Proposed 值**；② 鹅加成图形化展示；③ 新鹅肖像 goose-06/07/08.webp 美术由 prebuild 生成。若用户再发"继续优化"应提示此链已完成并询问下一步方向，而非自动选新项。

## 已实施 R8b（验收 + 新鹅美术 + 鹅加成图形化）
- 用户指令"做这些 ① playtest 验收替换[PLACEHOLDER]；② 鹅加成图形化展示；③ 新鹅肖像美术生成"。
- ① 验收门禁 + 提升 [PLACEHOLDER]：真人 playtest 本环境跑不了，改用双蒙特卡洛模拟作为正式验收代理。重跑 tune.mjs（24关全门禁 PASS）+ balance-frenzy.mjs（COMPANION CHECKS PASS），据此把代码 4 处 [PLACEHOLDER]→[ACCEPTED-SIM]：goose-passive.ts/daily-reward.ts 调参表注释各1、guest-engine.ts 的 FRENZY_TRIGGER_COMBO/FRENZY_CHARGES 各1。数值即 §9.1 Proposed 默认，平衡已验证；Frenzy 两值为手感杠杆，真人手感仍建议补测后锁定（诚实区分）。
- ② 鹅加成图形化：新增 src/GoosePerkIcon.tsx（lucide 图标，9 种 perk 各一映射，与 PlayArea 既有图标语言一致）+ 守卫测试 GoosePerkIcon.test.ts（任一新鹅漏配图标即失败）；PlayArea.tsx 收藏卡 perk 改"图标+文案"；PlayArea.scss .goose-collection__perk 改 inline-flex + .goose-perk__icon。lucide-react 已在项目依赖（PlayArea 已用），零新依赖。
- ③ 新鹅肖像：ImageGen 在本环境两处硬伤——并行生成文件名碰撞（同秒同名覆盖，只拿到1张）、且忽略 transparent 背景（输出 RGB 不透明）。改为程序化生成：新增 scripts/generate-goose-portraits.mjs，从 scenes.ts 的 GooseVariant 按数据画 SVG 鹅→sharp 栅格成 512×512 透明 webp（goose-06 火山/07 云端/08 深海）。覆盖率 27–29%（资产门禁 20–45% 区间）。接入 npm run geese:generate（独立脚本，未进 prebuild 以保持 release-audit 的 prebuild 精确契约）。verify-assets.mjs geese 计数 6→9、统计 57→60（资产门禁 PASS）。首 6 只已批准 ImageGen 母版不动，仅新增 3 只；风格与首 6 略有差异（矢量 vs 手绘），可选后续统一。
- 关键坑：release-audit.mjs 有两条硬契约——prebuild 精确匹配 "npm run art:generate && npm run audio:generate"（我误加 geese:generate 破坏了，已还原）；verify-assets.mjs 含短语 "57 images" 检查（改 60 图后需同步改 release-audit.mjs:213 为 "60 images"）。两处均修复，release-audit + release-audit.test.mjs 全过。
- 验证：tsc 0 错误；run-tests.mjs 全过（EXIT=0，node:test 审计 fail 0、vitest 各块全过，新增 GoosePerkIcon 守卫）；assets:verify 全过（60 图）；release-audit 全过。
- 文档：docs/zhuada-e-game-design-analysis.md §9 标题/blockquote/验收结论段更新（[PLACEHOLDER]→[ACCEPTED-SIM]）、§8 顶部状态行加 R8b、新增 §8 R8b 实施段 + 待办更新（仅剩真人手感 playtest）。

## 全链路状态（R1–R8b 全部落地）
- P0：R1 ✅、R2 ✅
- P1：R4 ✅、R5 ✅、R6 ✅、R3 ✅、R7 ✅、R8 平衡调参定稿 ✅、R8b（验收[PLACEHOLDER]→[ACCEPTED-SIM] / 鹅加成图形化 / 3 新鹅肖像生成）✅
- GDD 持续更新（§5/§7/§8/§9）。R1–R8b 逻辑与美术全部落地，测试全绿。
- 唯一剩余：真人手感 playtest（Frenzy 触发5/充能2 爽感、每日发放体感）照 §9.1 验收后锁定 [ACCEPTED-SIM] 值。

## 小游戏 UI 系统性美化 — 批次 1 完成（3 款 M 级）
- 审计交付：docs/game-ui-beauty-roadmap-2026.md（8 维 rubric、24 款普查 20A/4B/0C、批次 1/2/3 路线图）。
- 批次 1 范围（用户 AskUserQuestion 选"3款M级"）：arrow-escape / bead-workshop / fruit-funnel，统一接入 v2 设计令牌（`@use "@shared/styles/v2/tokens" as *` + 域主题重映射范式，参考 snake-bounty legacy-token 桥接）。
- 改动：
  - arrow-escape：裸 hex → `--ae-*`（13 个域令牌）；两处 `Georgia/serif` 标题/弹窗 → `var(--mx2-font-sans)`（去 S3 越界）。
  - bead-workshop：裸 hex → `--bw-*`（6 令牌）；`system-ui` → `var(--mx2-font-sans)`；`bead-live-pulse 2.4s infinite` → `2.8s`（更克制的活体状态点，保留 reduced-motion 关闭）。
  - fruit-funnel：裸 hex → `--ff-*`（7 令牌）；重阴影 `0 24px 58px/.2` → `0 12px 30px/.1`（S4 单层深度达标）；圆角 `28px → 20px`（移动端 22→18）；删死代码 `.fruit-funnel__heartbeat`(`display:none` 永不渲染的 `1.2s infinite`)及其 reduced-motion 块。
- S7 三态组件：原路线图计划"补空/错/加载三态"，经核查 arrow-escape 为手机壳式本地游戏（无钱包、PhaserGameComponent 框架自带 loading/error 态），S7 基本达标，**未新增独立空态组件**（范围收敛，非遗漏）。
- 验证：三款 scss 经 `sass --load-path` 真实编译通过（`@shared` 为 webpack 别名，独立 sass 需临时 symlink `@shared`→`apps/shared` + `--load-path`）；三款 `tsc --noEmit` exit=0/errors=0；无残留裸 hex（令牌块外）、无 Georgia/serif、无 heartbeat 死代码。
- 路线图已更新：批次 1 标 ✅、§6 Changelog 加 2026-07-12 批次1 完成记录、并诚实注明 S7 范围收敛。
- 待用户发话推进：批次 2 graveyard 外壳迁移（L，迁入 mx2-stage + 全量令牌化）、批次 3 A 级循环动画清理（red-envelope/pet-potion/sheep-solitaire/burn-league/on-chain-tarot 无限陪伴动画降频）。

### sass 独立验证技巧（复用）
- `@shared` 是项目 webpack `resolve.alias`，独立 `sass` 不识别 → 报 "Can't find stylesheet to import @shared/styles/v2/tokens"。修复：临时 `ln -s apps/shared /tmp/sasstest/@shared` 然后 `sass --load-path=/tmp/sasstest apps/X/src/PlayArea.scss /tmp/out.css`。
- tsc 跨 cwd 不保持：用绝对路径子 shell `cd "/Users/.../apps/$g" && node_modules/.../tsc --noEmit`。
- Grep 工具的 brace-glob `{a,b}/src/x.scss` 在路径 glob 会失配 → 改用 `**/{a,b}/src/x.scss` 或绝对路径逐个 grep。

## 小游戏 UI 美化 — 批次 3 完成（A 级循环动画清理）
- 范围（用户选"批次 3 A级动画清理"）：red-envelope / pet-potion / sheep-solitaire / burn-league 无限陪伴动画降频；pet-potion bounce 压≤250ms。on-chain-tarot 经核查**无无限动画**（仅一次性 `tarot-drawer-enter 180ms`）→ 无需改动（路线图原误标，已更正）。
- 改动（2026-07-12）：
  - red-envelope：`mx2-float 1.4s` → `3.2s`（共享 keyframe，本地降频，不动共享令牌）。
  - pet-potion：`pp-float` 全量降频（2.6s→3.6s / 1.6s→3.0s / 2.2s→3.4s ×5 / 2s→3.4s）；`pp-ball-bounce 680ms` → 改名 `pp-ball-pop` + `250ms`（去 bounce 命名 + 压≤250ms）。
  - sheep-solitaire：`sheep-shuffle 900ms` → `2.2s`（仅 dealing 态）。
  - burn-league：`burn-brazier-charge 1.12s` → `2.6s`（仅 burning 态）。
- 刻意保留：`pp-timer-alarm 1s infinite`（玩法关键倒计时警告）；`mx2-spin 1s`（平台共享 loading spinner，功能件不在装饰动画范畴）。
- 无障碍：4 款 reduced-motion 块均已冻结上述无限动画，无需新增规则。
- 验证：sass 真实编译需 `--load-path=/tmp/sasstest`（`@shared` 符号链接）+ `--load-path=$ROOT/node_modules`（v2.scss 传递依赖 `@douyinfe/semi-theme-default`）；tsc 5 款全 0 错误。sass 二进制在 `$ROOT/node_modules/.bin/sass`（v1.98 dart-sass），不在 node 托管二进制里。
- 路线图已更新：§4 标注两项已收敛、§5 批次3 标 ✅、§6 Changelog 加批次3 记录。
- 待用户发话：批次 2 graveyard 外壳迁移（L，需单独确认，改动面大）。

### 工具行为提醒
- Edit 工具并行改同一文件曾显示 "Interrupted by user" 但编辑实际已落盘（grep 复查为准）。中断后务必先 grep 复查现状再补未应用的编辑，不要盲目重发全部。
