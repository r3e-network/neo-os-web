# 2026-07-09

- 分析 neo-miniapps-platform 小游戏架构：mini-app 通过 `neo-manifest.json` + 宿主扫描注册，游戏走统一游戏框架（Phaser + BaseScene + defineMiniApp + GameBridge）。
- 调研微信小游戏“抓大鹅”：确认 A 类玩法为“限时三消找物/抓鹅”，B 类为合成玩法；无官方开源素材，采用原创 emoji/矢量美术复刻体验。
- 新建 `apps/zhuada-e/` 小游戏：
  - 完整文件：index.html、neo-manifest.json、package.json、vite.config.ts、public/logo.png、banner.png、src/main.tsx、manifest.ts、PhaserPlayArea.tsx、PlayArea.scss、locale/messages.ts、scenes/ZhuaDaEScene.ts、logic/engine-zhuada.ts、game-rules.ts、guest-engine.ts。
  - 实现核心循环：点击选中 3 个相同物品 → 消除 → 倒计时 → 清空抓大鹅过关；12 关递进曲线，纯本地免费模式。
- 验证：npm run build 通过，ESLint 无警告，agent-browser 端到端验证棋盘渲染与三消逻辑正常。

## 抓大鹅 转 B 类（物理抽物版）并完成验证（下午/晚）
- 用户决策：从 A 类 Phaser 网格找物改为 **B 类 Three.js + cannon-es 物理抽物版**（匹配其提供的教程，要可上线完整版）。
- 已重写 `apps/zhuada-e/` 为 B 类：ThreeGameComponent（复用 GameBridge，引擎无关）+ ZhuaDaScene（Three.js 渲染 + cannon-es 物理 + Raycaster 抽物 + 7 槽托盘 3 连消除 + 胜负演出）+ PlayArea（HUD + 开始/下一关/重来覆盖层）。
- 机制：物品落盒堆叠 → 点选射线抽出 → 托盘凑 3 消 → 盒空过关(`solved`)、托盘满且无可消三元组或超时失败(`expired`)。15 关曲线（种类3→12、份数2→5、时间120s→50s）。
- **关键坑修复（务必保留）**：① cannon-es 0.20.0 的 `world.fixedStep` 不推进刚体，必须 `world.step(1/60)`；② 生成节奏用未钳制 rawDt 累加，否则 rAF 节流下生成卡死；③ 拾取前 `camera/scene.updateMatrixWorld()` 否则射线瞄偏。
- 端到端验证通过（agent-browser）：胜利链路 score 375/itemsLeft 0；关卡递进 level 2；**真实 canvas 点击→射线→抽出** 10 个（items 18→8，托盘正确消除）；ESLint 0/0；build 通过（index.js 786KB/gzip 215KB）；`stage:miniapps:dist -- zhuada-e` 已拷贝到 host public，catalog 已含 zhuada-e。
- GDD 已更新为 v2.0（B 类），design-prototypes/gdd-zhuada-e.md。
- 注意：headless 环境会暂停 rAF，验证时用临时 debugSpawnAll/debugPickFirst 钩子（已移除）。

## 抓大鹅 v2.1：美术升级 / 数值调优 / 道具系统 / 代码分割（晚，用户指令“都做”）
- 用户回应“都做”＝同时推进四件事：① 原创 3D 美术替换 ② 蒙特卡洛数值调优 ③ 道具系统 ④ 代码分割减包。目标：保持可上线、可玩、打磨。
- **① 美术升级**：新增 `scenes/models.ts` —— 原创低多边形模型库，12 种物品（番茄/胡萝卜/玉米/茄子/苹果/西兰花/蘑菇/洋葱/青椒/瓜/蛋/鱼）+ 大鹅模型，替换裸 primitive 与 emoji。ZhuaDaScene 的 spawn/托盘/胜利大鹅全部改用 `buildModelMesh`/`buildGoose`。`engine-zhuada.ts` 给每个 ITEM_DEF 加 `model: ModelKind` 字段。
- **② 数值调优（蒙特卡洛）**：新增 `scripts/tune.mjs`（纯逻辑仿真，greedyPick/randomPick 两种策略 × 4000 次/关）。证明旧曲线（时间 120s→50s，与时间反比）数学上不可解；重写为 15 关显式曲线 `game-rules.ts` LEVEL_CURVE：种类 3→12、份数 2→5、**时间随物品数增长 40s→230s**，验证贪心 100% 可解、随机胜率随关下滑合理。
- **③ 道具系统**：引擎 `guest-engine.ts` 加 `shuffle()/hint()/addTime(ms)` + `powerups/shuffleNonce/hintNonce` observable；main.tsx 注册 `shuffle/hint/addTime` action 并暴露到 state；PlayArea 加道具栏（洗牌/提示/+15s）按钮 + scss；场景用 nonce 响应（洗牌重落、提示脉冲高亮可消物）。**里程碑返还**（行为经济学正向强化）：每 +100 分送提示、每 +200 分送加时、连击=4 送加时。初始赠送 shuffle×1/hint×3/addTime×1。
- **④ 代码分割**：PlayArea 改为动态 `import("./scenes/ZhuaDaScene")`（首次 dealt 才加载），three+cannon-es 隔离为异步 chunk。入口 index 从 786KB/gzip 215KB → **213KB/gzip 67KB**；ZhuaDaScene 异步 chunk 599KB/gzip 155KB。
- **验证**：agent-browser 端到端 —— 胜利链路 score 237/itemsLeft 0；道具链全部确认（hint 3→2 nonce1、addTime 1→0 +15000ms、shuffle 1→0 kindsChanged）；里程碑返还生效；ESLint 0/0；build 通过；`stage:miniapps:dist -- zhuada-e` catalogCount 73 + lazy chunk 已拷贝。控制台仅一条无关 ranksTab i18n 警告。
- GDD 升到 v2.1，overview.md 同步更新。抓大鹅现可上线：四件增强全部交付。

## 抓大鹅 v2.2：音频系统 + 移动端触屏适配（用户指令“继续”）
- 推进之前列的后续项里的前两项：① 音效/音乐 ② 移动端触控适配（③ 关卡手感校准留给真人 playtest，蒙特卡洛只保证可解）。
- **① 音频系统**：新增 `logic/sound.ts` —— 单例纯合成 WebAudio 音效引擎（振荡器 + 滤波白噪声，**零音频文件、零版权风险**）。9 种音效：land(碰撞速度缩放 + <45ms 节流)/pick/match/combo/win/fail/powerup/shuffle/click。接入口：guest-engine（`extract` 内 match/combo 分流）、ZhuaDaScene（碰撞回调 land、首次 pointerdown unlock、pick、playWin/playFail）、main.tsx + PlayArea（UI click + shuffle/powerup + 静音切换按钮 📢/🔇）。静音态写入 `localStorage["zhuada-e:sound-muted"]`，master.gain 平滑切换；`AudioContext` 懒创建、首次手势 `unlock()` resume 以满足自动播放策略。
- **② 移动端触屏**：`ZhuaDaScene` 拾取事件改用 `pointerdown`（覆盖鼠标/触屏/笔）；`PlayArea.scss` 的 `.goose-canvas-wrap`/`.goose-three-canvas` 加 `touch-action:none` + `-webkit-tap-highlight-color:transparent` + `user-select:none` + `overscroll-behavior:contain`，避免触屏落点被页面滚动/下拉刷新吞掉；射线归一化用 `getBoundingClientRect()`（与 DPR 无关）。
- **验证**：本环境无 Chromium/Playwright（agent-browser CLI 不在 PATH），改用 Node + mock AudioContext/localStorage 对 `sound.ts` 跑 10 项逻辑断言（懒创建、静音门控、持久化、unlock-resume、9 音效均不抛错、land 节流）全部 PASS；`npx eslint` 0/0；`npm run build` 通过（入口 218KB/gzip 69KB，音效引擎仅 +5KB；ZhuaDaScene 仍懒加载块 599KB/gzip 155KB）；`stage:miniapps:dist -- zhuada-e` catalogCount 73 + lazy chunk 已拷贝。GDD 升到 v2.2，overview.md 同步（新增 §10.5 音频与移动端 + todo 16/17）。
- 注意：真机/真浏览器的「可听感」与「触屏命中手感」仍需真人 playtest 复检（逻辑层已端到端+单测覆盖）。
