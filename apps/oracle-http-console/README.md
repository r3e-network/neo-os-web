# Oracle HTTP Console

Run allowlisted HTTP Oracle queries through a focused miniapp surface.

## What It Does

- executes host-routed allowlisted HTTP Oracle requests
- supports optional secret references
- shows status code, headers, and response body
