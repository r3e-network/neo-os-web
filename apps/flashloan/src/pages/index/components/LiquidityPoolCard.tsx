import "./LiquidityPoolCard.scss";

interface LiquidityPoolCardProps {
  t?: (key: string, params?: Record<string, string | number>) => string;
  [key: string]: unknown;
}

export default function LiquidityPoolCard(props: LiquidityPoolCardProps) {
  return (
    <div className="LiquidityPoolCard">
      {/* Migrated from LiquidityPoolCard.vue */}
    </div>
  );
}
