import "./FlashloanDocs.scss";

interface FlashloanDocsProps {
  t?: (key: string, params?: Record<string, string | number>) => string;
  [key: string]: unknown;
}

export default function FlashloanDocs(props: FlashloanDocsProps) {
  return (
    <div className="FlashloanDocs">
      {/* Migrated from FlashloanDocs.vue */}
    </div>
  );
}
