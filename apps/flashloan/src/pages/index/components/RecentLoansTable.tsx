import "./RecentLoansTable.scss";

interface RecentLoansTableProps {
  t?: (key: string, params?: Record<string, string | number>) => string;
  [key: string]: unknown;
}

export default function RecentLoansTable(props: RecentLoansTableProps) {
  return (
    <div className="RecentLoansTable">
      {/* Migrated from RecentLoansTable.vue */}
    </div>
  );
}
