import "./ProposalDetailsModal.scss";

interface ProposalDetailsModalProps {
  t?: (key: string, params?: Record<string, string | number>) => string;
  [key: string]: unknown;
}

export default function ProposalDetailsModal(props: ProposalDetailsModalProps) {
  return (
    <div className="ProposalDetailsModal">
      {/* Migrated from ProposalDetailsModal.vue */}
    </div>
  );
}
