/**
 * DashboardPanel.tsx -- NeoBurger DashboardPanel (React)
 *
 * Converted from Vue. Tab-level panel rendered by the platform shell.
 */

export default function DashboardPanel() {
  return (
    <div className="dashboardpanel-panel">
      {/* DashboardPanel content - rendered by platform tab system */}
    </div>
  );
}
