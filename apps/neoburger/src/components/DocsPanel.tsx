/**
 * DocsPanel.tsx -- NeoBurger DocsPanel (React)
 *
 * Converted from Vue. Tab-level panel rendered by the platform shell.
 */

export default function DocsPanel() {
  return (
    <div className="docspanel-panel">
      {/* DocsPanel content - rendered by platform tab system */}
    </div>
  );
}
