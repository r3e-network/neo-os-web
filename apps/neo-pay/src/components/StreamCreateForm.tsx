import "./StreamCreateForm.scss";

interface StreamCreateFormProps {
  t?: (key: string, params?: Record<string, string | number>) => string;
  [key: string]: unknown;
}

export default function StreamCreateForm(props: StreamCreateFormProps) {
  return (
    <div className="StreamCreateForm">
      {/* Migrated from StreamCreateForm.vue */}
    </div>
  );
}
