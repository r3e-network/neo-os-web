# AA Permissions Lab

Inspect and update verifier / hook bindings on the shared Neo AA core.

## What It Does

- reads current verifier / hook / backup owner for an `accountId`
- submits `updateVerifier`
- submits `updateHook`

This miniapp is scoped to permission and policy changes only.
