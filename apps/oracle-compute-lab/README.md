# Oracle Compute Lab

Run registered Morpheus compute jobs from a dedicated miniapp surface.

## What It Does

- submits a named compute script through the shared compute gateway
- shows job status, attestation, and output payload
