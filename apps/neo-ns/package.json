{
  "name": "miniapp-neo-ns",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "dev:h5": "vite -p h5",
    "build": "vite build",
    "test": "npx vitest run --config vite.config.ts src/nnsProduction.test.ts src/PlayArea.production.test.tsx"
  },
  "dependencies": {},
  "devDependencies": {
    "sass": "catalog:",
    "terser": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:"
  }
}
