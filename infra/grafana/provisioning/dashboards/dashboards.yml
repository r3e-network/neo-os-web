apiVersion: 1

providers:
  - name: 'Neo Service Layer'
    orgId: 1
    folder: 'Service Layer'
    folderUid: 'service-layer'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
