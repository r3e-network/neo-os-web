[package]
name = "service_layer_devpack"
version = "0.6.0"
edition = "2021"
description = "Devpack helpers for Service Layer functions (Rust experimental)."
license = "MIT"
readme = "README.md"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
