---
# Application for all Neo* services (production overlay)
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: neo-services-prod
  namespace: argocd
  labels:
    app.kubernetes.io/name: neo-services
    app.kubernetes.io/part-of: service-layer
    environment: prod
spec:
  project: default

  source:
    repoURL: https://github.com/r3e-network/neo-miniapp-platform.git
    targetRevision: HEAD
    path: k8s/overlays/prod
    kustomize:
      commonLabels:
        environment: prod

  destination:
    server: https://kubernetes.default.svc
    namespace: service-layer

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

  revisionHistoryLimit: 10

---
# Application for dev environment
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: neo-services-dev
  namespace: argocd
  labels:
    app.kubernetes.io/name: neo-services
    app.kubernetes.io/part-of: service-layer
    environment: dev
spec:
  project: default

  source:
    repoURL: https://github.com/r3e-network/neo-miniapp-platform.git
    targetRevision: HEAD
    path: k8s/overlays/dev
    kustomize:
      commonLabels:
        environment: dev

  destination:
    server: https://kubernetes.default.svc
    namespace: service-layer

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

  revisionHistoryLimit: 5
