apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: supabase

resources:
  - namespace.yaml
  - configmap.yaml
  - secrets.yaml
  - postgres.yaml
  - postgrest.yaml
  - gotrue.yaml
  - services.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/part-of: supabase
