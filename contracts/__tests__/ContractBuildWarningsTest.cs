using Xunit;

namespace NeoMiniAppPlatform.Contracts.Tests
{
    /// <summary>
    /// Placeholder — contract build warning tests were removed along with
    /// the contracts they validated. Custom contracts that remain (LastSurvivor,
    /// SelfLoan, etc.) are validated by their own test methods in other files.
    /// </summary>
    public class ContractBuildWarningsTest
    {
        [Fact]
        public void PlaceholderPassesBecauseNoDeletedContractsRemain()
        {
            Assert.True(true);
        }
    }
}
