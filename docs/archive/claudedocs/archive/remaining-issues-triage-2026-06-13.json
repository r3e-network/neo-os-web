[
  {
    "area": "dice-game-standalone",
    "is_real": true,
    "verdict": "fix-now",
    "layer": [
      "contract-new",
      "frontend",
      "onchain-op-testnet",
      "onchain-op-mainnet"
    ],
    "feasible_now": true,
    "evidence": "CONFIRMED REAL DEFECT. Current Neo N3 dice routes through kernel PlatformGame (testnet 0x740671b1 / mainnet 0xa7840a8d). apps/dice-game/src/main.tsx:393-410 calls invokeWithPayment(\"placeDiceBet\", memo \"miniapp-dice-game:stake\") → kernel PlatformGame.Dice.cs:107 RequestOracleForCallback(\"vrf_random\"). The GAS is consumed into a DiceBet BEFORE the VRF callback: PlatformGame.Dice.cs:88 ConsumeDirectGasCredit(appId,player,amount) deletes the direct-gas-credit ledger entry, then the amount lives inside an unresolved DiceBet struct (Resolved=false). Settlement is ONLY via ResolveDiceBet/RefundDiceBetFromOracle, both gated by ValidateOracle() (lines 122,182). The kernel's WithdrawGasCredit (PlatformGame.Credit.cs:94) reclaims only UNCONSUMED credit — it cannot touch an already-consumed bet — so a player has NO path to recover a stranded bet. main.tsx:441-460 documents exactly this (\"the deposit landed but the bet didn't settle… the contract exposes no withdraw, so 'credit' — not 'refund' — is honest\").\nORACLE IS DOWN (the strand trigger): read-only curl oracle.meshmini.app/v1/status → {\"error\":\"runtime_temporarily_degraded\",\"message\":\"Phala runtime origin is unavailable\"}; edge.meshmini.app/health → {\"degraded\":true,\"reason\":\"phala_runtime_control_plane_disabled\"}. So every N3 bet placed today is permanently stranded.\nKERNELS LIVE: testnet getDiceBetLimits returns HALT on 3 independent endpoints (testnet1.neo.coz.io, nspcc, neoline). Mainnet dice kernel 0xa7840a8d holds 1.4479624 GAS (balanceOf via mainnet1.neo.coz.io) — real liquidity already deposited.\nTEMPLATE READY: contracts/MiniAppCoinFlip/MiniAppCoinFlip.cs is the exact pattern — atomic Flip() via Runtime.GetRandom (lines 156-160), deposit-then-bet OnNEP17Payment memo (lines 99-123, credit-only, no transfers/reverts beyond memo assert — matches the OnNEP17Payment safety rule), house bankroll with \"bankroll >= amount\" max-payable guard (line 151), Withdraw(account) credit-refund (lines 220-233), WithdrawBankroll owner-only (lines 202-215). Owner = NR3E4D8N via [InitialValue]. Built NEF exists at contracts/build/MiniAppCoinFlip.{nef,manifest.json}; deployed+live-validated for fogplay (deploy/scripts/live_validate_coinflip.mjs: PASS, exactly solvent).\nEVM SIDE: keep it. Neo X mainnet MiniAppDiceGameEVM 0xFA795F81 has code (eth_getCode = 12570 bytes, deployed); dice-logic.ts:7-12 + main.tsx:366-374 show it settles atomically via getBet status (revert returns value) — no strand. Migration is N3-only.\nTOOLCHAIN: dotnet present (/opt/homebrew/bin/dotnet, ~/.dotnet 10.0.107); nccs installable as dotnet tool (prior migrations); TestEngine harness contracts/__tests__/MiniAppCoinFlipTests.cs exists to model for local NeoVM validation without testnet funds; deploy/scripts/deploy_contract_neonjs.mjs (testnet-pinned) + deploy_contract_mainnet.mjs proven for the prior 10 migrations.",
    "exact_fix": "YES — migrate to a standalone MiniAppDiceGame N3 contract modeled exactly on MiniAppCoinFlip; VRF is NOT preferable here (it is the source of the strand and is non-operational). Keep the EVM path unchanged.\n\n1) NEW CONTRACT contracts/MiniAppDiceGame/ (copy MiniAppCoinFlip + .Reads.cs):\n   - Rename class/DisplayName MiniAppDiceGame; keep [InitialValue] Owner = NR3E4D8NUXh3zhbf5ZkAp3rTxWbQqNih32; ContractPermission GAS transfer.\n   - Memos: FUND_MEMO \"miniapp-dice-game:fund\", BET_MEMO \"miniapp-dice-game:stake\" (match the frontend's existing `${appId}:stake`).\n   - Replace Flip(player,choice,amount) with Roll(player, BigInteger face, amount): assert face>=1 && face<=6 (replaces choice 0/1). MIN_BET 5_000_000 (0.05), MAX_BET 2_000_000_000 (20 GAS, matches manifest/kernel DI_MAX_BET).\n   - Payout: 5.70x = 6 faces * 95% (5% house edge). To stay integer/atomic: payout = amount * 570 / 100 = amount * 57 / 10. Use BigInteger payout = amount * 57 / 10 (base units, exact for typical stakes; the extra-from-house = payout - amount = amount*47/10). Max-payable guard: assert bankroll >= (payout - amount) i.e. bankroll >= amount*47/10 BEFORE accepting, so every win is fully payable (analogous to CoinFlip's bankroll>=amount). On win: bankroll -= (payout-amount); transfer payout. On loss: bankroll += amount.\n   - Outcome: BigInteger rolled = (entropy + mix) % 6 + 1 (1..6), same entropy mix as CoinFlip (Runtime.GetRandom + player + Runtime.Time + gameId). won = rolled == face. To avoid modulo bias on a 1..6 over a 256-ish space, GetRandom returns a full BigInteger so bias is negligible; if strictness wanted, mirror the kernel's rejection-sampling comment but it is unnecessary for GetRandom's wide range.\n   - Keep Withdraw(account) credit-refund, WithdrawBankroll(to,amount), Bankroll(), CreditOf(player), GetStats, GetGame, GetPlayerGames verbatim (rename Choice→Face in Game struct; event Rolled(gameId,player,face,rolled,won,payout)).\n   - Split to partials if >300 lines (keep .Reads.cs) to satisfy ContractPartialFilesStayReviewable.\n\n2) LOCAL VALIDATION: contracts/__tests__/MiniAppDiceGameTests.cs modeled on MiniAppCoinFlipTests.cs — fund bankroll, deposit credit, roll matching+non-matching face, assert 5.70x payout on win / bankroll grows on loss / AssertSolvent / Withdraw refund. Run `DOTNET_ROOT=$HOME/.dotnet dotnet test --filter ~MiniAppDiceGame`. (Do NOT put transfers/business reverts inside OnNEP17Payment — credit-only there.)\n\n3) DEPLOY TESTNET: `dotnet tool restore`/install nccs → build NEF to contracts/build/ → NEO_TESTNET_WIF=… node deploy/scripts/deploy_contract_neonjs.mjs contracts/build/MiniAppDiceGame.nef contracts/build/MiniAppDiceGame.manifest.json. Note: hash = Hash160(deployer + nef.checksum + name) is network-independent, so the testnet hash == future mainnet hash for the same NEF+deployer.\n\n4) LIVE-VALIDATE TESTNET: deploy/scripts/live_validate_dicegame.mjs modeled on live_validate_coinflip.mjs — fund bankroll, deposit, ~6 rolls on face=6, assert per-roll Rolled events + 5.70x payout + exact solvency (held GAS == bankroll, no strand).\n\n5) FRONTEND REWIRE apps/dice-game/src/main.tsx (N3 branch only; EVM branch lines 358-374 unchanged):\n   - Replace the kernel placeDiceBet path (lines 375-412): use ctx.services.chain.invokeWithPayment(amountFixed8, \"miniapp-dice-game:stake\", \"roll\", [player, face, amount]) — wait for \"Rolled\" event; outcome is SYNCHRONOUS in the same tx (no resolver poll). Drop resolveN3Bet / DiceBetResolved / DiceBetRefunded polling and the stakeSent \"recoverable credit\" catch (lines 263-290, 437-460) for N3 — there is no pending state anymore. Keep them only for the EVM branch.\n   - refreshLiquidity (lines 116-153): read standalone bankroll() instead of GAS.balanceOf(contract), and creditOf(player) instead of getDirectGasCredit(appId,player). Recompute maxPayableStake against the new cover multiple (47/10 extra-from-house, or keep 5.7 conservatively). The pre-flight guard (lines 381-390) stays.\n   - fundGameCredit action: change memo to \"miniapp-dice-game:fund\" if it is meant to fund the house bankroll, or \"miniapp-dice-game:stake\" if it pre-funds player bet credit (clarify the operation_panel \"Fund Stake\" intent — currently it funds player credit; keep stake memo).\n   - Add a withdrawCredit action wired to the standalone Withdraw(account) (model fogplay/src/main.tsx:36-37 + composables/useCoinFlip.ts) so stranded/over-deposited credit is now genuinely REFUNDABLE — this is the core fix.\n   - Update manifest.ts/neo-manifest.json: contract mode already \"custom\"; flip technologies.oracle.enabled→false, permissions.randomness stays true (Runtime.GetRandom), update description (drop \"Morpheus VRF\", say \"atomic on-chain roll\"). Update neo-manifest contracts.neo-n3-{testnet,mainnet} to the new hash; remove the runtime.modules platform-game binding (becomes a self-contained app like red-envelope, whose neo-manifest has no runtime.mode/technologies.oracle).\n   - Update apps/shared/constants/rpc.ts MINIAPP_CONTRACTS fallback for miniapp-dice-game and the LIVE_CHAIN_FLOWS harness map (per prior-migration practice).\n\n6) BUILD+TEST GATE: build 60/60 (vite) + full `npm test` green. Then DEPLOY MAINNET via deploy/scripts/deploy_contract_mainnet.mjs (same NEF → same hash); fund the new mainnet bankroll; optionally migrate the 1.45 GAS from the old kernel via WithdrawGameLiquidity if such an owner path exists (else leave it — it is small). Update neo-n3-mainnet hash + redeploy the web app.\n\nVRF-vs-GetRandom verdict: GetRandom (atomic, in-tx) is the correct choice for low-stakes dice — it removes BOTH the oracle dependency and the strand, exactly as done for fogplay/coinflip. VRF would only matter for higher-assurance/high-stakes fairness, which the MAX_BET=20 GAS cap does not warrant; and the VRF settler is down with no committed ETA.",
    "cost": "Agent effort: ~1 new C# contract (~250 lines, mostly copied from CoinFlip) + 1 TestEngine test + 1 live-validate script + N3-branch frontend rewire (delete poll/strand code, add withdrawCredit, swap reads) + manifest edits. Deploy count: 2 (testnet once, mainnet once — same NEF, same hash). GAS: testnet deploy ~10 GAS + small bankroll fund from NR3E4D8N (has testnet GAS); mainnet deploy ~10 GAS + a bankroll seed (e.g. 2-5 GAS) from NR3E4D8N's mainnet balance. nccs install via `dotnet tool`. Comparable to each of the prior 10 OS-path migrations (~half a day each).",
    "risk": "Software + testnet steps are fully reversible and low-risk (no real value; the leaked key already controls testnet). The standalone pattern is proven (16 apps, security-reviewed 0 critical/high). Residual risks: (1) GetRandom is block-producer-influenceable — acceptable at 20 GAS cap, same trade-off accepted for CoinFlip and documented in its header. (2) Payout 5.70x as amount*57/10 truncates sub-deci-base-unit dust on odd stakes — negligible, rounds in the house's favor, must be covered by the bankroll guard. (3) MAINNET deploy spends real GAS from the KNOWN-LEAKED NR3E4D8N key and seeds a real-GAS bankroll that the leaked key's WithdrawBankroll can drain — same accepted risk as the existing 10 mainnet contracts (memory notes user accepted, recommends rotating owner). (4) The old mainnet kernel keeps its 1.45 GAS + any already-stranded bets; this migration repoints the frontend off it but does not auto-refund pre-existing strands (no oracle to do so) — acceptable, small, pre-existing. Blast radius limited to the dice-game app + its own contract.",
    "needs_user_decision": true,
    "blockers": [
      "Mainnet deploy (step 6) spends real mainnet GAS from the known-leaked NR3E4D8N key and creates a real-GAS bankroll the leaked key can drain — needs the same explicit user go-ahead already given for the prior 10 mainnet contracts (the testnet contract + frontend rewire + local validation are fully feasible now with no decision). Recommend rotating contract owner to a cold/multisig before or alongside mainnet, per memory.",
      "nccs is not on PATH — must `dotnet tool install`/restore the Neo.Compiler.CSharp tool first (feasible now, no creds needed)."
    ]
  },
  {
    "area": "daily-checkin-pool-funding",
    "is_real": true,
    "verdict": "blocked-external",
    "layer": [
      "onchain-op-mainnet",
      "onchain-op-testnet",
      "external-creds"
    ],
    "evidence": "Read-only RPC against both deployed contracts (testnet 0xaba84da240a55410d284a656fc8dae044e6ec1a5, mainnet 0xbd4f3646e189350b9c11a659655854e6f03f9be4; manifest name MiniAppDailyCheckIn). KEY FACTS:\n1. claimRewards(user)->Void exists and pays from the contract's OWN GAS balance (RewardsClaimed event); frontend useCheckin.ts:719 + solvency test confirm this is the intended model. So the funding premise is right.\n2. Reward amounts are FIXED and NOT tunable: getPlatformStats returns weekReward=100000000 (1 GAS @ day7), twoWeekReward=200000000 (2 GAS @ day14), checkInFee=100000 (0.001 GAS), resetAfterDays=14. The 48-method ABI has NO setRewardRate/setWeekReward/fund/deposit/withdraw. Only governance setters are admin/oracle/pauseRegistry/paymentHub/timelock/automation/abstractAccount + setPaused + update(nef,manifest,data) + destroy.\n3. Live pools: testnet balanceOf(contract) on GAS = 187000000 (1.87 GAS); mainnet = 1500000 (0.015 GAS). totalRewarded=0 on BOTH nets — no reward has ever been paid. Mainnet pool is exactly 66.67x short of one 1-GAS milestone (1500000 vs 100000000) — matches the memory '~66x short'.\n4. The pool balance EXACTLY equals accumulated check-in fees: testnet totalCheckins=1870 ×100000 = 187000000; mainnet 15 ×100000 = 1500000. Proves the ONLY GAS inflow is check-in deposits; economics are structurally insolvent (each check-in adds 0.001 GAS, a 7-day streak owes 1 GAS = 1000x).\n5. FUNDING-BY-TRANSFER IS IMPOSSIBLE. NEF bytecode of onNEP17Payment contains guards 'unsupported asset', 'amount must be > 0', 'invalid payment memo', 'insufficient prepaid gas'. The only accepted deposit memo is the exact 28-char string 'miniapp-dailycheckin:checkin' (PUSHDATA1 0x1c, exact-equality, not prefix). Direct test-invoke of onNEP17Payment FAULTs ('unsupported asset'). A plain GAS transfer with no/other memo → 'invalid payment memo' → onNEP17 aborts → NEP-17 rolls back the whole transfer → funds returned. There is no fund/donate entry point.\n6. Admin is NOT the deployer: testnet admin = NM2SKt6s9hS7wan475SkXDFD4TPCTE2f18 (561cc063...), mainnet admin = NWpKxAGFuJ4B5m1EvnmCP19QrTAdYCo2Bc (ecf8b9cc...). Neither equals the leaked NR3E4D8N (6d0656f6dd91469db1c90cc1e574380613f43738). isPaused=false both nets, timeLockDelay=86400. No source for this contract exists anywhere in the r3e repos (it's the abandoned kernel-dependent generation), so update() is doubly blocked (no source + no admin key).\n7. The frontend gate is correct and LIVE: PlayArea.tsx:99-100 claimBlocked=isPaused||claimableButUnfunded, Claim CTA disabled when pool<accrued, honest underfunded banner (rewardsUnderfunded). This is the honest, correct current behaviour.",
    "exact_fix": "The naive operator fix (\"deployer transfers GAS to the contract\") DOES NOT WORK — onNEP17Payment rejects any deposit without the exact 'miniapp-dailycheckin:checkin' memo, so a funding transfer reverts. And rewards cannot be tuned down: there is no setRewardRate, and update() is blocked (no source + admin is an unknown key, not NR3E4D8N). Therefore there is NO direct on-chain op that funds or fixes this deployed contract with the keys/tools available.\n\nConcrete paths, in order of preference:\nA. RECOMMENDED — keep the frontend honest gate (already done) and treat the reward feature as unfunded/disabled until a proper standalone contract replaces it. The current state (Claim disabled when underfunded, honest banner, totalRewarded=0) is correct and ships safely. No on-chain op needed. Optionally lower the manifest's advertised rewards copy so the UI does not promise 1/2 GAS milestones it cannot pay (apps/daily-checkin/neo-manifest.json description + MILESTONES in useCheckin.ts:62 are the advertised values).\nB. PROPER FIX (separate initiative, follows the established self-contained pattern like MiniAppCoinFlip/MiniAppTipJar) — write contracts/MiniAppDailyCheckin/*.cs with: deposit-then-act check-in via OnNEP17Payment (credit-only, memo 'miniapp-dailycheckin:checkin'), streak/milestone tracking, claimRewards paying from a house bankroll, an owner-only fundPool path (OnNEP17Payment accepts a 'miniapp-dailycheckin:fund' memo → adds to bankroll), an owner setRewardRate(week,twoWeek) to right-size rewards to the funded bankroll, and Withdraw(account) credit-refund. Owner = a dedicated cold/multisig key (NOT NR3E4D8N). Validate locally via contracts/__tests__ TestEngine (never transfer/revert inside OnNEP17Payment — credit-only there). Deploy testnet → live-validate → rewire apps/daily-checkin to the new hash → deploy mainnet with a funded bankroll. Sustainable funding sizing once setRewardRate exists: if you keep 1/2 GAS milestones, budget ~3 GAS per user who reaches a 14-day streak; for the current ~3-5 real users that's ~15 GAS to be safe, but the right move is to set rewards far lower (e.g. weekReward=0.05 GAS, twoWeekReward=0.1 GAS) so the 0.001 GAS fee economics are not 1000x underwater — then a 10-20 GAS bankroll lasts.\nC. If the existing deployed contract must be reused: this requires the UNKNOWN admin key (NM2.../NWp...) which the user does not appear to control, to call update() with a recompiled NEF — but the source does not exist. Not feasible.",
    "feasible_now": false,
    "needs_user_decision": true,
    "risk": "reversibility: the only safe-now action (option A) is fully reversible/read-only; option B mainnet bankroll is an irreversible real-GAS spend. real-value: mainnet GAS bankroll + leaked-key-as-owner drain risk. blast radius: current honest gate = none.",
    "cost": "Option A: 0 GAS, ~0 effort (already shipped). Optional manifest/MILESTONES copy edit: pure software, no deploy. Option B (proper standalone contract): ~1 new contract (4-6 .cs partials), local TestEngine validation, 1 testnet deploy + 1 mainnet deploy, plus a mainnet GAS bankroll (recommend right-sizing rewards down so 10-20 GAS suffices rather than overfunding). Requires a NON-leaked owner key the user must provision (external cred). Option C: infeasible (no source, admin key not controlled).",
    "blockers": [
      "No C# source for the deployed MiniAppDailyCheckIn contract exists in any r3e repo (deployed-only artifact of the abandoned kernel-dependent generation) — cannot recompile/update it.",
      "The contract admin (testnet NM2SKt6s9hS7wan475SkXDFD4TPCTE2f18 / mainnet NWpKxAGFuJ4B5m1EvnmCP19QrTAdYCo2Bc) is an unknown key, NOT the leaked NR3E4D8N deployer — setPaused/update/proposeAdmin are not callable.",
      "onNEP17Payment rejects all deposits except the exact 'miniapp-dailycheckin:checkin' memo, so the contract cannot be funded by a plain GAS transfer (the 'simple operator fix' is structurally impossible).",
      "No setRewardRate/setWeekReward method in the 48-method ABI — rewards cannot be tuned down without an update() (blocked above).",
      "Proper fix (option B) needs a dedicated non-leaked owner key for the new contract's bankroll, which the user must provision (external cred), plus a mainnet GAS spend decision."
    ]
  },
  {
    "area": "custom-anchor-agent-provisioning",
    "is_real": true,
    "verdict": "fix-now",
    "layer": [
      "frontend",
      "onchain-op-testnet",
      "onchain-op-mainnet"
    ],
    "feasible_now": true,
    "needs_user_decision": false,
    "evidence": "PARTIAL DEFECT — provisioning exists but the standalone miniapp does not use it.\n\nCONTRACT (already deployed, fully capable): contracts/platform/PlatformAnchor/PlatformAnchor.Agents.cs exposes RegisterAgent(appId, agentAccount, candidate, verificationScriptHash) (line 14) AND batch RegisterAgents(appId, UInt160[] accounts, ECPoint[] candidates, ByteString[] scriptHashes) (line 36, capped at 21, line 44). Both are gated by ValidateAppAuthority(appId) (Internal.cs:40-48), which accepts EITHER the platform Admin() OR the per-app appAdmin witness. Admin() is set to the deploy tx sender (_deploy, PlatformAnchor.cs:100) = NR3E4D8N. So the deployer can provision agents for ANY app; the app's own admin (the wallet that registered it) can also self-provision.\n\nHOST-APP PATH IS COMPLETE: platform/host-app/lib/custom-anchor.ts buildCustomAnchorRegistrationPlan() (line 54) builds a 3-call atomic batch: aaCore.registerAccounts (21 deterministic AA agent accounts) + anchor.registerCustomAnchorApp + anchor.registerAgents(appId, 21 accounts, 21 candidate keys, 21 scriptHashes). It requires exactly 21 unique compressed candidate pubkeys (parseAnchorCandidateKeys, line 32). useMiniAppDetailInvoke.ts:207-246 wires method 'registerCustomAnchor' to submit this via adapter.invokeMultiple. The miniapp-definition public/miniapp-definitions/custom-anchor.json:94-133 declares 'registerCustomAnchor' with params slug/nonce/mode/candidates ('21 candidate public keys'). So the host detail page DOES provision 21 agents in one tx.\n\nSTANDALONE MINIAPP IS THE GAP: apps/custom-anchor/src/main.tsx registerAction('register') (line 342-389) ONLY calls registerCustomAnchorApp(appId, mode, appAdmin) (line 377) after a 1-GAS fee deposit. It NEVER collects candidate keys and NEVER calls registerAgents → result is a registered anchor with agentCount=0. PlayArea.tsx confirms: dispatch('register', {anchorAppId, mode}) (line 159) with no candidates field; the 0/21 callout + stake gate (lines 43, 95, 286-301) and locale registerAgentsNote ('a new anchor starts with 0 of its 21 AA agents... provisioned by the anchor operator (admin console)', messages.ts:113-116) are an honest band-aid, not a fix. The manifest still advertises 'user-owned 21-agent NEO voting anchor' (manifest.ts:5).\n\nYIELD LINKAGE: provisioning agents is NECESSARY but not alone SUFFICIENT for yield. Staking earns via the MasterChef accumulator in Staking.cs: rewards only accrue when the operator calls HarvestRewards/FundRewards (Staking.cs:46-74) to inject GAS, distributed pro-rata by stake (DistributeRewards, Internal.cs:97). The agents enable VoteAgent (Agents.cs:91) → NEO.Vote(agentAccount, candidate) so the anchor's NEO actually votes and generates Neo GAS rewards, which the operator then harvests in. With 0 agents, VoteAgent reverts (ValidateAgent agentId<=count, Internal.cs:65) → no vote, no yield source at all. So a 0-agent anchor genuinely earns nothing.\n\nLIVE STATE (read-only RPC, testnet 0xab079b4f9a0a2471d136392e25eb8e99898dcad0): getAgentCount('miniapp-trustanchor')=21 (HALT); getAnchorStats('miniapp-profitanchor') HALT mode=2, totalStaked=0. Both FLAGSHIP anchors ARE fully provisioned (testnet_anchor_deployment.json lists all 42 agent AA accounts + 42 'Register *-agent-N' txids + 'Set Anchor AA verify scopes'). So trustanchor/profitanchor are fine; only user-created custom anchors via the standalone app are born inert. mainnet_anchor_deployment.json exists (same deployer-derived hashes).\n\nThis is a one-time provision per anchor (21 agents), not ongoing. It does NOT require external validator NEO votes/setup at registration — candidates are just public keys the operator picks; the actual NEO voting happens later via VoteAgent once users stake and the operator/AA witness triggers it.",
    "exact_fix": "Two coordinated fixes; no contract change needed (RegisterAgents already deployed on testnet+mainnet).\n\nFIX 1 — Standalone app provisions 21 agents at registration (primary):\nFile apps/custom-anchor/src/main.tsx registerAction('register') (line 342): after registerCustomAnchorApp, add a registerAgents call so a newly created anchor is born with 21 agents. Mirror platform/host-app/lib/custom-anchor.ts exactly:\n  (a) Add a 'candidates' field (21 compressed pubkeys, one per line) to the register form in apps/custom-anchor/src/PlayArea.tsx (the registerInput panel, ~line 486-527) + locale keys in src/locale/messages.ts; validate with the same rules as parseAnchorCandidateKeys (exactly 21 unique /^(02|03)[0-9a-fA-F]{64}$/).\n  (b) Derive the 21 deterministic AA accounts via the existing deriveAnchorAgentAccounts logic (factor the pure helper out of platform/host-app/lib/custom-anchor.ts into apps/shared so both consumers share it — it has zero host deps, only @noble/hashes).\n  (c) Submit the three calls. Standalone ctx.services.chain only exposes single invoke (no invokeMultiple — verified: only wallet-sdk/aa-market-hub use invokeMultiple), so EITHER (i) call ctx.services.chain.invoke('registerAccounts') on aaCore, then 'registerCustomAnchorApp', then 'registerAgents' as 3 sequential txs with waitForDepositConfirmation between (acceptable — register is already multi-step today), OR (ii) preferred: expose invokeMultiple on the standalone chain service to make it atomic like the host path. aaCore hash for the standalone runtime: source it the same way the host does (EXTERNAL_INTEGRATIONS[network].contracts.aaCore) or add to the app manifest/rpc constants.\n  (d) Update apps/custom-anchor/src/manifest.ts register operation + the 0/21 callout/gate: once provisioning is in the register flow, the gate becomes a fallback for externally-created/under-provisioned anchors rather than the normal case.\n\nFIX 2 — One-time backfill any already-registered-but-inert custom anchors (on-chain op):\nEnumerate AnchorAppRegistered events on 0xab079b4f… (testnet) and the mainnet hash, find appIds with getAgentCount==0, and for each call registerAgents(appId, 21 accounts, 21 candidates, 21 scriptHashes) as the platform Admin (NR3E4D8N) using deploy/scripts/deploy_anchor_testnet.go's ensureAgentSet path (it already implements per-agent registerAgent with derived AA accounts) — or via a small neon-js script reusing buildCustomAnchorRegistrationPlan's agent derivation. Validate locally first with contracts/__tests__ (Neo.SmartContract.Testing TestEngine, dotnet test, DOTNET_ROOT=$HOME/.dotnet) since the deployer has limited testnet NEO; agent registration itself costs only GAS, not NEO.\n\nNote: the operator must also fund/harvest (HarvestRewards/FundRewards) and call VoteAgent for staking to actually yield — provisioning alone makes staking non-inert (votable) but ongoing yield depends on operator harvesting. Document this in the reward-model copy (already partially present in rewardModelBody).",
    "risk": "Low–moderate, mostly reversible. Software change to one standalone app + a shared helper extraction; no contract redeploy. FIX 1 only changes how the register tx is composed; existing flagship anchors (trust/profit, already 21/21) are untouched. FIX 2 is additive on-chain (registering agents to an app that has none) — agents start active but inert until staked/voted; mis-chosen candidate keys are correctable via SetAgentCandidate (admin-only, Agents.cs:56). Testnet backfill is free of real value. Mainnet backfill spends only GAS (no NEO), is owner-gated, and is additive (no fund movement, no user balances touched). Blast radius limited to custom anchors; flagship anchors and all other miniapps unaffected. The candidate-key UX adds friction (user must supply 21 valid council pubkeys) — consider a sensible default candidate set for Profit mode like the deploy script's defaultCandidate.",
    "cost": "FIX 1: ~1 focused frontend session (extract shared agent-derivation helper, add candidates field + validation + 3-call wiring, locale, manifest copy, tests in apps/shared/test/custom-anchor.playarea.test.tsx). Pure software, no chain spend. FIX 2 (backfill): per inert anchor, 1 registerAccounts + 1 registerAgents tx (≈ a few GAS each in sysfee for 21-element arrays); testnet free, mainnet a few GAS from the deployer (currently ~216 GAS mainnet balance per memory). Local TestEngine validation: no funds. needs_user_decision=false: testnet ops + pure software need no decision; the mainnet backfill is owner-gated, GAS-only, additive, and uses the already-accepted NR3E4D8N owner without moving real value — but flag it for the user if they want to gate any mainnet write."
  },
  {
    "area": "neo-swap-real-settlement",
    "is_real": true,
    "verdict": "fix-needs-decision",
    "layer": [
      "contract-new",
      "contract-update",
      "frontend",
      "onchain-op-testnet"
    ],
    "feasible_now": false,
    "needs_user_decision": true,
    "exact_fix": "PROBLEM (verified read-only): neo-swap is honestly preview-only. apps/neo-swap/neo-manifest.json has `contracts: {}`; useSwapEngine.ts gates the CTA on routerAvailable = Boolean(chain.contractAddress.get()) (lines 63-67) and shows t(\"swapRouterUnavailable\"). The frontend already has FULL settlement wiring waiting for a contract: executeSwap() (lines 338-385) calls chain.invoke(\"swapTokenInForTokenOut\", [sender, amountInt, minOutputInt, path, deadline], {waitForEvent:\"SwapExecuted\"}). It also derives the rate from the on-chain MorpheusDataFeed via useMorpheusDataFeed.getPriceWithMeta (lines 278-282). TWO independent defects make a real swap non-trivial, and one is a hard blocker.\n\nDEFECT 1 (the feed is worse than \"frozen\" — it is GONE): Both MorpheusDataFeed contracts return \"Called Contract Does Not Exist\" on the platform's own RPC (api.n3index.dev) AND on mainnet1.neo.coz.io. Verified getLatest(\"TWELVEDATA:NEO-USD\"): mainnet 0x9bea75cf702f6afc09125aa6d22f082bfd2ee064 → FAULT \"called contract ... not found\"; testnet 0x03013f49c42a14546c8bbe58f9d434c3517fccab → FAULT \"not found\". So a feed-priced swap is impossible today on either net; the price source must be owner-set (like SelfLoan's neoPrice), not the feed.\n\nDEFECT 2 (HARD BLOCKER — no NEO reserves possible): A reserve/AMM swap must hold BOTH NEO and GAS to fill both directions. Verified deployer/owner NR3E4D8NUXh3zhbf5ZkAp3rTxWbQqNih32 balances on all four RPC endpoints: TESTNET NEO=0, GAS=9007; MAINNET NEO=0, GAS=90.9. The owner has ZERO NEO on both networks. You cannot seed a NEO reserve from a 0-NEO key, so the NEO→GAS direction can quote but never settle (and GAS→NEO would immediately drain the only NEO ever deposited by a user). A house-bankroll swap with no inventory in one leg is structurally broken.\n\nCONTRACT SKETCH (if/when NEO is obtained): MiniAppSwap : SmartContract, modeled on contracts/MiniAppCoinFlip + contracts/MiniAppSelfLoan (which already does dual-token: [ContractPermission GAS-hash \"transfer\"] + [ContractPermission NEO-hash 0xef4073a0... \"transfer\"], OnNEP17Payment branching on Runtime.CallingScriptHash == NEO.Hash vs GAS.Hash, NEO as integer). Storage: PREFIX_NEO_RESERVE, PREFIX_GAS_RESERVE, PREFIX_GAS_PER_NEO (owner-set, GAS base-units per 1 NEO), PREFIX_SPREAD_BPS, per-user PREFIX_CREDIT. OnNEP17Payment: CREDIT ONLY by memo (\"swap:fund-neo\"/\"swap:fund-gas\" → reserves; \"swap:in\" → user credit) — NEVER transfer or business-revert inside OnNEP17Payment (crashes the TestEngine host per repo lesson). Direct method swapNeoForGas(user, neoAmount, minGasOut): assert witness(user), consume NEO credit, gasOut = neoAmount * gasPerNeo * (10000-spreadBps)/10000, assert gasReserve >= gasOut && gasOut >= minGasOut, effects-before-interaction (write reserves, then GAS.transfer to user), emit SwapExecuted(user, NEO, GAS, neoIn, gasOut). swapGasForNeo symmetric (NEO is integer → floor). Owner: fundReserve via memo, withdrawReserve(to, token, amount) gated on CheckWitness(Owner). Withdraw(account) credit-refund per CoinFlip. CRITICAL FRONTEND MISMATCH: the contract's swap entrypoint must be named/shaped to match the frontend, OR rewire useSwapEngine.ts: the frontend currently calls \"swapTokenInForTokenOut\" with a Uniswap-style path Array + deadline (router semantics), which does NOT match the simple reserve model — rewire executeSwap (lines 367-373) to call swapNeoForGas/swapGasForNeo and the rate source (lines 272-299) to read the contract's getQuote instead of the dead feed, plus set manifest contracts.{testnet,mainnet}. Local validation: contracts/__tests__/MiniAppSwapTests.cs with Neo.SmartContract.Testing TestEngine (fund NEO from engine.ValidatorsAddress, not testnet — deployer has 0 NEO anyway), dotnet test, DOTNET_ROOT=$HOME/.dotnet.\n\nRESERVE-SEEDING COST: contract = 0 (NeoVM-native, no oracle). Deploy ~10 GAS/network (have 9007 testnet / 90.9 mainnet — fine). But the BINDING cost is NEO inventory: a usable two-sided market needs, say, 50-200 NEO PLUS matching GAS per network seeded into reserves. The owner has 0 NEO on both nets, so this requires the user to acquire/transfer NEO (external value the user controls) — that is why this needs a decision, not just for mainnet but even for a meaningful testnet demo.",
    "risk": "Software (new contract + frontend rewire) is fully reversible and low-risk; local TestEngine validation needs no funds. The real risk is economic, not code: (1) a reserve swap with owner-set price and no live feed is a fixed-price desk — anyone can arbitrage the spread against the real market and drain whichever reserve is mispriced (the SelfLoan owner-set-neoPrice model has the same exposure but smaller surface). (2) On mainnet the reserves are real-value GAS+NEO owned by the KNOWN-LEAKED NR3E4D8N key (WIF is in .env and transcripts) — anyone with the WIF calls withdrawReserve and drains it; same leaked-owner risk already flagged for self-loan/coinflip mainnet pools. (3) Mismatch between the existing frontend router ABI (\"swapTokenInForTokenOut\"/path/deadline) and a simple reserve contract will silently break settlement if not rewired. Blast radius is bounded by the seeded reserve size.",
    "cost": "Contract authoring: ~1 contract (~250-300 lines, clone of CoinFlip+SelfLoan) + ~1 test file; frontend rewire of useSwapEngine.ts (rate source + executeSwap + manifest). Deploy: ~10 GAS per network (affordable from existing balances). DECISIVE COST: NEO reserve inventory the owner does not have — 0 NEO on testnet and 0 NEO on mainnet (verified on api.n3index.dev + mainnet1.neo.coz.io). Even a testnet demo needs testnet NEO acquired/faucet'd to NR3E4D8N. Mainnet needs real NEO purchased + accepting it sits under a leaked key. Agent effort to build+validate locally: moderate; cannot complete end-to-end without external NEO.",
    "evidence": "Read-only RPC (4 endpoints): getnep17balances NR3E4D8NUXh3zhbf5ZkAp3rTxWbQqNih32 → TESTNET {NEO:0, GAS:9007.20}, MAINNET {NEO:0, GAS:90.91} on both coz.io and api.n3index.dev. getLatest on MorpheusDataFeed → FAULT \"Called Contract Does Not Exist\" for mainnet 0x9bea75cf702f6afc09125aa6d22f082bfd2ee064 and testnet 0x03013f49c42a14546c8bbe58f9d434c3517fccab (registry: apps/shared/constants/generated-morpheus-registry.ts lines 40, 92). Frontend honest-preview state: apps/neo-swap/neo-manifest.json line 24 `contracts: {}`, line 7 description \"No on-chain swap router is deployed yet\"; apps/neo-swap/src/hooks/useSwapEngine.ts routerAvailable (63-67), canSwap gate (158-178), executeSwap router call (338-385). Pattern references: contracts/MiniAppCoinFlip/MiniAppCoinFlip.cs (bankroll, OnNEP17Payment credit-only, Withdraw, owner=InitialValue NR3E4D8N line 52); contracts/MiniAppSelfLoan/MiniAppSelfLoan.cs lines 42-43 (dual NEO+GAS ContractPermission), 104-112 (OnNEP17Payment branch on CallingScriptHash), owner-set neoPrice (no oracle). Existing tests apps/shared/test/neo-swap.logic.test.ts already assert the honest preview + staleness behavior (33-117).",
    "blockers": [
      "Owner key NR3E4D8N holds 0 NEO on testnet AND mainnet (verified) — a two-sided NEO/GAS reserve swap cannot be seeded without externally-acquired NEO the user controls; even a testnet demo needs testnet NEO faucet'd in.",
      "Both MorpheusDataFeed contracts are non-existent on-chain (FAULT 'Called Contract Does Not Exist'), so a feed-priced swap is impossible — pricing must be owner-set, which makes it a fixed-price arb target rather than a market-tracking swap.",
      "Mainnet reserves would sit under the known-leaked NR3E4D8N WIF — drainable by anyone holding it (same risk class already flagged for self-loan/coinflip mainnet pools); needs a dedicated/cold owner before holding real NEO+GAS.",
      "Frontend ABI mismatch: useSwapEngine.executeSwap calls Uniswap-style swapTokenInForTokenOut(path, deadline); a simple reserve contract uses swapNeoForGas/swapGasForNeo — the frontend must be rewired or the contract must mimic the router ABI."
    ]
  },
  {
    "area": "private-transfer-real-settlement",
    "is_real": false,
    "verdict": "not-worth-fixing",
    "layer": [
      "contract-new",
      "infra-edge",
      "infra-box",
      "frontend"
    ],
    "exact_fix": "No defect to fix — the current seal-only state is the correct, honest ceiling for this app as scoped. Turning it into a real confidential transfer is a net-new multi-component product initiative, NOT a deferred-bug fix. Concrete scope IF pursued later:\n\n1. NEW Neo N3 escrow contract (contracts/MiniAppPrivateTransfer/, ~CoinFlip-sized 250-350 lines). It must: (a) accept deposits via OnNEP17Payment with memo \"miniapp-privatetransfer:deposit\" crediting funds keyed by note_commitment (credit-only in OnNEP17Payment per the host-crash lesson); (b) expose requestSettle(commitment) emitting an oracle request the relayer polls; (c) expose a settle/release method gated by a TEE fulfillment signature (verify against the worker's signer via ComputeFulfillmentDigest, the same digest the relayer builds in workers/morpheus-relayer/src/router.js buildFulfillmentDigestBytes), releasing to the recipient that the TEE decrypted from the sealed envelope and returned in the fulfillment result; (d) Withdraw(account) refund path. Validate locally via contracts/__tests__ TestEngine (no testnet NEO needed) before any deploy.\n\n2. NEW relayer/worker route: the existing /oracle/decrypt only returns plaintext to a trusted caller; it does NOT release funds. You would either (a) add an N3 confidential-settlement lane that reads the on-chain request, fetches the stored ciphertext, decrypts in-enclave, and posts a fulfillment with recipient+amount + TEE signature back via neo-n3.js fulfillNeoN3Request; or (b) bind the generic kernel decrypt fulfillment to the new escrow's callback. Requires box env wiring (NEOX_MESSAGE_CONTRACT analog: a new N3_PRIVATE_TRANSFER_CONTRACT) — server-side change to /opt/morpheus/nitro/*.env + service restart.\n\n3. Restore the confidential store backend the CURRENT seal flow already depends on: apps/web /api/confidential/store is Supabase-backed and not served at the public edge (mainnet 'not found'); it also requires isAuthorizedAdminRequest + project_slug which a miniapp caller cannot supply. The seal app degrades to inline_fallback today, which is acceptable for seal-only but would be a hard blocker for settlement.\n\n4. Frontend: only after 1-3 exist — wire deposit (wallet NEP-17 transfer with memo), poll settlement, rewire the step-4 \"Not in this app\" badges. Until then, do NOT change the honest 'no funds moved' framing.\n\nPRIVACY CAVEAT to surface as a product decision: on release the recipient + amount are posted in plaintext to the public Neo N3 chain (same as neo-message reveal). This is a TEE-mediated delayed/relayed transfer, not a true confidential transfer — it hides the link only until settlement, not after. The manifest currently (correctly) says zk is disabled; do not market this as on-chain-confidential.",
    "feasible_now": false,
    "risk": "Low risk to leave as-is (current state is honest and moves no funds; nothing is broken or misrepresented). HIGH risk/effort if built: net-new escrow contract holding real user funds + new TEE-signature-gated release path = highest-stakes contract class on the platform (custody + signature verification). Testnet lane is degraded (emergency-vercel-runtime), so even a testnet build path is partially blocked; the store backend the seal flow uses is not edge-served. Building it touches a new contract (custody), a new relayer/worker route + box env (server-side change to the production Nitro box), and the Supabase store backend.",
    "needs_user_decision": true,
    "cost": "Large, multi-day, multi-component. New C# escrow contract (~300 lines) + TestEngine suite; new relayer/worker confidential-settlement lane (JS) + box env wiring + service restart on the production Nitro box; restore/expose the confidential store backend (Supabase + edge); frontend rewire. Multiple deploys (testnet then mainnet). GAS: contract deploy ~10 GAS/network + escrow operating funds; the leaked NR3E4D8N owner key must NOT own a real-custody escrow (rotate to cold/multisig owner first). Not a one-agent quick fix.",
    "blockers": [
      "Testnet oracle runtime is degraded (emergency-vercel-runtime, Phala origin unavailable) — only mainnet /oracle/public-key + /oracle/decrypt are live; testnet settlement validation is blocked",
      "Confidential store backend (apps/web /api/confidential/store) is Supabase-backed, requires admin auth + project_slug, and is NOT served at the public edge for miniapp callers (mainnet returns 'not found') — the seal flow already degrades to inline_fallback",
      "No N3 confidential/escrow contract exists; worker env only configures NEOX_MESSAGE_CONTRACT (neo-message on Neo X), no N3 analog",
      "Existing /oracle/decrypt is a decrypt-return lane, not a fund-release lane — there is no TEE->on-chain N3 settlement path to ride; it must be built new",
      "Real-custody escrow owned by the known-leaked NR3E4D8N key is unsafe; needs a dedicated cold/multisig owner before mainnet",
      "Privacy model is product-level: recipient+amount become public on release (no post-settlement confidentiality) — needs a product decision on whether that meets the 'confidential transfer' bar"
    ],
    "evidence": "apps/private-transfer/src/PlayArea.tsx (seal-only flow; noFundsBanner; step1/step4 badged 'Not in this app' L734-758) and appConfig.ts L85,L118-121,L204-208 = honest framing. neo-manifest.json L24 contracts:{}, L102 transactions:false. apps/shared/utils/morpheus-confidential-envelope.ts: settlement.mode 'confidential_compute_release' is a declared intent, never executed. platform/host-app/pages/api/morpheus/confidential/store.ts proxies to {base}/api/confidential/store. Live read-only probes (2026-06-13): mainnet /oracle/public-key OK key_source=nitro-sealed; testnet=runtime_temporarily_degraded; mainnet+testnet /api/confidential/store = not served/degraded; /oracle/decrypt OK but 'sealed envelope required' (decrypt-only); /oracle/message-reveal = neox-only. SSM read-only: morpheus-relayer-nitro + morpheus-nitro-worker + morpheus-nitro-signer all active/running; relayer polling neo_n3 (checkpoint 10666795 advancing) + neox; MORPHEUS_ACTIVE_CHAINS=neo_n3,neox; worker env has NEOX_MESSAGE_CONTRACT only, no N3 confidential/escrow contract; morpheus-db-prune.service=failed (Supabase quota). workers/morpheus-relayer/src/router.js resolveKernelIntent maps 'decrypt'->/oracle/decrypt (confidential.decrypt), buildFulfillmentDigestBytes shows the TEE-sig digest a new contract would verify. workers/nitro-worker/src/capabilities.js L58-75 handleOracleDecrypt returns plaintext only. neo-n3.js exposes generic fulfillRequest callback (kernel pattern). No confidential-release escrow in contracts/ (grep confirms only generic witness/oracle usage). CoinFlip self-contained pattern = 328 lines (size reference for a new escrow)."
  },
  {
    "area": "aa-market-hub-seller-index",
    "is_real": false,
    "verdict": "not-worth-fixing",
    "layer": [
      "contract-new",
      "frontend"
    ],
    "feasible_now": true,
    "needs_user_decision": false,
    "exact_fix": "CONCLUSION: not-worth-fixing. The \"sellers cannot see/refund buyer pending payments\" premise describes correct, intentional behavior, not a defect. The frontend-only honest-copy ceiling is already shipped.\n\nEVIDENCE / ARCHITECTURE:\n- The contract is the standalone escrow `AAAddressMarket` at /Users/jinghuiliao/git/r3e/neo-abstract-account/contracts/market/AAAddressMarket.cs (testnet 0x8dbd4cf6fc47afc013e7fd7128d028db2985bddf, mainnet 0xae7afe3a85ab08bfd1d4907b35ae8b80c75b3a69). It is NOT the AA core wallet (UnifiedSmartWalletV3); it only Contract.Call()s the core (enterMarketEscrow/cancelMarketEscrow/settleMarketEscrow/getBackupOwner). The pending-payment mapping lives entirely in THIS contract's own storage (Prefix_PendingPayment 0x03 + listingId + payer, lines 280-284), so any new read method touches only this contract — it CANNOT break existing AA accounts.\n- Pending payments are by-design payer-witness-gated. RefundPendingPayment (lines 167-179) requires Runtime.CheckWitness(payer); GetPendingPaymentOf (235-238) is payer-keyed. A seller seeing/refunding buyer-escrowed GAS would be a fund-seizure vector, not a feature.\n- Buyer flow is ATOMIC: apps/aa-market-hub/src/utils/aa-market.ts buyAddressListing (522-562) uses wallet.invokeMultiple({ GAS.transfer -> OnNEP17Payment records pending; settleListing -> ClearPendingPayment + pays seller }) in ONE transaction. In NeoVM a reverting settleListing aborts the whole tx, rolling back the transfer. So the app can NEVER strand a pending payment. A residual pending entry can only arise from an out-of-band manual GAS transfer to the market with a listingId as data, which only the payer can (and should) recover via Refund Pending Payment.\n- Honest ceiling ALREADY shipped: apps/aa-market-hub/src/locale/messages.ts sellerSettlementNote = \"...buyer payments are escrowed and refundable by the buyer, so they are not shown or refundable here.\" buyEscrowExplainer tells the buyer to use Refund Pending Payment if settlement fails. The composable (useAAMarketHub.ts) already surfaces myPendingPayment + a buyer-only refund button (ManageListingCard.tsx hasPendingRefund). This is the correct UX.\n\nIF a seller/listing-index were nonetheless wanted (it is purely additive, low contract risk, but does NOT change who can refund): add to AAAddressMarket.cs a maintained index keyed by listingId (e.g. Prefix_PendingPayers 0x04 + listingId -> serialized UInt160[] of payers, appended in PutPendingPayment and pruned in ClearPendingPayment), plus a [Safe] read `GetPendingPayersForListing(BigInteger listingId)` returning the payer/amount pairs. (Do NOT use Storage.Find/iterators returned across the ABI boundary — return a materialized array.) This is read-only enumeration for display; it still must NOT let the seller call RefundPendingPayment for a payer (keep the CheckWitness(payer) gate). Frontend rewire: add listAddressListing-level call to the new read in aa-market.ts and show a per-listing \"buyer payments pending\" badge in ListingCard.tsx — informational only.\n\nWHY NOT TO SHIP IT: it adds storage + a write-path index to a live escrow contract to display a state that, by the atomic buy design, essentially never occurs, and it cannot legitimately enable seller refunds anyway. Net value ~0; risk non-zero.\n\nDEPLOY PATH (if ever pursued):\n- Mainnet 0xae7afe3a... HAS update(nef,manifest) (verified on-chain: getcontractstate methods include admin/setAdmin/update; updatecounter 0) and admin == 0x6d0656f6dd91469db1c90cc1e574380613f43738 (verified via admin() call), which is the byte-reversed form of the leaked NR3E4D8N key. Mainnet has 0 listings (getListingCount=0), so an in-place update() by NR3E4D8N is feasible and low-blast-radius — BUT spending mainnet GAS + reusing the leaked key on mainnet is exactly the gate this triage is told to avoid, and the feature has ~0 value, so do not.\n- Testnet 0x8dbd4cf6... was deployed from OLDER source (verified on-chain: getcontractstate methods are [createListing..getPendingPaymentOf] with NO admin/setAdmin/update; updatecounter 0; commit 6e921bc \"Add admin-gated updates to AA contracts\" added Update AFTER this deploy). It is therefore NOT updatable in place — it would require a fresh redeploy (new hash) + migrating 87 existing listings (getListingCount=87) or accepting an empty board, then re-pointing apps/aa-market-hub/neo-manifest.json + MARKET_HASH_BY_NETWORK in aa-market.ts. Not worth it for a display-only nicety.",
    "cost": "Software-only if pursued: ~1 C# method + index write-path in AAAddressMarket.cs, recompile via nccs, local NeoVM validation in neo-abstract-account/tests (Neo.SmartContract.Testing). Mainnet update() = a few GAS via leaked NR3E4D8N key (0 listings, in-place). Testnet = full redeploy (~10+ GAS) + 87-listing migration. Recommended action: ZERO — ship nothing; the honest copy is already in place.",
    "risk": "Doing nothing: no risk — current behavior is correct and the honest seller note is already shipped. If the index were added: contract change is additive and isolated to AAAddressMarket's own storage (does NOT touch AA core / UnifiedSmartWalletV3 accounts), but it touches a LIVE escrow contract holding real GAS. Testnet path is irreversible (new hash, 87-listing migration). Mainnet in-place update is reversible-ish (admin can update again) but reuses the known-leaked NR3E4D8N admin key on mainnet and spends mainnet GAS. The feature can never legitimately let a seller refund buyer funds (payer-witness gate must stay), so the upside is display-only.",
    "blockers": [
      "Testnet contract 0x8dbd4cf6... has no update() method (older bytecode) -> any contract change there needs a fresh redeploy + migration of 87 live listings; not feasible as an in-place fix.",
      "Mainnet in-place update() is gated on reusing the leaked NR3E4D8N admin key + spending mainnet GAS (avoid for a zero-value feature).",
      "No actual defect to fix: the atomic transfer+settle buy flow makes stranded pending payments effectively impossible, and payer-only refund is a deliberate anti-fund-seizure invariant."
    ],
    "evidence": "Files: /Users/jinghuiliao/git/r3e/neo-abstract-account/contracts/market/AAAddressMarket.cs (GetPendingPaymentOf 235-238, RefundPendingPayment payer-witness gate 167-179, OnNEP17Payment 181-200, Update admin-gated 76-80, PendingPaymentKey listingId+payer 280-284); /Users/jinghuiliao/git/r3e/neo-os-web/apps/aa-market-hub/src/utils/aa-market.ts (atomic buyAddressListing 522-562, payer-keyed getPendingPaymentOf 289-307, refundPendingAddressPurchase 564-583); apps/aa-market-hub/src/composables/useAAMarketHub.ts (myPendingPayment surfaced 88-96); apps/aa-market-hub/src/components/ManageListingCard.tsx (buyer-only refund button 98-102); apps/aa-market-hub/src/locale/messages.ts (sellerSettlementNote / buyEscrowExplainer honest copy ~69, ~119). Read-only RPC (verified live 2026-06-13): testnet seed1t5.neo.org:20332 getcontractstate 0x8dbd4cf6... -> methods lack admin/update, updatecounter 0, getListingCount=87; mainnet mainnet1.neo.coz.io getcontractstate 0xae7afe3a... -> methods include admin/setAdmin/update, updatecounter 0, getListingCount=0, admin()=0x6d0656f6dd91469db1c90cc1e574380613f43738 (== byte-reversed NR3E4D8N 0x3837f413...). Git: neo-abstract-account commit 6e921bc \"Add admin-gated updates to AA contracts\" post-dates the testnet market deploy."
  },
  {
    "area": "oracle-consoles-live-read",
    "is_real": true,
    "verdict": "not-worth-fixing",
    "layer": [
      "frontend"
    ],
    "feasible_now": false,
    "needs_user_decision": false,
    "evidence": "SHARED PANEL ALREADY SUPPORTS LIVE-READ — the deferred premise (\"shared panel has no fetch lane\") is stale. apps/shared/components-react/ConsoleToolPanel.tsx already defines the opt-in `execute?` hook (ConsoleToolConfig, lines 82-85), renders a \"Send live request\" secondary button only when execute is provided (lines 323-335), hides the preview-only notice when wired (lines 278-283), and fully handles success/warning(input_required)/error toasts + request counter + digest (runExecute lines 219-234, applyResult 193-213). The docstring (lines 69-81, dated 2026-06-12) already prescribes wiring execute to the tokenless GET lanes only. console-tool-panel.test.tsx (lines 160-267) ALREADY tests this path (notice hidden, button appears, success counts, input_required warns, throw -> error toast, empty -> unexpectedError). So no shared change is even needed; the only gap is the 3 appConfigs don't define `execute`.\n\nREAL BLOCKER = CORS (read-only verified 2026-06-13). The worker base is https://oracle.meshmini.app/mainnet (generated-morpheus-registry.ts:11), a Cloudflare worker (x-morpheus-edge: cloudflare). Probed via node fetch:\n- TOKENLESS+LIVE 200 JSON: feeds/price/NEO, feeds/catalog, neodid/providers, oracle/public-key, health.\n- NOT ROUTES (404): `prices` and `oracle/http` -> the HTTP console has NO matching public GET.\n- POST oracle/query tokenless -> 401 (confirms docstring).\n- CRITICAL: full response-header dump on GET oracle/public-key shows NO access-control-allow-origin; OPTIONS preflight returns 200 but NO ACAO/ACAM. With Origin: https://app.meshmini.app the GET still returns ACAO: null. So a browser fetch() from the deployed miniapp origin to oracle.meshmini.app would be CORS-BLOCKED (JS cannot read the response). There is no same-origin proxy/rewrite (root vercel.json is bare: only git.deploymentEnabled).\n\nWHY the existing live reads work without CORS: oracle-price-console reads on-chain via datafeed.getPriceWithMeta()/listPairs() over RPC (usePriceConsole.ts:153-194, label sourceOnChain) — NOT a browser fetch to the worker. apps/neo-message/src/main.tsx:73 DOES browser-fetch oracle/public-key (ORACLE_EDGE_BASE message-logic.ts:20) — that path is itself CORS-exposed/latent on the no-ACAO worker. The 3 config-only consoles have no on-chain equivalent for a GET-style fetch.\n\nVALUE: only neodid-console (providers catalog) and compute-lab (sealing public-key) have a genuinely useful tokenless GET; http-console has none. Even those two are CORS-blocked, so a wired execute would throw \"Failed to fetch\" -> error toast every time, which is strictly worse than today's honest preview-only notice.",
    "exact_fix": "No code change is justified now because the value is blocked by CORS, not by the shared panel. If the worker CORS is later fixed (the real prerequisite), the fix is SMALL and shared-safe:\n\n1) Worker (neo-os-services repo, infra prerequisite — out of THIS repo's reach): add `Access-Control-Allow-Origin: *` (or the app origin) + handle OPTIONS on the tokenless GET lanes feeds/price/:symbol, feeds/catalog, neodid/providers, oracle/public-key. Until this exists, no frontend wiring can work.\n\n2) Then, per-app opt-in ONLY (no shared edit needed):\n- apps/oracle-neodid-console/src/appConfig.ts: add to consoleConfig an `execute(values, t)` that does `const base = getExternalIntegrationConfig().morpheusPublicApiUrl; const r = await fetch(base + \"/neodid/providers\"); if(!r.ok) throw new Error(t(\"liveCatalogUnavailable\")); const { providers } = await r.json();` then build a ConsoleResult whose rows list the live provider ids/claim_types and whose payload echoes the catalog (no input_required), reusing previewId for a digest. Add executeActionKey (e.g. \"Fetch live catalog\") + localized error key to appMessages. Leave buildResult untouched.\n- apps/oracle-compute-lab/src/appConfig.ts: same shape, fetch base + \"/oracle/public-key\", surface algorithm + public_key + key_source rows.\n- apps/oracle-http-console: DO NOT wire — no tokenless GET matches (prices/oracle/http are 404); keep preview-only.\n- PlayArea.tsx files already spread the config (config.execute is read by the panel), so no PlayArea change.\n\n3) Keeping tests green: the frozen per-app config tests (oracle-*-console.config.test.ts) only exercise buildResult/validators/manifest copy — none assert execute absence — so adding execute does not touch them. The shared console-tool-panel.test.tsx already covers the execute path generically; no new shared test churn. Add focused tests in each app's config test for the new execute (mock global fetch). This is the only way to keep the \"frozen copy/status\" pins intact.",
    "cost": "Zero on-chain (no GAS/NEO, no deploy). If pursued post-CORS: ~2 appConfig edits + small test additions (~1 agent session). Currently blocked, so 0 productive cost.",
    "risk": "Low blast radius if ever done (per-app opt-in, additive `execute`, shared panel untouched and already tested). But doing it NOW is net-negative: a wired live-read against the no-CORS worker would fail every click with \"Failed to fetch\" -> red error toast, replacing today's honest, correct preview-only state with a broken-looking one. Reversible (just remove the execute key). No real-value exposure, no key use.",
    "blockers": [
      "Worker emits NO access-control-allow-origin on tokenless GETs (verified: header dump + OPTIONS preflight on oracle.meshmini.app/mainnet) — browser fetch from app origin is CORS-blocked; needs a worker-side CORS fix in the neo-os-services repo OR a same-origin Vercel rewrite proxy (root vercel.json has none).",
      "oracle-http-console has no tokenless GET at all (prices + oracle/http both 404) — not fixable by wiring.",
      "Shared-component live-read lane is NOT actually missing — execute hook + button + tests already exist in ConsoleToolPanel.tsx/console-tool-panel.test.tsx; the deferred-task premise is outdated."
    ]
  },
  {
    "area": "oracle-result-envelope-dup-export",
    "is_real": true,
    "verdict": "fix-now",
    "layer": [
      "infra-edge"
    ],
    "evidence": "File: /Users/jinghuiliao/git/r3e/neo-os-services/packages/shared/src/index.ts. This is a HAND-AUTHORED ambient type-declaration file (it contains `const` without initializer at L240 and bodyless function signatures at L246/L250/L257) used only as the `types` entrypoint of the `.` export. It has TWO duplicate-export defects, not one:\n(1) RESULT_ENVELOPE_VERSION — local `export const RESULT_ENVELOPE_VERSION = '2026-04-tee-v1';` at L156 AND re-export `export { RESULT_ENVELOPE_VERSION, ... } from './workflow-catalog.js';` at L189-193.\n(2) PUBLIC_RUNTIME_DISCOVERY_LINKS — local `export const PUBLIC_RUNTIME_DISCOVERY_LINKS: {...};` at L240-244 AND re-export `export { PUBLIC_RUNTIME_DISCOVERY_LINKS, ... } from './public-runtime.js';` at L259-264 (plus buildPublicRuntimeCatalogSummary/buildPublicRuntimeStatusSnapshot/getPublicRuntimeStatusNotes declared at L246-257 then re-exported, giving TS2484 conflicts too).\nEmpirical typecheck (local tsc 5.9.3, `npx tsc --noEmit --strict --module nodenext`): `index.ts(156,14): error TS2323: Cannot redeclare exported variable 'RESULT_ENVELOPE_VERSION'`, `index.ts(190,3): error TS2323` (same symbol), `index.ts(240,14): TS2323` + `index.ts(260,3): TS2323/TS2484` for PUBLIC_RUNTIME_DISCOVERY_LINKS, plus TS2484 conflicts for the three public-runtime functions.\n\nCRITICAL NUANCE — the prompt's framing (\"breaks bare-root importers / /utils works\") is imprecise about the mechanism. It is NOT a runtime break:\n- package.json (packages/shared) exports map: `\".\"`: { types: ./src/index.ts, default: ./src/index.js }. The RUNTIME entrypoint is index.js, which re-exports RESULT_ENVELOPE_VERSION ONLY ONCE (from ./workflow-catalog.js, index.js L16-20) and has NO local duplicate. So `import { RESULT_ENVELOPE_VERSION } from '@neo-os-services/shared'` works at runtime. Confirmed live: workers/nitro-worker/src/platform/result-envelope.js:1 imports it from bare-root and runs fine.\n- The break is TYPE-LEVEL only: any TS consumer (or editor/IDE) resolving the bare-root `.` types entrypoint loads index.ts and sees TS2323. The `/utils` subpath \"works\" because it resolves to utils.js with utils.d.ts beside it (no duplicate).\n- Repo CI does NOT catch this: there is NO tsconfig.json anywhere in the repo; root scripts have no `tsc`; workers' `check` scripts are `node --check` (JS syntax only); typescript is present only as @typescript-eslint. So the defect is latent — invisible to repo gates, only visible to external/editor TS consumers.\n\nImporters of RESULT_ENVELOPE_VERSION (grep across repo, excl. node_modules/dist/build):\n- workers/nitro-worker/src/platform/result-envelope.js:1 — bare-root `@neo-os-services/shared` (runtime OK).\n- workers/morpheus-relayer/src/workflow-persistence.js:1 — deep path `../../../packages/shared/src/workflow-catalog.js` (unaffected).\n- scripts/lib-public-runtime-catalog.mjs:5 — (resolves via index.js, OK).\n- packages/shared/src/index.js:17 (the canonical runtime re-export), index.ts:156/190 (the defect), workflow-catalog.js:1 (source of truth), utils.d.ts:1 (a THIRD stray copy of the const — see note in exact_fix), workflow-catalog.test.mjs:9/30 (test).\nCanonical value source: workflow-catalog.js:1 `export const RESULT_ENVELOPE_VERSION = '2026-04-tee-v1';`.",
    "exact_fix": "In /Users/jinghuiliao/git/r3e/neo-os-services/packages/shared/src/index.ts, make the declaration file mirror the runtime index.js (which re-exports, never locally declares). Two edits:\n(1) DELETE the standalone local declaration at L156: `export const RESULT_ENVELOPE_VERSION = '2026-04-tee-v1';`. Keep the re-export block L189-193 (`export { RESULT_ENVELOPE_VERSION, getWorkflowDefinition, listWorkflowDefinitions } from './workflow-catalog.js';`) as the single source. This makes the .ts faithful to index.js and to workflow-catalog.js as the value's home.\n(2) Same pattern for the parallel PUBLIC_RUNTIME_DISCOVERY_LINKS / public-runtime defect (in scope as the identical class of bug): DELETE the local declarations L240-257 (`export const PUBLIC_RUNTIME_DISCOVERY_LINKS: {...};` and the three bodyless `export function build.../get...` signatures) and keep ONLY the re-export block L259-264 `export { PUBLIC_RUNTIME_DISCOVERY_LINKS, buildPublicRuntimeCatalogSummary, buildPublicRuntimeStatusSnapshot, getPublicRuntimeStatusNotes } from './public-runtime.js';`. The types are already fully provided by public-runtime.d.ts (verified: it declares the const + all three functions with full signatures). Keep the local `type` aliases (PublicRuntimeCatalogSummary etc.) only if other code in index.ts references them; otherwise they are also redundant with public-runtime.d.ts — but leaving the `type` exports is harmless (no TS2323 for type-only re-export collisions). The minimal safe change is removing the value/function VALUE declarations that collide.\nALSO recommended (separate, secondary): utils.d.ts:1 contains a THIRD stray `export const RESULT_ENVELOPE_VERSION = '2026-04-tee-v1';` that does not belong in the utils declaration file (utils.js has no such export — verified utils.js has json/timingSafeCompare/trimString/parseTimestampMs/getClientIp/stableStringify/mapWithConcurrency only). Remove that stray line from utils.d.ts so the `/utils` types match its runtime surface. Not strictly required to fix the bare-root error but it is a related latent declaration drift.\nTEST COVERAGE: the existing packages/shared/src/workflow-catalog.test.mjs (L9 import, L30 assert RESULT_ENVELOPE_VERSION === '2026-04-tee-v1') already pins the value but only via the deep path, so it would NOT have caught the bare-root type break. Add a regression guard: (a) a runtime test that does `import { RESULT_ENVELOPE_VERSION } from '@neo-os-services/shared'` (bare-root) and asserts the value — proves the runtime entrypoint stays intact; and (b) a type/CI guard — add `npx tsc --noEmit packages/shared/src/index.ts` (or a tiny tsconfig with `checkJs:false`, `noEmit:true`, including index.ts) to scripts/verify_repo.sh so the redeclaration class of bug is caught in CI going forward. Validate the fix with the same command used here: `cd /Users/jinghuiliao/git/r3e/neo-os-services && npx --no-install tsc --noEmit --strict --module nodenext --moduleResolution nodenext packages/shared/src/index.ts` — the two TS2323/TS2484 redeclaration errors must disappear (the residual TS7016 \"no declaration for ./rate-limit.js/./workflow-catalog.js\" warnings are a separate, pre-existing missing-.d.ts gap, not part of this fix). Also run `npm --prefix packages/shared test` (node --test) — workflow-catalog.test.mjs must stay green.",
    "feasible_now": true,
    "risk": "Very low. Edits are confined to a hand-written .ts/.d.ts declaration file (no runtime JS changes). The runtime entrypoint index.js is unchanged and already correct, so no behavior change for any importer. Fully reversible (delete-only of duplicate declarations; git-tracked file, last touched in commit 70e53de). No on-chain, no deploy, no service restart, no key use. Blast radius: only TypeScript type resolution for bare-root consumers — strictly improves it (removes errors). The only way to regress is to accidentally remove the re-export instead of the local declaration; the validation tsc command + the node --test guard catch that immediately.",
    "needs_user_decision": false,
    "cost": "Trivial agent effort: ~2 small edits in one file (index.ts), 1 optional edit in utils.d.ts, 1 small regression test + 1 line in verify_repo.sh. Zero GAS/NEO, zero deploys, no on-chain ops. Validation = 1 tsc invocation + 1 node --test run (both local, seconds)."
  },
  {
    "area": "stale-data-feed-pusher",
    "is_real": false,
    "verdict": "not-worth-fixing",
    "layer": [
      "infra-box"
    ],
    "feasible_now": true,
    "needs_user_decision": false,
    "exact_fix": "NO FIX NEEDED for the stated defect — the issue premise (\"MorpheusDataFeed frozen since 2026-03-09, breaking neo-swap + neo-treasury\") is STALE INFORMATION. The pusher was restored during the Phala->AWS Nitro migration and both feeds are live now.\n\nNetwork/hash mapping (apps/shared/constants/generated-morpheus-registry.ts): mainnet morpheusDatafeed = 0x03013f49c42a14546c8bbe58f9d434c3517fccab (lines 40); testnet morpheusDatafeed = 0x9bea75cf702f6afc09125aa6d22f082bfd2ee064 (line 92). The \"0x9bea75cf\" named in the issue is the TESTNET feed. neo-swap (apps/neo-swap/neo-manifest.json:30) and neo-treasury (apps/neo-treasury/neo-manifest.json:23) BOTH default_network=neo-n3-mainnet, and apps/shared/constants/rpc.ts getNetwork() returns 'mainnet' by default — so on their default network they read the MAINNET feed 0x03013f49..., not 0x9bea75cf.\n\nRead-only RPC proof (2026-06-13, mainnet1.neo.coz.io getLatest): mainnet 0x03013f49 NEO-USD recordAge 227s, GAS-USD 536s, ETH-USD 1156s, BTC-USD 1776s — all fresh. testnet 0x9bea75cf (api.n3index.dev/testnet) NEO-USD recordTs age ~68s — also fresh.\n\nPusher health (SSM read-only, AWS_PROFILE=morpheus): systemd morpheus-feed-pusher.service (Neo N3, EnvironmentFile /opt/morpheus/nitro/feed-pusher.env, Environment FEED_CHAINS=neo-n3, ExecStart node feed-pusher.mjs in /opt/morpheus/neo-os-services) fires every 5 min via morpheus-feed-pusher.timer (active 2 days); last run 15:41:22 UTC exited status=0/SUCCESS \"pushed 8 pairs ... txid 0xddfb49b6...\". morpheus-feed-pusher-neox.service fires every 2 min, all succeeding. /opt/morpheus/nitro/feed-pusher.log shows continuous successful pushes. morpheus-feed-monitor reports \"ok feed_age=1min gas=75.1\". The N3 updater wallet (9fb28bdacfaa7fcc0a4d660d0dc990b0e7d46118) holds 75.1 GAS, far above the 8-GAS warn floor — pusher wallet IS funded. The pusher is signed by the 8787 Nitro enclave updater (role=updater), and force-pushes at least every MAX_STALE_SEC=1800s even on unchanged price.\n\nThe \"2026-03-09\" date now survives only as the TESTNET feed's stale dataTimestamp field [1] (1773068521) while its recordTimestamp field [3] is current — i.e. testnet TwelveData returns stale source timestamps but the on-chain record IS actively updated.\n\nOPTIONAL secondary (testnet-only cosmetic, NOT the mainnet-default behavior, low priority): apps/neo-swap/src/hooks/useSwapEngine.ts:21 RATE_STALE_AFTER_MS=10min keys its 'Rate may be stale' banner on dataTimestamp (field [1], lines 288-289). On mainnet dataTimestamp==recordTimestamp (fresh) so no false banner. On TESTNET, dataTimestamp is the frozen 2026-03-09 value while recordTimestamp is fresh, so a testnet user would see a false 'stale' banner. If desired, change the freshness signal to recordTimestamp (field [3]) for the testnet case, or have the testnet feed-update path forward real source timestamps. This is pure-software, testnet-only, and does NOT affect production/mainnet users.",
    "evidence": "Files: apps/shared/constants/generated-morpheus-registry.ts:40 (mainnet feed 0x03013f49...) + :92 (testnet feed 0x9bea75cf...); apps/neo-swap/neo-manifest.json:30 + apps/neo-treasury/neo-manifest.json:23 (default_network neo-n3-mainnet); apps/shared/constants/rpc.ts:195-200 getNetwork()=>'mainnet'; apps/shared/composables/useMorpheusDataFeed.ts:111 reads integration.contracts.morpheusDatafeed; apps/neo-swap/src/hooks/useSwapEngine.ts:18-21,288-289 (dataTimestamp-keyed stale guard, 10min). Pusher: deploy/feed-pusher/feed-pusher.mjs:155 N3_FEED=03013f49... :154 N3_MAGIC=860833102 (mainnet) :157-158 updater pub/sh :311-315 GAS warn. Live RPC (read-only, 2026-06-13 ~15:43 UTC): mainnet getLatest all 4 pairs fresh (NEO recordAge 227s); testnet getLatest NEO recordAge ~68s. SSM read-only (i-0c52851f134db20ee): morpheus-feed-pusher.timer active 2d, last service run status=0/SUCCESS pushing 2-8 pairs every 5min; feed-monitor 'ok feed_age=1min gas=75.1'; updater 75.1 GAS.",
    "cost": "Zero for the named issue — nothing to deploy, no GAS spend, no agent work. The (optional, declined) testnet cosmetic banner fix would be a 1-line frontend change + rebuild + vitest, no on-chain or infra action.",
    "risk": "None taken (read-only triage). The premise is a false alarm: both feeds are live; the production-default (mainnet) feed that neo-swap/neo-treasury read is fresh within minutes. No reversibility/real-value/blast-radius concern. The only residual is the testnet-only false 'stale' banner in neo-swap, which is cosmetic and not on the default network."
  },
  {
    "area": "degraded-apps-sweep (automation-copilot, neodid-passport, forever-album, gas-sponsor, neo-treasury)",
    "is_real": true,
    "verdict": "fix-needs-decision",
    "layer": [
      "frontend",
      "infra-edge",
      "external-creds"
    ],
    "feasible_now": false,
    "needs_user_decision": true,
    "risk": "Mixed by app. The two pure-software fixes (neo-treasury stale-feed re-classification, gas-sponsor honest-degradation) are zero-risk read-only / display-only. The infra fix (re-point public edge DNS off the emergency Vercel shim to the live Nitro box at 32.199.39.216, plus provision the enclave signer secret) touches the shared public gateway for ALL oracle/edge-dependent apps — moderate blast radius, reversible (DNS/origin swap), no on-chain value at stake. forever-album local-only storage is a deliberate product ceiling, not a defect — changing it is a new initiative, not a fix.",
    "cost": "No GAS/NEO. Software-only fixes: ~1 agent-session each for neo-treasury memory re-classification (no code change needed) and a gas-sponsor copy tweak. Infra fix: operator effort to (a) re-point edge.meshmini.app / oracle.meshmini.app origin to the box (DNS + nginx TLS server_name + cert), (b) provision the enclave signer secret (awaiting_enclave_signer_secret), (c) fix the harmless db-prune table-list bug. These need AWS/DNS/Vercel/cert creds the user controls — cannot be done from this environment.",
    "evidence": "LIVE PROBES (2026-06-13): (1) neo-treasury: reads on-chain MorpheusDataFeed via apps/shared/utils/price.ts -> useMorpheusDataFeed.getLatest. Default network = neo-n3-mainnet (apps/neo-treasury/neo-manifest.json:23). RPC invokefunction on mainnet datafeed 0x03013f49c42a14546c8bbe58f9d434c3517fccab returns HALT NEO=$2.229 ts=2026-06-13T15:46:31Z (age ~0h), GAS $1.122, BTC $64173 (all <0.2h). The frozen feed (0x9bea75cf, NEO ts 2026-03-09, age ~96d) is the TESTNET datafeed — registry generated-morpheus-registry.ts has mainnet block (lines 6-57: datafeed 0x03013f49) and testnet block (lines 58+: 0x9bea75cf), mapping is CORRECT. So neo-treasury is FUNCTIONAL on its default network; the fleet-health 'frozen feed' note is a stale/mis-scoped entry. (2) gas-sponsor: apps/gas-sponsor/src/composables/useGasSponsor.ts gates sponsorship on VITE_PLATFORM_API (unset) and honestly shows sponsorServiceUnconfigured; donate/send are pure on-chain GAS transfers that work. Live edge.meshmini.app/api/gas-sponsor/eligibility => HTTP 503 runtime_temporarily_degraded 'Phala runtime origin is unavailable'. (3) neodid-passport: passport.ts resolveDidDocument calls /api/morpheus/neodid/resolve (edge route); live edge.meshmini.app/api/morpheus/neodid/resolve => 503; box nitro-worker (:8788) is up and serves /neodid/* but requires auth + enclave signer (port 8080 healthz: degraded:true reason awaiting_enclave_signer_secret). On-chain NeoDIDRegistry 0xb81f31ea (mainnet) has only getBinding/isMasterNullifierUsed/useActionTicket — NO full-document resolve, so a tokenless on-chain resolve is NOT a drop-in. Frontend already degrades honestly ('Resolver unavailable in this standalone preview'). (4) automation-copilot: real executor exists (workers/morpheus-relayer/src/automation.js processAutomationJobs, wired at relayer.js:181, enabled by default config.js:370) and relayer-nitro is ACTIVE (118k+ ticks). BUT registration path is /api/edge/automation-triggers (automationGateway.ts:175) which routes through the dead public edge (503), so zero jobs are registered (no automation log activity in 24h). Frontend already handles handoff-only honestly (PlayArea.tsx executorUnavailableTitle/Body). (5) forever-album: useForeverAlbum.ts header documents a deliberate redesign to local-only AES-GCM browser storage because 45KB on-chain blob = ~45 GAS and mainnet storage kernel is 'Unknown contract'. Box infra: db-prune got HTTP 204 on 5/7 Supabase tables (only morpheus_policy_decisions/morpheus_risk_events 404) => Supabase reachable, NO quota outage now. feed-pushers actively pushing. Box public IP 32.199.39.216, nginx server_name _ only (not mapped to edge DNS).",
    "exact_fix": "Per-app: (A) neo-treasury — NOT a code defect. Re-classify in fleet-health memory: on default mainnet the feed (0x03013f49) is FRESH and neo-treasury works. No fix needed. Optional hardening: in useMorpheusDataFeed.getPriceWithMeta (apps/shared/composables/useMorpheusDataFeed.ts) add a staleness guard that surfaces a 'feed stale' notice when recordTimestamp age exceeds a threshold (helps the testnet case), but this is enhancement, not a bug fix. (B) gas-sponsor — code is already correct (honest unconfigured/unavailable notices, donate/send work on-chain). The ONLY real fix is infra: restore the platform paymaster API behind VITE_PLATFORM_API. That endpoint is part of the dead public edge. No frontend change needed. (C) automation-copilot — executor is real and running; frontend is correct. The ONLY fix is infra: restore /api/edge/automation-triggers by re-pointing the public edge to the live Nitro box. Software-feasible alternative would be a self-contained NeoVM automation contract, but cron-style price-threshold keepers fundamentally need an off-chain runner (which already exists on the box), so the contract-rewrite pattern does NOT apply here. (D) neodid-passport — frontend correct (honest degradation). Fix is infra: (i) re-point edge to the box AND (ii) provision the enclave signer secret on the box (resolve 'awaiting_enclave_signer_secret'). A tokenless on-chain resolve is NOT viable because NeoDIDRegistry lacks a document-resolve method. (E) forever-album — deliberate local-only product ceiling; not a defect. Cross-device sync would require a NEW off-chain storage lane (IPFS/Arweave/S3) — a product decision + new infra, out of scope for a fix. THE ONE SHARED ROOT-CAUSE INFRA FIX (unblocks automation-copilot, neodid-passport, gas-sponsor at once): re-point public edge origins (edge.meshmini.app, oracle.meshmini.app) from the emergency-vercel-runtime shim to the live Nitro box (public IP 32.199.39.216, nginx->127.0.0.1:8788), add a real TLS server_name + cert on the box's nginx, and provision the enclave signer secret. Plus trivially fix the db-prune table-list (drop morpheus_policy_decisions/morpheus_risk_events from its table set, or guard 404 as non-fatal) so the prune job stops failing. ALL of these require AWS/DNS/cert/Vercel creds the user controls and CANNOT be performed from this read-only environment.",
    "blockers": [
      "Public edge DNS/origin (edge.meshmini.app, oracle.meshmini.app) is locked to the emergency-vercel-runtime shim returning 503; re-pointing needs DNS + Vercel + TLS-cert creds the user controls (external-creds).",
      "Box nginx has no TLS server_name for the edge hostnames; needs cert + nginx config change (infra-edge) — write access to the box is out of scope (read-only triage).",
      "Enclave signer secret not provisioned on the box (awaiting_enclave_signer_secret) — blocks neodid resolve/sign and gas-sponsor signing; needs SSM/secret write the user controls.",
      "forever-album cross-device storage and automation persistence both ultimately depend on infra the user owns; no on-chain/contract fix substitutes for the off-chain edge gateway.",
      "All five are infra/product-gated, not contract-gated, so the self-contained-NeoVM-contract pattern does not apply."
    ]
  },
  {
    "area": "security-followups (rotate Neo X key 0x0b8584 + CF/Vercel tokens; control-plane DLQ wrangler queues create; AA Supabase migration 20260611_draft_metadata_hardening.sql)",
    "is_real": true,
    "verdict": "blocked-external",
    "layer": [
      "external-creds",
      "infra-edge",
      "onchain-op-mainnet"
    ],
    "feasible_now": false,
    "needs_user_decision": true,
    "evidence": "All three are genuine standing follow-ups (confirmed in /Users/jinghuiliao/git/r3e/neo-os-services/claudedocs/SESSION-HANDOFF-2026-06-07.md lines 80-90 \"Security (must rotate)\" + line \"Single key 0x622ae03B is owner+updater+verifier of all Neo X contracts + feed + dice bankroll — single point of compromise\"). None is fixable in THIS environment — every one is gated on external creds the user controls:\n\n(1) KEY/TOKEN ROTATION. Neo X key 0x0b8584 (controls deployer/owner 0x622ae03BDB6d7E2A29BE853c75d625bB25c0139C) is NOT on disk here — grep across both repos finds only the doc mention; it lives in transcripts + the box's 0600 env files. The oracle .env on disk holds only one secret: VERCEL_TOKEN=vcp_6Z… (present). There is NO Cloudflare API token anywhere on disk (CLOUDFLARE/CF_API only appear in .env*.example templates). The Cloudflare token LwOeK… and the new Neo X key must come from the user.\n\n(2) CONTROL-PLANE DLQ. wrangler 4.100.0 is reachable via npx but `npx wrangler whoami` returns \"You are not authenticated. Please run wrangler login.\" (verified). The config deploy/cloudflare/morpheus-control-plane/wrangler.meshmini.toml (account_id bf0d7e814f69945157f30505e9fba9fe) declares dead_letter_queue=morpheus-oracle-request-dlq (line 33) and morpheus-feed-tick-dlq (line 40); the consumers cannot deploy until both queues exist (README lines 67-69). `wrangler login` is interactive OAuth (browser) — cannot be done by an agent — and no CF API token is present to substitute.\n\n(3) AA SUPABASE MIGRATION. /Users/jinghuiliao/git/r3e/neo-abstract-account/supabase/migrations/20260611_draft_metadata_hardening.sql is complete, correct, idempotent SQL (FIX1: FOR UPDATE re-select in append_aa_draft_activity/set_aa_draft_relay_preflight/append_aa_draft_submission_receipt; FIX2: 64KiB pg_column_size cap + array caps + activity/receipt trimming in create_aa_draft; FIX3: revoke execute on assert_aa_draft_activity_allowed from public/anon/authenticated; FIX4: idempotent index re-creation). It targets the AA project's Supabase DB (a DIFFERENT project than the platform's dmonstzalbldzzdbbcdj in /Users/jinghuiliao/git/r3e/neo-os-web/.env). The AA Supabase project ref + service-role key are NOT on disk (no .env / config.toml project_id in neo-abstract-account). supabase CLI 2.104.0 is installed but has no AA project linked/auth. README mandates applying it (and 20260327) in filename order; CI replays it (.github/workflows/supabase-migrations.yml).",
    "exact_fix": "RUNBOOK for the user (these are external-creds tasks; the software/SQL is already done):\n\n(1) AA SUPABASE MIGRATION (lowest risk, do first). The file is correct as-is. Apply against the AA Supabase project:\n  - Linked CLI: `cd ~/git/r3e/neo-abstract-account && supabase link --project-ref <AA_PROJECT_REF>` then `supabase db push` (applies all unapplied migrations in supabase/migrations/ in filename order — replay-safe).\n  - OR Supabase dashboard SQL editor: paste the entire contents of supabase/migrations/20260611_draft_metadata_hardening.sql and run (it is idempotent — create or replace / create index if not exists / revoke).\n  - Verify after: in SQL editor `select proname from pg_proc where proname='create_aa_draft';` exists, and `select has_function_privilege('anon','public.assert_aa_draft_activity_allowed(text,jsonb)','execute');` returns false.\n  Note: a Supabase MCP server is connected in this session — if the user authorizes it against the AA org, mcp__claude_ai_Supabase__list_projects → find the AA ref → apply_migration could do this, but it still needs the user's Supabase auth/decision.\n\n(2) CONTROL-PLANE DLQ. From ~/git/r3e/neo-os-services:\n  - `npx wrangler login` (interactive browser OAuth — USER must do this; an agent cannot complete the browser step) OR export `CLOUDFLARE_API_TOKEN=<token with Workers Queues edit on account bf0d7e81…>`.\n  - `npx wrangler queues create morpheus-oracle-request-dlq`\n  - `npx wrangler queues create morpheus-feed-tick-dlq`\n  - Then (re)deploy: `cd deploy/cloudflare/morpheus-control-plane && npx wrangler deploy -c wrangler.meshmini.toml` (set required secrets SUPABASE_URL/SUPABASE_SERVICE_ROLE_KEY via `wrangler secret put` first).\n  - Verify: `npx wrangler queues list` shows all four queues; control-plane deploy no longer errors on missing DLQ.\n\n(3) KEY + TOKEN ROTATION (highest risk — mainnet value + leaked key). USER-only.\n  Neo X key rotation (key controls real Neo X mainnet GAS: dice bankroll + feed + oracle ownership on contracts 0x38DD6BCE… price feed, 0xeCFC1C65… oracle, 0xFA795F81… dice):\n  a. Generate a fresh EVM key (ideally split owner vs updater/verifier roles, or move owner to a multisig).\n  b. On the three Neo X contracts, transfer ownership / re-grant updater+verifier roles from old 0x622ae03B… to the new address (on-chain mainnet txs from the OLD key — real GAS).\n  c. Move the dice bankroll GAS from old deployer to new.\n  d. Update the box's 0600 env files (NOT in this read-only triage): set NEOX_FEED_PK / MORPHEUS_RELAYER_NEOX_UPDATER_PK / MORPHEUS_RELAYER_NEOX_VERIFIER_PK to the new key, then `systemctl restart morpheus-relayer-nitro morpheus-feed-pusher-neox.timer`.\n  Cloudflare token LwOeK… : revoke in CF dashboard → create new scoped token → update wherever it was used (CI/box).\n  Vercel token (jim8y): revoke in Vercel account settings → create new → update ~/git/r3e/neo-os-services/.env VERCEL_TOKEN and any CI.",
    "cost": "No GAS/agent effort possible here. User-side: (1) AA migration ≈ 5 min, $0. (2) DLQ ≈ 5 min after wrangler login, $0 (Cloudflare Queues free-tier-eligible). (3) Key rotation = several Neo X mainnet txs (real GAS, a few GAS total for ownership transfers + bankroll move) + a box restart; CF/Vercel token rotation is free but touches CI/box config.",
    "risk": "(1) AA migration: low/reversible — create-or-replace functions + idempotent indexes + a revoke; replay-tested in CI; worst case is restoring prior function bodies from earlier migrations. (2) DLQ: low — creating queues is additive; the only effect is making exhausted messages inspectable instead of silently dropped. (3) Key rotation: HIGH and largely irreversible — operates on mainnet real-value contracts (dice bankroll, feed, oracle ownership); a botched ownership transfer could brick admin control or strand bankroll GAS; must be sequenced carefully and done by the user with the live key. The leaked key remains a live compromise risk until rotated, but rotation must not be automated.",
    "blockers": [
      "No Cloudflare API token on disk and wrangler is not authenticated (npx wrangler whoami = not authenticated); wrangler login is interactive browser OAuth an agent cannot complete",
      "Neo X private key 0x0b8584 is not present in this environment (only referenced in a handoff doc); rotation also requires signing mainnet txs with the live owner key",
      "AA Supabase project ref + service-role key are not on disk (the platform .env Supabase project dmonstzalbldzzdbbcdj is a different project); supabase CLI installed but no AA project linked/authed",
      "Task is read-only/no box changes/no mainnet writes — all three require an external action the user controls"
    ]
  },
  {
    "area": "mainnet-rollout-gap",
    "is_real": false,
    "verdict": "not-worth-fixing",
    "layer": [
      "frontend"
    ],
    "exact_fix": "No fix needed for the round-3 mainnet gap. Evidence-based triage shows round-3 (commit 161931f16) introduced exactly FIVE new contract read calls across all 60 apps, and every one of them resolves on the deployed mainnet contract:\n\nComplete set of new `.read(` method names added in 161931f16 (verified with: `git show 161931f16 | grep '^+' | grep -oE '\\.(read|invoke|call)\\(\"[a-zA-Z]+'`): minBurn, maxBurn, isPaused, getRewardPerNeo, balanceOf.\n\nPer app (frontend file -> deployed mainnet contract from neo-manifest.json -> live RPC getcontractstate/invokefunction on mainnet1.neo.coz.io):\n\n1) burn-league apps/burn-league/src/composables/useBurnLeague.ts:357-358 adds chain.read(\"minBurn\") + chain.read(\"maxBurn\"). Mainnet 0xdd3bf2ff39bc4e39107ace953e2271a43a58e28f (== testnet hash, self-contained gen) ABI HAS both; live invoke: minBurn HALT=100000000, maxBurn HALT=100000000000. Also guarded by `if (minGas>0)` literal fallback. OK.\n\n2) gov-merc apps/gov-merc/src/hooks/useGovMerc.ts highestBid: the diff shows chain.read(\"highestBid\") was ALREADY present (it is in the `-`/context lines); round-3 only surfaces the value into a new observable. minBid is NOT a new read. Mainnet 0x140f5faf5692d21421a79278b0e45b9b9bd4bb46 (== testnet, self-contained gen) HAS highestBid+minBid; live minBid HALT=100000000. OK.\n\n3) custom-anchor apps/custom-anchor/src/main.tsx:~205 adds ctx.services.chain.read(\"getRewardPerNeo\",[String target]). Mainnet 0x02beeef6f65c6989a121c0a0e6b23190333edb98 ABI HAS getRewardPerNeo; live invoke HALT=0. OK.\n\n4) flashloan apps/flashloan/src/composables/useFlashloanCore.ts:273 reads rawStats.providerFeeShare — an OBJECT-FIELD access on the existing getPlatformStats result (mainnet 0xb5d8...e4 HAS getPlatformStats), with `|| DEFAULT(80)` fallback. Not a new chain read; absent field degrades to 80. OK.\n\n5) daily-checkin apps/daily-checkin/src/composables/useCheckin.ts:426,430 adds chain.read(\"isPaused\").catch(()=>false) and chain.read(\"balanceOf\",[contractHash],{scriptHash:GAS_HASH}).catch(()=>0). The balanceOf targets the GAS native token (always exists), and isPaused exists on mainnet 0xbd4f3646e189350b9c11a659655854e6f03f9be4 anyway; both are .catch-wrapped. OK.\n\n6) dice-game: round-3 diff to apps/dice-game added NO chain reads (UI-only: settlement-pending copy, chain-label, bet-tracker betId index). Its existing reads getDirectGasCredit/placeDiceBet exist on mainnet 0xa7840a8d... OK.\n\nConclusion: the deferred concern (round-3 wiring calling testnet-only methods) does not reproduce on mainnet. Nothing to change. If anything, leave a note that the suspect methods were verified live.",
    "feasible_now": true,
    "evidence": "Read git show 161931f16 (the round-3 commit) for burn-league/flashloan/daily-checkin/gov-merc/custom-anchor/dice-game composables; enumerated every new `.read(` method = {minBurn, maxBurn, isPaused, getRewardPerNeo, balanceOf}. Pulled mainnet contract hashes from each apps/<app>/neo-manifest.json. Queried LIVE mainnet (https://mainnet1.neo.coz.io) getcontractstate ABIs: burn-league HAS minBurn/maxBurn/settle/creditOf (self-contained gen, hash==testnet); gov-merc HAS highestBid/minBid/bid/settleEpoch (self-contained, ==testnet); custom-anchor HAS getRewardPerNeo; daily-checkin HAS isPaused/getCheckInStateForFrontend/getPlatformStats (older kernel gen, hash!=testnet but methods present); flashloan HAS getPlatformStats/getLoanDetails/getProviderStatsDetails (older kernel gen). Live invokefunction confirmed HALT: burn-league minBurn=1e8, maxBurn=1e11; gov-merc minBid=1e8; custom-anchor getRewardPerNeo=0. flashloan providerFeeShare is an object-field read with ||80 fallback; daily-checkin isPaused+balanceOf both .catch-wrapped (balanceOf on GAS native). dice-game round-3 diff added zero chain reads.",
    "risk": "None. Read-only triage; no files edited, no deploys, no on-chain writes. The finding is that the current shipped state is correct — taking no action carries no risk. The only residual (pre-existing, NOT round-3) item observed: flashloan/daily-checkin/dice-game mainnet hashes differ from testnet and are still the older kernel/oracle contract generation, but their frontend reads happen to land on methods those contracts expose, so round-3 added no break. dice-game remains oracle/VRF-dependent (separate known degraded-infra item, unrelated to this gap).",
    "cost": "Zero. No GAS, no deploy, no agent effort beyond this verification. Pure read-only confirmation.",
    "needs_user_decision": false,
    "blockers": []
  }
]