# Miniapp Deep-Audit Inventory (2026-06-04)

Static per-app validation of 58 apps + adversarial verification. Categories: wiring, correctness, robustness, security, perf.

**Counts:** Critical 6 · High 42 · Medium 61 · Low 105  
**By category:** correctness 79 · robustness 75 · wiring 41 · perf 12 · security 7

## CRITICAL (6)

| app | cat | file:line | issue | fix |
|---|---|---|---|---|
| gov-merc | correctness | apps/gov-merc/src/hooks/useGovMercPool.ts:145 | placeBid writes storage key "bid" but loadBids lists prefix "bid:" — leaderboard never populates | Write to a unique, prefixed key so it both matches the list prefix and does not clobber prior bids, e.g. `await storageService.set(`bid:${currentEpoch.get()}:${address.get()}`, { b |
| last-survivor | wiring | apps/last-survivor/src/composables/useLastSurvivor.ts:153 | Countdown clock is frozen — timer derives subscribe to no observable that the host watches | Give the derives real deps so their subscribe() wires up notifications: timeRemainingSeconds = createDerived(fn, [now, endTime]); countdown/dangerLevel/dangerProgress/shouldPulse = |
| milestone-escrow | correctness | apps/milestone-escrow/src/composables/useMilestoneEscrow.ts:417 | Amount pre-scaled to base units but edge functions expect human-decimal strings → 10^8x overpayment | Pass human-decimal strings to the OS proxies, not base units. Validate the raw input (reject NaN/negative/zero, enforce decimal count) but send the user's decimal value through: de |
| neo-multisig | correctness | apps/neo-multisig/src/main.tsx:272 | Multisig witness signatures collected via NEP-413 signMessage cannot satisfy on-chain CheckMultisig | Do not use signMessage to produce transaction witness signatures. Sign the raw sign-data bytes with the wallet's transaction/raw-sign primitive (e.g. a signData/signTransaction dAp |
| neo-pay | correctness | apps/neo-pay/src/composables/useNeoPayApp.ts:207 | NEO streams sent to edge at wrong scale (1e8x too large) | Decide one decimal convention. Simplest: drop the NEO 0-decimal branch and always pass an 8-decimal-consistent amount; or, since the edge hardcodes 8 decimals and the deposit only  |
| self-loan | robustness | apps/self-loan/src/composables/useSelfLoan.ts:437 | takeLoan has no rollback: NEO collateral is stranded if escrow.create fails after deposit succeeds | Wrap Step 2 so a failure triggers compensation: on escrow.create rejection, attempt paymentService.withdraw(String(collateral)) (or a dedicated release) before re-throwing, and sur |

## HIGH (42)

| app | cat | file:line | issue | fix |
|---|---|---|---|---|
| asset-factory | wiring | apps/shared/factory/FactoryPlayArea.tsx:323 | Sign button enables off the live-preview plan but signs the (often null) stored plan | Gate the Sign button and copy on the STORED plan, not the live preview. Either (a) only enable Sign when `val('currentPlan')` is non-null AND publishable — e.g. `const storedPlan = |
| automation-copilot | correctness | apps/automation-copilot/src/composables/useAutomationCopilot.ts:161 | registerTrigger reuses a stale triggerRequest after inputs change | Always rebuild before registering so current inputs are used: replace `if (!triggerRequest.get()) buildRecipePayload();` with an unconditional `buildRecipePayload();` (it is synchr |
| breakup-contract | correctness | apps/breakup-contract/src/composables/useBreakup.ts:266 | createContract writes metadata under wrong prefix/shape; loadContracts can never read it | Either write under the same prefix the list reads (e.g. storageService.set(`contracts:${id}`, fullStoredContract) with id/party1/party2/stake/title/terms/startTime/duration/flags p |
| breakup-contract | correctness | apps/breakup-contract/src/composables/useBreakup.ts:256 | Escrow id returned by create() is discarded; Sign/Break target contract.id instead | Persist the escrow id returned by create() alongside the contract metadata (const escrowId = await escrowService.create(...); store it), and have signContract/breakContract use the |
| burn-league | correctness | apps/burn-league/src/composables/useBurnLeague.ts:204 | User rank resolved by burned-amount equality instead of address, and races with stats load | Pass the connected wallet address into the composable (or read it from ctx) and match by address: const userEntry = mapped.find(e => e.address.toLowerCase() === myAddress.toLowerCa |
| custom-anchor | correctness | apps/custom-anchor/src/main.tsx:86 | NEO metrics divided by 1e8 (fromFixed8) though NEO is indivisible | Stop using fromFixed8 for the NEO fields. Set totalStaked.set(String(stackNumber(total))) and userStake.set(String(stackNumber(stake))) (whole-NEO, like asNumber in the reference). |
| daily-checkin | correctness | apps/daily-checkin/src/composables/useCheckin.ts:255 | lastCheckinTime divided by seconds-per-day, but value is a millisecond timestamp | Divide by MS_PER_DAY (not MS_PER_DAY/1000) if lastCheckinTime is in ms: lastCheckInDay.set(lastCheckinTime ? Math.floor(lastCheckinTime / MS_PER_DAY) : 0). Confirm the contract's s |
| dev-tipping | correctness | apps/dev-tipping/src/composables/useDevTippingStats.ts:106 | Recent tips render reads non-existent fields; every tip shows as "Anonymous" | Make producer and consumer agree on one shape. In loadRecentTips, capture the tipper: read `parsed.tipper`/`anonymous` from the stored tip and emit a `tipperName` field (e.g. `tipp |
| dev-tipping | robustness | apps/dev-tipping/src/main.tsx:37 | UI is never refreshed after a successful tip; stats and lists go stale | In main.tsx registerAction("sendTip"), after a successful wallet.sendTip, re-run the loaders and recompute the derived observables — e.g. pass an onSuccess that calls `await stats. |
| dice-game | robustness | apps/dice-game/src/main.tsx:82 | Double-submit window: in-flight guard set after await ensureWallet(), risking duplicate on-chain GAS bet | Set the in-flight flag before the first await: move `isSubmitting.set(true)` and `lastStatus.set(ctx.t("statusSubmitting"))` above `await ctx.services.chain.ensureWallet()` (and wr |
| event-ticket-pass | correctness | apps/event-ticket-pass/src/composables/useEventTicket.ts:370 | Minted counter increment is never persisted, so the sold-out cap is bypassable | In issueTicket, after a successful mint+storage write, persist the updated event: await storageService.set(eventStorageKey(event.id), eventPayload(updatedEvent)) before events.set( |
| event-ticket-pass | robustness | apps/event-ticket-pass/src/composables/useEventTicket.ts:367 | issueTicket mints NFT then writes storage with no rollback — partial failure orphans the NFT | Either write storage first then mint (so a mint failure leaves a recoverable pending record), or wrap the two steps so a storage failure after a successful mint attempts a compensa |
| explorer | correctness | apps/explorer/src/PlayArea.tsx:25 | Block height & tx count metrics always render "—" because num() parses comma-formatted strings as NaN | Don't double-parse a pre-formatted string. Either (a) display the strings directly: const activeHeight = str("mainnetHeight")/str("testnetHeight"), render it as-is and derive is-pe |
| explorer | wiring | apps/explorer/src/composables/useExplorer.ts:169 | Network toggle never refetches recent transactions, leaving stale wrong-network data | Make selectedNetwork.set trigger a reload. Either subscribe in the composable: selectedNetwork.subscribe(() => loadRecentTxs()), or wrap the setter so PlayArea's onUpdateSelectedNe |
| flashloan | correctness | apps/flashloan/src/composables/useFlashloanCore.ts:147 | Empty-loan guard is dead code — string-vs-number comparison never matches | Compare the parsed numeric value, not the string. E.g. change line 147 to `const isEmpty = amount === "0" && fee === "0" && !callbackMethod && !timestamp;` or, more robustly, parse |
| fogplay | wiring | apps/fogplay/src/composables/useCoinFlip.ts:185 | Flip button can lock permanently after a decimals validation failure | In the setBetAmount action (main.tsx) or inside a betAmount-change path, clear the error: coinFlip.validationError.set(null) whenever the amount changes (or recompute via validateB |
| forever-album | correctness | apps/forever-album/src/PlayArea.tsx:75 | UI size guard measures raw file bytes while chain enforces base64 length, so valid-looking uploads fail at sign time | Measure the same quantity the chain check uses: store the data-URL length on each UploadItem (e.g. a payloadBytes field = dataUrl.length) and sum that for totalPayloadSize, or comp |
| gas-sponsor | wiring | apps/gas-sponsor/src/composables/useGasSponsor.ts:127 | requestSponsorship passes a string to SDK requestSponsorship(amount: number) | Coerce to a number at the call site: `const result = await apiRequest(parseFloat(requestAmount.get()));` (the value is already validated as a finite number > 0 a few lines above).  |
| gasbox | wiring | apps/gasbox/src/PlayArea.tsx:679 | Prize result overlay can never be dismissed (string bound via bool()) | Use the real boolean instead of the display string. Replace `const isPlayingDisplay = bool("isPlayingDisplay")` and the overlay guard with the already-bound `isPulling` (gasbox.isP |
| gasbox | robustness | apps/gasbox/src/main.tsx:60 | Failed pull shows a false success toast and surfaces no error | Make playMachine rethrow on failure (or have the pull action inspect playError). Simplest: in playMachine's catch, `playError.set(...)` then `throw e;` so notify.guard shows the re |
| gov-merc | correctness | apps/gov-merc/src/hooks/useGovMercPool.ts:115 | Deposit/withdraw write keys never read back — totalPool and userDeposits stay 0 after a successful action | Reconcile the write/read key contract. Either have depositNeo/withdrawNeo update the aggregate records loadPoolData/loadUserDeposits read (e.g. read-modify-write "pool-state" and " |
| graveyard | correctness | apps/graveyard/src/composables/useGraveyard.ts:141 | Buried items and counters are optimistic-only and are wiped by any refresh/reload | Persist on success: in executeDestroy after burn, write storageService.set('history:'+memoryId, {id,hash,time,forgotten:false,memoryType}) so loadHistory reads it back; in forgetMe |
| last-survivor | correctness | apps/last-survivor/src/PlayArea.tsx:46 | Estimated Cost ignores the selected key count — always shows the price of 1 key | Drive the estimate from the value the user is editing. Either bind the input to the composable's keyCount observable (call keyCount.set in onKeyCountChange and read estimatedCost f |
| milestone-escrow | wiring | apps/milestone-escrow/src/PlayArea.tsx:100 | NEO token option is non-functional — deposit always transfers GAS and never scales NEO | Either remove the NEO option until NEO deposits are supported by the payment proxy, or thread the asset through the deposit/create calls (deposit(amount, memo, asset)) and have the |
| neo-multisig | robustness | apps/neo-multisig/src/main.tsx:289 | Collected signatures are never verified before counting toward threshold | In signRequest, after extracting {publicKey, signature}, call verifySignature(signData, extracted.signature, signerKey) and reject with toastSignFailed when it returns false, befor |
| neo-ns | correctness | apps/neo-ns/src/hooks/useNeoNS.ts:162 | registerDomain registers the live (possibly edited) query, not the searched name | Clear stale search state when the query changes: in PlayArea setSearch, also call state.searchResult?.set(null) and state.registrationCost?.set(0) on edit; OR in registerDomain, re |
| neo-pay | correctness | apps/neo-pay/src/composables/useNeoPayApp.ts:227 | Deposit always transfers GAS even for NEO streams | Remove the NEO token option (PlayArea tokenOptions + manifest token field), or extend PaymentProxy.deposit/os-payment-deposit to accept an asset and emit a NEO.transfer intent befo |
| neo-pay | robustness | apps/neo-pay/src/composables/useNeoPayApp.ts:226 | Deposit succeeds then createStream fails leaves funds stranded | Wrap the two steps so a Step 2 failure triggers paymentService.withdraw(totalFixed) (or a server-side atomic deposit+create edge). At minimum, surface a distinct 'funds deposited b |
| neo-pay-shared-example | correctness | apps/neo-pay/src/composables/useNeoPayApp.ts:209 | NEO streams with rate <1/day truncate to 0 and always fail (90d NEO preset is broken) | Stop deriving a fractional per-interval rate for NEO. Either (a) for NEO, compute intervalCount from a whole-NEO-per-interval value and reject fractional NEO streams in PlayArea be |
| neo-pay-shared-example | robustness | apps/neo-pay/src/composables/useNeoPayApp.ts:227 | Two-step create (deposit then createStream) has no rollback — funds stranded if step 2 fails | Wrap step 2 in a try/catch that calls paymentService.withdraw(totalFixed) (the method exists on PaymentProxy) to refund the deposit when createStream fails, then rethrow the normal |
| neo-swap | wiring | apps/neo-swap/src/manifest.ts:26 | Manifest stats & sidebar reference state keys that don't exist | Either expose matching observables in the returned state record (e.g. add selectedPairDisplay = derived `${from.symbol}/${to.symbol}`, pairCount = popularPairs.length, currentRate  |
| neo-treasury | robustness | apps/neo-treasury/src/utils/treasury.ts:186 | Single failed address balance aborts the entire treasury load | Make per-address fetching resilient: in the loop, on catch, push a wallet entry with neo:0/gas:0 (or skip it) and continue instead of throwing — optionally accumulate a non-fatal w |
| private-transfer | correctness | apps/private-transfer/src/PlayArea.tsx:265 | NEO asset accepts fractional amounts that can never settle | Make amount validation asset-aware: when asset==="NEO" require Number.isInteger(Number(amount)) and set the input step to "1" (or render presets without decimals for NEO). Mirror t |
| profitanchor-admin | robustness | apps/profitanchor-admin/src/PlayArea.tsx:108 | No in-flight/double-submit guard on any of the three submit buttons | Add an in-flight observable. Either return anchor.isLoading from setup() and bind it (state: { ..., isLoading: anchor.isLoading }) then `disabled={!canMove \|\| bool('isLoading')}` |
| quadratic-funding | wiring | apps/quadratic-funding/src/PlayArea.tsx:220 | Per-project claim feature is fully implemented but unreachable from the UI | Register the action (ctx.registerAction("claimProject", (...a)=>qf.handleClaimProject(a[0] as ProjectItem))) and add a Claim button to each project card in PlayArea gated on canCla |
| recovery-guardian | correctness | apps/shared/test/recovery-guardian.playarea.test.tsx:124 | Test suite is stale and fully failing — tests a removed on-chain version of the app | Rewrite the test to the current contract: assert the 6 link buttons (open/copy/share recovery preview & credential) are disabled until accountAddress + recoveryNewOwner + valid rec |
| self-loan | correctness | apps/self-loan/src/composables/useSelfLoan.ts:484 | repay() never releases locked collateral — escrow.refund is documented but never called | After the repay deposit, when the resulting position shows borrowed==0, call escrowService.refund(String(loan.get().id)) to release collateral (and surface success). If release is  |
| soulbound-certificate | correctness | apps/soulbound-certificate/src/composables/useSoulbound.ts:444 | Token ID passed as raw string ByteArray instead of base64-encoded (verify/revoke) | Import encodeTokenId from @shared/utils/parsers and wrap both ByteArray token-id args: `{ type: "ByteArray", value: encodeTokenId(tokenId) }` at lines 444 and 475, matching event-t |
| time-capsule | correctness | apps/time-capsule/src/composables/useTimeCapsule.ts:462 | fishCapsule charges 0.05 GAS but never changes any state - same candidate stays fishable, fee is repeatable with no effect | After a successful deposit, mutate state so the candidate is no longer re-selectable (e.g. mark it fished/claimed in its index record via storageService.set, or record the fished i |
| unbreakable-vault | wiring | apps/unbreakable-vault/src/composables/useVaultBreaker.ts:270 | Break attempt feedback (failed/success) is emitted on event names nobody subscribes to | Route these through the subscribed channel: either call setStatus directly from the registered actions in main.tsx (e.g. on attemptBreak return success/failure and setStatus(t('bro |
| unbreakable-vault | robustness | apps/unbreakable-vault/src/PlayArea.tsx:198 | attemptBreak / loadVault dispatch rejections are unhandled — no error toast on chain/wallet failure | Wrap the break/load dispatches like handleCreate does, e.g. onClick={async () => { try { await dispatch("attemptBreak"); } catch (e) { setStatus?.(e instanceof Error ? e.message :  |
| wallet-health | robustness | apps/wallet-health/src/composables/useWalletAnalysis.ts:68 | Balance-refresh failures are silently swallowed — user sees no error and stale balances | Either subscribe to balances:error/wallet:error and route them to a visible status/observable, or stop swallowing: let refreshBalances/connectWallet re-throw so the registerActions |

## MEDIUM (61)

| app | cat | file:line | issue | fix |
|---|---|---|---|---|
| aa-account-lab | correctness | apps/aa-account-lab/src/PlayArea.tsx:67 | Successful inspect of an unregistered account renders empty state (hasInspected false-negative) | Track an explicit inspected flag instead of inferring from the value. Add an observable e.g. `hasInspected = createObservable(false)` set to true at the end of inspectAccount, bind |
| aa-market-hub | robustness | apps/aa-market-hub/src/utils/aa-market.ts:312 | Unbounded listing fetch and render (no cap, no pagination) | Cap the work: compute `const total = Math.min(count, MAX_LISTINGS)` (e.g. MAX_LISTINGS=200) before building the reads array, batch the reads in chunks (e.g. 20 at a time) instead o |
| aa-permissions-lab | correctness | apps/aa-permissions-lab/src/PlayArea.tsx:66 | normalize() placeholder check misses the translated 'notAvailable' value in non-en locales | Compare against the resolved translation, not hard-coded strings/keys. e.g. const NA = t("notAvailable"); then `if (!trimmed \|\| trimmed === NA \|\| trimmed === "N/A") return PLAC |
| aa-relay-console | correctness | apps/aa-relay-console/src/PlayArea.tsx:86 | Stale relay result permanently shadows later sponsor check/request in the inline result card | Clear the relay result at the start of the sponsor flows (in `checkSponsor`/`requestSponsor` call `lastRelayResult.set(null)` before/after setting `sponsorResult`), OR in PlayArea  |
| aa-session-key-lab | wiring | apps/aa-session-key-lab/src/composables/useAASessionKeyLab.ts:222 | isCheckingSponsorship wrapper has a dead subscribe() — sponsorship spinner gets stuck on | Bind the live flag directly instead of a dead wrapper. Either return aa.isCheckingSponsorship itself in the state record (it is a real createObservable that notifies), or make the  |
| aa-session-key-lab | wiring | apps/aa-session-key-lab/src/PlayArea.tsx:100 | Derived previews (Account ID Hash / Target Contract / Allowed Method) read action-controlled form.* not the live inputs | Drive the derived previews from the live input state. Simplest: sync form on input change (onChange={(v)=>{setAccountSeed(v); /* dispatch a lightweight setForm action or pass curre |
| automation-copilot | robustness | apps/automation-copilot/src/composables/useAutomationCopilot.ts:157 | registerTrigger has no in-flight guard; form Enter-submit allows concurrent registrations | Guard the entry of registerTrigger: `if (isRegistering.get()) return;` before `isRegistering.set(true)`. Optionally also gate the form onSubmit on the isRegistering binding in Play |
| breakup-contract | robustness | apps/breakup-contract/src/composables/useBreakup.ts:341 | breakContract has no in-flight (double-submit) guard | Add `if (isLoading.get()) return;` as the first line of breakContract (mirroring the other two actions), and/or pass disabled={isLoading} to the Break NeoButton in ContractList.tsx |
| breakup-contract | robustness | apps/breakup-contract/src/composables/useBreakup.ts:256 | create() then storage.set() is non-transactional with no compensation | Make the metadata write best-effort (fire-and-forget with .catch, like badgeService.award on line 276) so a storage failure does not mask a successful escrow creation, OR wrap the  |
| burn-league | security | apps/burn-league/src/composables/useBurnLeague.ts:273 | Raw unvalidated amount string sent to chain/OS instead of the parsed value | Send the parsed/normalized number, not the raw string. After validation compute a canonical string (e.g. const canonical = String(amount) or amount.toFixed(8)) and pass canonical t |
| burn-league | robustness | apps/burn-league/src/composables/useBurnLeague.ts:284 | On-chain burn can succeed but be reported as failure with no data refresh (no compensation between placeBet and submitScore) | Treat post-placeBet steps as best-effort: wrap submitScore in its own try/catch (like updateStat/award already are) so a leaderboard-submit failure does not mask a successful burn, |
| council-governance | robustness | apps/council-governance/src/PlayArea.tsx:148 | Create-proposal form is wiped and tab switches even when creation fails | Only reset the form and switch tabs on confirmed success. Surface the success signal: in main.tsx make the createProposal action `return await ctx.services.notify.guard(...)` (guar |
| custom-anchor | robustness | apps/custom-anchor/src/main.tsx:82 | Post-action loadData serves stale cached reads (15s TTL, invoke does not bust cache) | Make the post-action refresh bypass the cache: give loadData an optional `fresh` param and pass cache:false (or omit cache) when called after stake/withdraw/claim, e.g. await loadD |
| dice-game | robustness | apps/dice-game/src/main.tsx:87 | No rollback/compensation if contract invoke fails after the GAS prepayment succeeds | This is shared-service behavior, but the app should at minimum (a) distinguish 'payment sent but bet not registered' from 'nothing happened' in lastStatus and history, and (b) reco |
| event-ticket-pass | correctness | apps/event-ticket-pass/src/composables/useEventTicket.ts:461 | checkInTicket can write a stale ticket payload under the current input's storage key | In checkInTicket, ignore the stale lookup when it disagrees with the input: if (!ticket \|\| ticket.tokenId !== tokenId) ticket = await lookupTicket(); and derive the storage key f |
| explorer | robustness | apps/explorer/src/composables/useExplorer.ts:205 | Failed or not-found search produces no user-visible feedback; registered errorKey is unreachable | In search(), set a user-facing not-found/error state when !res.ok (e.g. searchResult.set({ type: 'none' }) plus a 'no results' message, or surface via an error observable), and re- |
| fogplay | correctness | apps/fogplay/src/composables/useCoinFlip.ts:183 | Bet min/max error shows literal {min}/{max}/{tokenGas} placeholders | Pass the params: return t('minBetError', { min: MIN_BET, tokenGas: t('tokenGas') }); and t('maxBetError', { max: MAX_BET, tokenGas: t('tokenGas') }). |
| fogplay | robustness | apps/fogplay/src/composables/useCoinFlip.ts:295 | Deposit not compensated if bet placement or settlement fails (funds stranded) | Track whether the deposit succeeded but the bet did not; in the catch, attempt paymentService.withdraw(amountBase) (best-effort, swallow its error) so the wager is returned when th |
| forever-album | wiring | apps/forever-album/src/PlayArea.tsx:75 | UI total-size limit (61440) disagrees with composable limit (60000), enabling an Upload the action rejects | Source a single limit from the composable (it already exports MAX_TOTAL_BYTES). Bind MAX_TOTAL_BYTES into PlayArea via state and set uploadLimit = MAX_TOTAL_BYTES (60000) so the di |
| forever-album | robustness | apps/forever-album/src/composables/useForeverAlbum.ts:378 | Multi-photo upload writes one signed tx per photo with no rollback; mid-loop failure strands partial on-chain writes | Either (a) batch all photo writes into a single intent/transaction so the operation is atomic, or (b) track succeeded record ids during the loop and on failure surface a precise me |
| gas-sponsor | correctness | apps/gas-sponsor/src/PlayArea.tsx:106 | Several UI labels render raw i18n keys because t() returns the key and `\|\| fallback` is dead code | Add the 7 missing keys (donate, sendGas, donateAction, sendAction, recipient, used, resetsAt) to apps/gas-sponsor/src/locale/messages.ts with en/zh values; the `\|\| fallback` stri |
| gasbox | robustness | apps/gasbox/src/PlayArea.tsx:216 | Studio form is wiped even when publish fails | Only reset on confirmed success. Have the action return a success boolean (e.g. dispatch returns the guard result / a status flag) and gate `resetStudioForm()` on it, or move the r |
| gasbox | correctness | apps/gasbox/src/composables/useGasBox.ts:451 | User pays GAS to pull but no prize is ever paid out on-chain | Either wire the payout (release the won item's escrowed value to the player via PaymentProxy/EscrowProxy after resolution) or, if payouts are intentionally out of scope for this bu |
| gov-merc | robustness | apps/gov-merc/src/hooks/useGovMercPool.ts:143 | placeBid debits GAS then writes storage with no rollback if the write fails — GAS can be stranded | Wrap the two steps so a storage failure triggers a compensating action or at least a persisted pending record before the payment. Minimal: record an intent (`bid:...:pending`) befo |
| gov-merc | robustness | apps/gov-merc/src/main.tsx:28 | Wallet address captured once at setup; never re-subscribed to chain.address changes | Subscribe to ctx.services.chain.address and propagate changes: `const stop = ctx.services.chain.address.subscribe(() => { pool.setAddress(ctx.services.chain.address.get() ?? ""); v |
| graveyard | wiring | apps/graveyard/src/composables/useGraveyard.ts:141 | Selected memoryType never reaches the chain/burn call (control is on-chain no-op) | If memory type is meant to be recorded, pass it through the service boundary (e.g. extend the NFT burn payload or storageService.set the type alongside the persisted history record |
| graveyard | robustness | apps/graveyard/src/composables/useGraveyard.ts:223 | Concurrent forget silently no-ops with no user feedback | Either disable all Forget buttons while any forget is in flight (pass a global isForgetting flag to HistoryTab and gate every Forget button on it), or surface feedback when the gua |
| last-survivor | robustness | apps/last-survivor/src/composables/useLastSurvivor.ts:391 | buyKeys deposits GAS then places bet with no rollback if step 2 fails | Prefer a single atomic OS/edge operation that both takes payment and records the bet (e.g. placeBet carrying the deposit), or wrap the two steps so a placeBet failure triggers a co |
| memorial-shrine | robustness | apps/memorial-shrine/src/PlayArea.tsx:85 | Mainnet receipt ID is not cleared after a successful tribute, so the consumed receipt is reused | In handlePayTribute, also reset the receipt input after dispatch: add setTributeReceiptId("") alongside setTributeMessage("") (line 87). |
| milestone-escrow | robustness | apps/milestone-escrow/src/composables/useMilestoneEscrow.ts:398 | Non-numeric amount throws raw 'Cannot convert NaN to a BigInt' instead of friendly invalidAmount | After parseFloat, validate: const parsed = parseFloat(raw); if (!Number.isFinite(parsed) \|\| parsed <= 0) throw new Error(t('invalidAmount')); then compute the bigint. This keeps  |
| milestone-escrow | robustness | apps/milestone-escrow/src/composables/useMilestoneEscrow.ts:417 | No compensation if escrow create fails after deposit succeeds — GAS stranded in payment balance | Wrap Step 2 so that on failure you either attempt paymentService.withdraw(totalAmount) as compensation or surface a specific recoverable error telling the user funds are in their e |
| neo-convert | robustness | apps/neo-convert/src/main.tsx:62 | downloadPaperWallet rejection is silent — no user-visible error | Wrap the body of downloadPaperWallet in try/catch and surface failures, e.g. emit eventBus.emit('convert:error', { message }) (matching the empty-account path it already uses) or r |
| neo-multisig | robustness | apps/neo-multisig/src/PlayArea.tsx:60 | Sign button stays enabled after the current wallet already signed, allowing redundant re-sign prompts | Derive the connected account's public key from ctx/chain and disable/relabel the Sign button when selectedRequest.signatures already contains that key (e.g. canSign = ... && !alrea |
| neo-multisig | perf | apps/neo-multisig/src/main.tsx:113 | syncHistoryFromStorage double-writes the history cache for every request | Drop the redundant updateHistoryItem inside the loop (addToHistory already carries status), and batch the rebuild: collect all items, set history once, and call saveHistory a singl |
| neo-ns | correctness | apps/neo-ns/src/hooks/useNeoNS.ts:107 | Domain expiry multiplied by 1000 likely double-scales an already-ms timestamp | Verify the unit returned by properties.expiration against a live testnet domain. If it is milliseconds, drop the * 1000 (expiry: Number(props.expiration \|\| 0)). If seconds, keep  |
| neo-pay | wiring | apps/neo-pay/src/main.tsx:61 | Success toasts display raw i18n keys (streamCreated/streamCancelled/streamClaimed) | Either pass existing keys (e.g. 'vaultCreated') or add streamCreated/streamCancelled/streamClaimed entries to messages.ts with en/zh strings. |
| neo-sign-anything | wiring | apps/neo-sign-anything/src/main.tsx:34 | Successful sign shows misleading "Copied" toast | Add a dedicated locale key (e.g. signSuccess: { en: "Message signed", zh: "已签名" }) to apps/neo-sign-anything/src/locale/messages.ts and change main.tsx:34 to notify.guard(() => sig |
| neo-swap | correctness | apps/neo-swap/src/main.tsx:44 | selectPair sets tokens but never reloads the exchange rate (stale/inverted quote) | In selectPair, after `swap.fromToken.set(from); swap.toToken.set(to);` call `swap.loadExchangeRate().catch(() => {});` (and clear fromAmount/toAmount) before returning, matching se |
| neo-swap | correctness | apps/neo-swap/src/hooks/useSwapEngine.ts:172 | Fractional NEO amount is floored to 0 but the swap still executes | Reject non-integer amounts when the from-token has 0 decimals: in canSwap and/or executeSwap, require Number.isInteger(amount) when fromToken.decimals === 0 (throw t('insufficientB |
| neo-treasury | perf | apps/neo-treasury/src/utils/treasury.ts:173 | 42 wallet balances fetched sequentially with await-in-for-loop | Replace the sequential loop with bounded-concurrency parallelism, e.g. `const results = await Promise.all(addresses.map(a => getNep17Balances(a).then(...).catch(...)))` (combine wi |
| neo-x-bridge | robustness | apps/neo-x-bridge/src/PlayArea.tsx:94 | Non-numeric gas limit bypasses validation and reaches the intent payload | Make the gas-limit validity check reject non-numeric input, e.g. const gasNum = Number(gasLimit); const gasLimitInvalid = gasLimit.trim().length > 0 && (!Number.isFinite(gasNum) \| |
| oracle-neodid-console | correctness | apps/shared/components-react/ConsoleToolPanel.tsx:125 | Validation failure shown as green success toast | Derive the status type from the payload, e.g. const ok = next.payload.status !== "input_required"; setStatus(next.status, ok ? "success" : "warning"). Alternatively have buildResul |
| oracle-price-console | correctness | apps/oracle-price-console/src/main.tsx:36 | Success toast 'Price loaded' always fires, even when the RPC fetch fails | Either (a) make the success toast conditional on the result: in main.tsx do `const r = await price.fetchPrice(); if (r.success) ctx.services.notify.success('priceLoaded'); else ctx |
| oracle-vrf-console | wiring | apps/oracle-vrf-console/src/appConfig.ts:141 | Digest payload key mismatch leaves "Request ID" stat + sidebar permanently blank | In buildResult (appConfig.ts), name the payload digest key `digest` (or add `digest: requestId` alongside `client_digest`) so runPreview's `payload.digest` lookup resolves. e.g. pa |
| private-transfer | security | apps/private-transfer/src/PlayArea.tsx:27 | Amount validator accepts scientific/hex/whitespace strings stored verbatim | Validate amount against a strict decimal regex (e.g. /^\d+(\.\d{1,8})?$/ for GAS, /^\d+$/ for NEO) instead of Number()>0, rejecting scientific notation, hex, leading/trailing junk, |
| profitanchor | correctness | apps/profitanchor/src/main.tsx:74 | agentCount derive masks zero registered agents as 21, hero reports "21/21 registered" | Stop coercing 0 to the static fallback. Use a nullish check that only falls back when stats is missing, e.g. `const s = anchor.stats.get(); return s ? s.agentCount : agentAccounts. |
| profitanchor-admin | correctness | apps/profitanchor-admin/src/main.tsx:41 | agentCountDisplay falsy-OR hides a real zero agent count | Use nullish coalescing against an explicitly-undefined count rather than `\|\|`: have loadStats leave selectedAgentId/agentCount undefined when the map value is absent, and render  |
| quadratic-funding | robustness | apps/quadratic-funding/src/composables/useQuadraticContributions.ts:65 | Negative / non-numeric amounts bypass JS validation and reach the chain call | After normalization, validate the result is a non-negative integer string before dispatch, e.g. if (!/^\d+$/.test(value) \|\| value === '0') { setStatus(t('invalidContribution'),'e |
| quadratic-funding | correctness | apps/quadratic-funding/src/composables/useQuadraticRounds.ts:46 | Admin capability derives read the unbound `address` observable and go stale on wallet connect | Either add address (and chainType) to the bound state record in main.tsx so wallet changes trigger re-render, or list the address observable in the deps of these derives: createDer |
| recovery-guardian | correctness | apps/recovery-guardian/src/main.tsx:59 | `\|\|` fallback hides legitimate 0 values for threshold/timelock (and other numeric fields) | Use nullish coalescing on the raw value before stringifying, e.g. `const v = latestPayload.get()?.timelock; return v === undefined \|\| v === null ? ctx.t('notAvailable') : String( |
| red-envelope | robustness | apps/red-envelope/src/composables/useRedEnvelope.ts:380 | Create flow deposits GAS then creates pool with no rollback on partial failure | Capture the poolId from createPool and either (a) make the edge function fund the pool atomically from the deposit and only deposit after the pool exists, or (b) wrap the two steps |
| self-loan | robustness | apps/self-loan/src/PlayArea.tsx:441 | Add-Collateral button has no in-flight guard — double-click double-deposits NEO | Add an isAddingCollateral observable (mirroring isBorrowing/isRepaying), set it true/false around the deposit in addCollateral(), bind it in main.tsx, and on the button add loading |
| self-loan | correctness | apps/self-loan/src/PlayArea.tsx:136 | Estimated Borrow ignores the origination fee, overstating GAS the user actually receives | Compute the fee in PlayArea using the bound platformStats.platformFeeBps (or expose a netBorrow/fee derive from the composable) and show net: estimatedNet = expectedBorrow * (1 - f |
| soulbound-certificate | robustness | apps/soulbound-certificate/src/composables/useSoulbound.ts:262 | My Certificates list only reads OS storage, never the chain; issued certs may not appear | Add a chain read path for certificates (e.g. getOwnerCertificates/getCertificatesByIssuer -> getCertificateDetails, mirroring loadTemplatesFromChain) and prefer it when an address  |
| time-capsule | wiring | apps/time-capsule/src/composables/useTimeCapsule.ts:402 | Open button shown using a live time check but openCapsule rejects on the stale cap.locked computed at load time | In openCapsule recompute lock status live instead of trusting the stale flag: replace `if (cap.locked)` with a fresh check `if (!cap.revealed && Date.now() < cap.unlockTime)`. Alte |
| time-capsule | robustness | apps/time-capsule/src/composables/useTimeCapsule.ts:338 | createCapsule has no compensation: escrow is created (0.2 GAS) before the index write - if storageService.set throws the capsule is orphaned | Wrap the post-create steps so a failed index write triggers compensation: on storageService.set failure call escrowService.refund(escrowId) (when a real escrowId was returned) befo |
| trustanchor | wiring | apps/trustanchor/src/hooks/useTrustAnchor.ts:165 | getCredit/pendingWithdraw is read on every load but never surfaced in the UI | Either remove loadPendingWithdraw() and the pendingWithdraw observable if 'withdraw' returns NEO directly to the wallet, OR surface pendingWithdraw as a 4th stat chip / sidebar ite |
| trustanchor-admin | robustness | apps/trustanchor-admin/src/PlayArea.tsx:105 | No in-flight / double-submit guard on fund-moving admin actions | Add an in-flight observable (e.g. createObservable(false)) set true before chain.invoke and false in a finally inside each registered action, bind it into state, and disable each N |
| trustanchor-admin | wiring | apps/trustanchor-admin/src/PlayArea.tsx:29 | voteAgentId input seeded from stats once at mount, never re-syncs to live selected route | Drive the field from the live value instead of a one-shot initializer: either initialize to "" and set a useEffect that runs setVoteAgentId(String(stats.selectedAgentId)) when stat |
| unbreakable-vault | correctness | apps/unbreakable-vault/src/composables/useVaultBreaker.ts:245 | attemptFee fallback uses ?? (nullish) but loadVault produces 0, so a missing fee pays/shows 0 GAS instead of the 0.1 default | Treat 0/non-positive as missing: const fee = vaultDetails.get()?.attemptFee; const base = (Number.isFinite(fee) && fee > 0) ? fee : toFixed8(ATTEMPT_FEE); use base in both the disp |
| wallet-health | robustness | apps/wallet-health/src/PlayArea.tsx:168 | Connect Wallet button has no in-flight guard — double-click fires multiple ensureWallet() calls | Add an isConnecting observable in useWalletAnalysis, set it true at the top of connectWallet and false in a finally; bail early if already connecting (if (isConnecting.get()) retur |

## LOW (105)

| app | cat | file:line | issue | fix |
|---|---|---|---|---|
| aa-account-lab | correctness | apps/aa-account-lab/src/composables/useAAAccountLab.ts:129 | Escape timelock: zero silently coerced to 30 days, negatives unvalidated, shared bounds bypassed | Parse and validate explicitly before the chain call: `const n = Number.parseInt(registerForm.escapeTimelock, 10); if (!Number.isInteger(n) \|\| n < AA_REGISTRATION_MIN_ESCAPE_TIMEL |
| aa-account-lab | robustness | apps/aa-account-lab/src/composables/useAAAccountLab.ts:130 | Verifier params hex not validated before being sent as a ByteArray arg | Validate before invoke, e.g. `if (verifierParams && (verifierParams.length % 2 !== 0 \|\| !/^[0-9a-f]*$/i.test(verifierParams))) throw new Error(t('verifierParamsHint'));` or impor |
| aa-market-hub | correctness | apps/aa-market-hub/src/utils/validation.ts:36 | Intended price bounds and hash validators are dead code (never enforced) | Either wire the validators in: in CreateListingCard use isMarketPriceGas(priceGas) for the price gate and isHash160OrNeoAddress(...) for the AA/account inputs (and isHash160OrNeoAd |
| aa-market-hub | correctness | apps/aa-market-hub/src/components/CreateListingCard.tsx:39 | isValidGasPrice mixes whole GAS with padded fraction digits | Replace with the canonical parser already in the codebase: `const fractions = parseGasAmountFractions(normalizedPriceGas); const isValidGasPrice = fractions !== null && fractions > |
| aa-permissions-lab | robustness | apps/aa-permissions-lab/src/composables/useAAPermissionsLab.ts:52 | No format validation on AccountId/verifier/hook hashes before the OS/chain call | Add a lightweight client-side guard before the calls: validate accountIdHash/verifierHash/hookHash match /^0x?[0-9a-fA-F]{40}$/ (20-byte script hash) and verifierParamsHex matches  |
| aa-relay-console | correctness | apps/aa-relay-console/src/PlayArea.tsx:94 | Inline result labels a sponsorship *request* as 'Sponsor check complete.' | Distinguish by result shape: a SponsorshipResult has `approved`, a SponsorshipStatus has `eligible` (and a RelayResult has `txid`). In PlayArea, when `relayResult` is null, pick `t |
| aa-relay-console | robustness | apps/aa-relay-console/src/PlayArea.tsx:76 | Sponsor amount accepts zero / negative / out-of-range before reaching the edge request | Add a positive-number guard to the enable condition, e.g. `const amt = Number(sponsorAmountLocal); const hasSponsorAmount = Number.isFinite(amt) && amt > 0;` and surface a hint whe |
| aa-session-key-lab | robustness | apps/aa-session-key-lab/src/PlayArea.tsx:192 | Check/Request Sponsorship buttons have no in-flight disable guard | Add `disabled={isCheckingSponsorship}` to both sponsorship buttons (after fixing the binding in finding 1 so the flag actually reflects in-flight state), mirroring the disabled gua |
| asset-factory | robustness | apps/shared/factory/FactoryPlayArea.tsx:167 | copyText has no error handling and falsely reports success when clipboard is unavailable | Wrap in try/catch and guard the success state: `try { if (!navigator.clipboard) throw new Error('clipboard unavailable'); await navigator.clipboard.writeText(text); setCopied(targe |
| asset-factory | correctness | apps/shared/factory/factoryPlan.ts:96 | Symbol validation message contradicts the regex (digits-first symbols rejected) | Either relax the regex to allow a leading digit if intended (`/^[A-Z0-9][A-Z0-9]{1,11}$/`) or correct the message to 'Symbol must be 2-12 uppercase characters and start with a lett |
| asset-factory | robustness | apps/shared/factory/runtime.tsx:49 | generatedPackages counter increments even for blocked/unpublishable plans | Increment only when the plan is publishable: `if (plan.publishable) generatedCount.set(generatedCount.get() + 1);`, or rename the stat to 'plans generated' if counting attempts is  |
| automation-copilot | security | apps/automation-copilot/src/automationGateway.ts:134 | Target price accepts negative / non-numeric values without validation | Validate before building: reject targetPrice <= 0 or NaN with a t('...') error via lastError, and set the NeoInput to type=number with min=0 (and inputMode=decimal). Surface the er |
| automation-copilot | robustness | apps/automation-copilot/src/composables/useAutomationCopilot.ts:208 | refreshTriggers leaves a stale latestTrigger when the server returns an empty list | Reset selection when the list is empty: `latestTrigger.set(list[0] ?? null);` so the UI returns to the empty/route state consistent with triggerCount=0. |
| breakup-contract | wiring | apps/breakup-contract/src/pages/index/composables/useBreakupContract.ts:20 | Dead, divergent second composable (direct-chain version) left in tree | Delete src/pages/index/composables/useBreakupContract.ts (and prune the now-empty legacy pages/index/composables dir). Optionally relocate the still-used ContractList.tsx to src/co |
| council-governance | wiring | apps/council-governance/src/main.tsx:47 | createProposal action does not return the guard result, so success can never be detected | Return the guard result from the action handlers: `return ctx.services.notify.guard(...)` (or `const r = await ctx.services.notify.guard(...); return r;`). This lets PlayArea gate  |
| council-governance | wiring | apps/council-governance/src/main.tsx:88 | executeProposal/finalizeProposal action is registered and exported but unreachable from the UI | Either add a Finalize/Execute button in the proposal detail panel for statusKey==="passed" proposals that calls dispatch("executeProposal", proposal.id), or remove the unused regis |
| council-governance | correctness | apps/council-governance/src/composables/useGovernance.ts:373 | Policy value accepts non-integer floats for integer-only Neo policy parameters | Tighten validation to integers and non-negative range, e.g. `if (!Number.isInteger(parsedValue) \|\| parsedValue < 0) throw new Error(t("invalidPolicyValue"));` (and/or use BigInt  |
| custom-anchor | correctness | apps/custom-anchor/src/PlayArea.tsx:91 | agentCount falls back to hardcoded 21 when real count is 0 | Drop the `\|\| 21` fallbacks and render the actual agentCount (showing 0 or a neutral placeholder like '--' before load). If a non-zero default is desired purely for the empty/pre- |
| daily-checkin | robustness | apps/daily-checkin/src/PlayArea.tsx:183 | Refresh button not gated during claim; concurrent loadAll race | Disable the Refresh button while either action is in flight: disabled={isLoading \|\| isClaiming}. Optionally add a single in-flight guard at the top of loadAll to coalesce overlap |
| daily-checkin | correctness | apps/daily-checkin/src/PlayArea.tsx:64 | Confetti effect never updates prevCanCheckIn on the firing edge | Track previous value via a ref updated every render, or restructure: always call setPrevCanCheckIn(canCheckIn) (move it out of the conditional, unconditionally at the end of the ef |
| dev-tipping | robustness | apps/dev-tipping/src/main.tsx:49 | selectDev fires an unconditional success toast on every developer click | Drop the selectDev dispatch entirely and rely on the local setSelectedDevId in PlayArea (handleSelectDev already sets it), or make the action a silent no-op / remove the notify.suc |
| dev-tipping | robustness | apps/dev-tipping/src/PlayArea.tsx:55 | Tip form fields are not reset after a successful send | Have the action surface success (return the guard result / boolean) and, on success, clear the form state (setTipAmount(""), setTipMessage(""), optionally setSelectedDevId(null)).  |
| dice-game | correctness | apps/dice-game/src/PlayArea.tsx:53 | bool() binding ignores its second argument (dead default param) | Drop the second arg: `const isSubmitting = bool("isSubmitting");` to match the actual binding signature and avoid implying a configurable default that does not exist. |
| event-ticket-pass | robustness | apps/event-ticket-pass/src/composables/useEventTicket.ts:67 | Event/ticket IDs from Date.now() base36 can collide on rapid creation | Append a random suffix or a monotonic counter, e.g. `${prefix}-${Date.now().toString(36)}-${Math.random().toString(36).slice(2,8)}`, to guarantee uniqueness. |
| flashloan | wiring | apps/flashloan/src/PlayArea.tsx:404 | Recent-executions status uses wrong i18n key, renders raw "success"/"failed" | Map the status to the prefixed key like the lookup card does: replace `t(loan.status) \|\| loan.status` with `t(`status${loan.status[0].toUpperCase()}${loan.status.slice(1)}`)`, or |
| flashloan | perf | apps/flashloan/src/composables/useFlashloanCore.ts:230 | Recent-loans fetched with sequential awaits in a loop | Collect the IDs first then await Promise.all: `const ids = []; for (let id = totalLoans; id >= start; id--) ids.push(id); const entries = await Promise.all(ids.map(id => chainServi |
| flashloan | robustness | apps/flashloan/src/composables/useFlashloanCore.ts:177 | Loan amount validation does not enforce contract min/max bounds | In validateLoanRequest, after parsing amountNum, compare against the loaded bounds: `if (amountNum < contractStats.get().minLoan \|\| amountNum > contractStats.get().maxLoan) retur |
| fogplay | robustness | apps/fogplay/src/main.tsx:41 | No inline validation feedback while typing an out-of-range amount | In setBetAmount, run validateBetAmount(amount) and set validationError to its result so the inline error appears as the user types (this also fixes fogplay-1's clear-on-change need |
| fogplay | correctness | apps/fogplay/src/locale/messages.ts:65 | Displayed wager range (0.1 - 100) contradicts actual minimum (0.05) | Set wagerRange to '0.05 - 100' (or derive it from MIN_BET/MAX_BET) so the displayed range matches the enforced range. |
| fogplay | perf | apps/fogplay/src/utils/audio.ts:57 | audioManager module is exported but never imported anywhere (dead code) | Either delete apps/fogplay/src/utils/audio.ts, or wire audioManager.play('flip'/'win'/'lose'/'click') into the corresponding placeBet/win/loss/choice paths if the sound feedback wa |
| forever-album | wiring | apps/forever-album/src/composables/useForeverAlbum.ts:247 | openDecrypt does not set decryptTarget, so the viewer's Decrypt button would no-op if its branch were reachable | Remove the dead encrypted branch in the viewer, or have openDecrypt set decryptTarget from viewingPhoto.get() before opening the decrypt modal so handleDecrypt has a valid target i |
| gas-sponsor | robustness | apps/gas-sponsor/src/main.tsx:44 | Donate/Send failures use the wrong error fallback key 'loadFailed' ('Failed to load data') | Pass a transfer-specific fallback key, e.g. notify.guard(() => sponsor.handleDonate(), 'donateSuccess', 'donateFailed') and notify.guard(() => sponsor.handleSend(), 'sendSuccess',  |
| gasbox | correctness | apps/gasbox/src/composables/useGasBox.ts:536 | buyMachine deposits a decimal display string while every other deposit uses base units (1e8x underpay) — currently unreachable | Make buyMachine consistent with playMachine: deposit `String(Math.trunc(machine.salePriceRaw))` (base units), or standardize a single helper for converting a Machine price to the d |
| graveyard | correctness | apps/graveyard/src/composables/useGraveyard.ts:154 | Fee/gas display math diverges between executeDestroy and loadStats | Derive the per-burial increment from the same buryFee source used in loadStats (store/read a single fee constant) so the optimistic increment in executeDestroy and the reload value |
| graveyard | security | apps/graveyard/src/composables/useGraveyard.ts:131 | assetHash sent to burn with only a length check, no format validation | Add a lightweight format check before burn (e.g. validate hex / expected hash length / strip-and-revalidate) and show an inline error, matching the existing assetHashTooShort UX pa |
| last-survivor | correctness | apps/last-survivor/src/composables/useLastSurvivor.ts:332 | History sort comparator returns NaN for winner entries, producing unreliable ordering | Sort on a real numeric/temporal key rather than coercing heterogeneous ids: give every entry a numeric sortKey (e.g. a timestamp) at construction and sort on that, or fall back to  |
| memorial-shrine | wiring | apps/memorial-shrine/src/main.tsx:27 | closeMemorial / shareMemorial are implemented but never registered or wired to any control | Either register actions (ctx.registerAction("closeMemorial", ...) / ("shareMemorial", ...)) and add a close (X) button and a Share button to the detail-card header in PlayArea, or  |
| memorial-shrine | robustness | apps/memorial-shrine/src/composables/useMemorialContract.ts:66 | Dead composable contains a two-step payTribute that can strand GAS with no rollback | Delete apps/memorial-shrine/src/composables/useMemorialContract.ts and useMemorialActions.ts (both fully unused). If a two-step flow is ever needed, model it on the atomic invokeWi |
| memorial-shrine | correctness | apps/memorial-shrine/src/PlayArea.tsx:61 | Create form validates only string length, so non-numeric death year is silently stored as 0 | Validate that the parsed year is a sane integer before enabling submit, e.g. canCreateMemorial = formName.trim() && Number.isInteger(parseInt(formDeathYear,10)) && parseInt(formDea |
| memorial-shrine | robustness | apps/memorial-shrine/src/PlayArea.tsx:180 | Offering cost display is derived by string-splitting the translated label on the '·' character | Carry the GAS cost as a structured field on each offering option (e.g. { value, label, costGas }) and render selectedOffering.costGas directly, instead of string-splitting the labe |
| milestone-escrow | robustness | apps/milestone-escrow/src/composables/useMilestoneEscrow.ts:411 | Beneficiary address never validated; empty/invalid value reaches escrow create | Before the deposit, validate beneficiaryAddr: reject empty and validate the N3 address shape (reuse the shared address validator if available), throwing new Error(t('invalidAddress |
| miniapp-factory | correctness | apps/shared/factory/runtime.tsx:49 | "Generated" stat overcounts: increments on every Generate click, even for identical input | Only bump generatedCount when the new plan's digest differs from the previously applied plan's digest, e.g. capture the prior plan in applyPlan and do `if (currentPlan.get()?.diges |
| miniapp-factory | robustness | apps/shared/factory/FactoryPlayArea.tsx:265 | Generate button has no double-submit / in-flight guard | Add an isGenerating observable in createFactorySetup, set it true/false around the generatePlan action body, and bind it to the Generate NeoButton as loading={isGenerating} / disab |
| miniapp-factory | wiring | apps/shared/factory/FactoryPlayArea.tsx:21 | symbol_format error copy says "letters or digits" but regex forbids a leading digit | Align copy and rule: either change the message to "Symbol must start with an uppercase letter, 2-12 uppercase letters or digits." or, if a leading digit is intended to be valid, re |
| neo-convert | wiring | apps/neo-convert/src/composables/useNeoConvert.ts:58 | activeTab never updated — sidebar 'Active Tab' is permanently stuck on 'generate' | Either remove the activeTab sidebar item / tabs from the manifest (they are unused in this single-scroll layout), or, if tabs are intended, wire an action that sets activeTab on ta |
| neo-convert | correctness | apps/neo-convert/src/composables/useConverter.ts:46 | Orphaned second showSecrets observable in useConverter (dead state) | Delete the showSecrets observable from useConverter (and its reset in clearResult), or, if independent reveal state per section is desired, bind useConverter.showSecrets separately |
| neo-multisig | correctness | apps/neo-multisig/src/composables/useMultisigCreation.ts:52 | Entire useMultisigCreation composable (and 5 UI components) is dead, unused code | Delete useMultisigCreation.ts and the unused components (or wire them in if intended). If kept intentionally, add a comment/exclusion so reviewers don't assume it is the active pat |
| neo-ns | robustness | apps/neo-ns/src/hooks/useNeoNS.ts:182 | setRecord/transferDomain pass addresses to chain.invoke with no format validation | Validate the address before invoking: trim, reject empty/whitespace, and validate Neo N3 address format (e.g. the shared neoHelpers / address validator). Surface t('invalidAddress' |
| neo-ns | correctness | apps/neo-ns/src/hooks/useNeoNS.ts:78 | expiringSoon counts already-expired domains as 'expiring soon' | Add a lower bound so only future-but-near expiries count: d.expiry - Date.now() > 0 && d.expiry - Date.now() < EXPIRY_WARNING_MS, and surface already-expired domains in a separate  |
| neo-pay | wiring | apps/neo-pay/src/PlayArea.tsx:385 | Initial stream load shows no spinner; isRefreshing bound but unused | Gate the list spinners on isRefreshing (or isLoading \|\| isRefreshing) and keep the create button's loading on isCreating, so the lists reflect actual fetch state. |
| neo-pay | robustness | apps/neo-pay/src/PlayArea.tsx:470 | Per-row Cancel/Claim buttons not disabled while a cancel/claim is in flight | Expose claimingId/cancellingId in the state record and disable/loading-spin the specific row's button when stream.id matches, so concurrent actions are visibly queued rather than d |
| neo-pay-shared-example | correctness | apps/neo-pay-shared-example/src/main.tsx:34 | createStream action reads form.title not declared in the cast type (tsc error) | Add `title?: string;` to the cast type at apps/neo-pay-shared-example/src/main.tsx lines 24-30 so the type matches the payload PlayArea dispatches. |
| neo-pay-shared-example | robustness | apps/neo-pay-shared-example/src/PlayArea.tsx:497 | Claim/Cancel buttons show no in-flight state (guarded only inside composable) | Bind claimingId and cancellingId into the state record in main.tsx, then in PlayArea set loading/disabled on the specific stream's Claim/Cancel button when its id matches the in-fl |
| neo-sign-anything | perf | apps/neo-sign-anything/src/components/SignHero.tsx:1 | Dead/duplicated source: unused components and legacy pages/ tree | Delete src/components/Sign*.{tsx,scss}, src/pages/index/, and src/pages/index/composables/useSignAnything.ts (the dead duplicate), or wire them in if they were intended to replace  |
| neo-swap | perf | apps/neo-swap/src/hooks/useSwapEngine.ts:115 | Two independent price RPC reads run sequentially in loadExchangeRate | Run them concurrently: `const [fromPrice, toPrice] = await Promise.all([datafeed.getPrice(fromToken.get().symbol), datafeed.getPrice(toToken.get().symbol)]);` then compute `const r |
| neo-treasury | robustness | apps/neo-treasury/src/PlayArea.tsx:186 | handleDisbursement awaits dispatch without try/catch -> unhandled rejection on submit failure | Wrap the body of handleDisbursement in try/catch (swallow or setStatus the error) so the rejection is handled at the component boundary, e.g. `try { await dispatch(...); setStatus( |
| neo-treasury | robustness | apps/neo-treasury/src/PlayArea.tsx:447 | Watchlist row React key falls back to empty string, risking duplicate keys | Use a guaranteed-unique key, e.g. key={wallet.address \|\| `${group.name}-${index}`} by including the map index, or filter out wallets without an address before rendering. |
| neo-x-bridge | security | apps/neo-x-bridge/src/bridgeConsole.ts:137 | Recipient / target contract accepted with no address-format validation | Add lightweight per-direction address validation: for the Neo X side require /^0x[0-9a-fA-F]{40}$/ (EVM), for the Neo N3 side validate the N3 address/script-hash shape, and surface |
| neo-x-bridge | perf | apps/neo-x-bridge/src/PlayArea.tsx:194 | Launch-context effect omits runWorkspaceAction/dispatch from deps (stale-closure smell, low impact) | Wrap runWorkspaceAction in useCallback (or move it above the effect and include it in the dependency list), then add it to the effect deps so the lint exhaustive-deps rule is satis |
| nft-factory | robustness | apps/shared/factory/FactoryPlayArea.tsx:167 | copyText shows false "Copied" and can throw unhandled rejection when clipboard unavailable | Wrap in try/catch and only confirm on real success, with a fallback: `try { if (!navigator.clipboard) throw new Error('no clipboard'); await navigator.clipboard.writeText(text); se |
| nft-factory | correctness | apps/shared/factory/factoryPlan.ts:96 | Symbol validation rejects digit-leading symbols but error copy claims digits are allowed anywhere | Either relax the regex to permit a leading digit if that is intended (`/^[A-Z0-9]{2,12}$/`), or update ERROR_COPY.symbol_format to state the real constraint, e.g. "Symbol must be 2 |
| nft-factory | perf | apps/shared/factory/FactoryPlayArea.tsx:137 | Live-preview fallback rebuilds plan and JSON on every keystroke/render before first generate | Memoize the fallback plan on the actual draft inputs, e.g. `const fallbackPlan = useMemo(() => buildFactoryPlan(kind, draft, { appId }), [kind, nep11, appId]); const currentPlan =  |
| on-chain-tarot | perf | apps/on-chain-tarot/src/composables/useTarot.ts:230 | waitForReading sleeps 500ms even on the final attempt and after a not-found read | Skip the sleep on the last attempt: `if (attempt < maxAttempts - 1) await new Promise(...)`. Better: since `derivedCards` is already computed and authoritative, treat the storage r |
| on-chain-tarot | wiring | apps/on-chain-tarot/src/components/TarotActions.tsx:1 | TarotActions.tsx and TarotHero.tsx are dead components never imported by PlayArea | Delete src/components/TarotActions.tsx, src/components/TarotHero.tsx and src/components/TarotHero.scss, or import and actually use them from PlayArea. Confirm no other app referenc |
| on-chain-tarot | correctness | apps/on-chain-tarot/src/composables/useTarot.ts:310 | Local readingsCount increment can diverge from the persisted storage index | Only increment readingsCount after a confirmed successful storage.set (move the increment inside the try, or re-derive from storage). Alternatively, after a successful draw call lo |
| on-chain-tarot | correctness | apps/on-chain-tarot/src/composables/useTarot.ts:100 | extractReadingId poolId fallback can yield a non-unique seed and identical readings | Remove poolId/pool_id from the id-extraction precedence (a pool id is not a per-draw unique value), or only accept it when it is combined with another unique component. Keep the tx |
| on-chain-tarot | wiring | apps/on-chain-tarot/src/composables/useTarot.ts:337 | getReading is exported but never consumed | Either wire getReading into a share/copy control in PlayArea (and register it as an action), or remove it from the composable's return to reduce dead API surface. |
| oracle-compute-lab | correctness | apps/shared/components-react/ConsoleToolPanel.tsx:128 | reset() clears status/digest but leaves requestCount stale | If the counter is meant to represent the current session's preview activity, add setObservable(state, 'requestCount', 0) inside reset(). If it is intended to be a lifetime/session- |
| oracle-http-console | security | apps/shared/components-react/ConsoleToolPanel.tsx:85 | "Digest" is a 32-bit non-cryptographic FNV hash but is marketed as an audit/integrity primitive | If the digest is meant to be comparable/auditable across the intended-vs-submitted boundary, derive it from a real hash (e.g. SHA-256 of the canonical `${method}\|${url}\|${jsonPat |
| oracle-http-console | correctness | apps/oracle-http-console/src/appConfig.ts:80 | POST body is folded into the digest and payload even when method is GET | In buildResult, gate the body on method: `const body = method === "POST" ? clean(values.body, "") : "";` so GET requests neither include the body in payload nor let it perturb the  |
| oracle-http-console | robustness | apps/oracle-http-console/src/appConfig.ts:77 | URL field accepts arbitrary unvalidated text with no scheme/format check | Add lightweight validation in buildResult (or before allowing copy) — e.g. try `new URL(url)` and require protocol http:/https:; on failure set status to an error message via t() i |
| oracle-neodid-console | correctness | apps/shared/components-react/ConsoleToolPanel.tsx:123 | Request counter increments even on validation failure | Only increment when a real preview is produced, e.g. guard on next.payload.status !== "input_required" (or an ok flag) before calling setObservable(state, "requestCount", count + 1 |
| oracle-neodid-console | correctness | apps/shared/components-react/ConsoleToolPanel.tsx:122 | lastDigest set to empty string after failed preview | Fall back to the existing/placeholder value instead of "": setObservable(state, "lastDigest", String(next.payload.digest ?? next.payload.requestId ?? t("notAvailable"))), or skip u |
| oracle-price-console | wiring | apps/oracle-price-console/src/manifest.ts:28 | Stat tile binds to non-existent state key 'oracleHash' and missing message key 'overviewOracle' | Remove the 4th stat entry, or point it at an existing state observable (e.g. valueKey:"datafeedShort" or expose datafeedHash in the returned state) and use an existing label key li |
| oracle-seal-console | correctness | apps/shared/components-react/ConsoleToolPanel.tsx:132 | reset() sets lastDigest to 'N/A' but initial state shows '—' (inconsistent placeholder) | In ConsoleToolPanel.reset(), change setObservable(state, 'lastDigest', t('notAvailable')) to t('digestPlaceholder') so reset matches the initial value. (Shared component — affects  |
| oracle-seal-console | security | apps/oracle-seal-console/src/appConfig.ts:83 | 'Sealed envelope' digest is a non-cryptographic 32-bit FNV-1a hash (no confidentiality/integrity) | Either (a) reword the output labels/docs to 'preview fingerprint' (not 'digest'/'seal') to avoid implying cryptographic protection, or (b) if a real digest is intended, compute it  |
| oracle-seal-console | robustness | apps/oracle-seal-console/src/appConfig.ts:82 | Payload field accepts arbitrary text; invalid JSON is silently 'sealed' | In buildResult, attempt JSON.parse(payload) when non-empty; on failure either include a row/flag like 'payloadValid: false' in the result or surface a warning via the result status |
| oracle-vrf-console | robustness | apps/oracle-vrf-console/src/appConfig.ts:110 | `rounds` number field accepts 0/negative/non-integer with no validation | In buildResult, parse and clamp rounds: `const n = Math.max(1, Math.floor(Number(rounds) \|\| 1)); const roundsStr = String(n);` and use it for both summary and payload; optionally |
| oracle-vrf-console | wiring | apps/oracle-vrf-console/src/appConfig.ts:33 | Hero stat labelled "Mode" actually shows the static endpoint label, and the chosen proof mode is never surfaced | Either relabel the stat (use a `statEndpoint`/'Endpoint' label whose value matches) or bind a `lastMode` observable updated in runPreview and point the 'Mode' stat at it. Simplest: |
| private-transfer | robustness | apps/private-transfer/src/PlayArea.tsx:24 | Recipient regex allows base58-invalid characters and skips checksum | Use a Base58 alphabet character class /^N[1-9A-HJ-NP-Za-km-z]{33}$/ and, if a base58check helper is available in shared utils, verify the 4-byte checksum before enabling Seal. |
| private-transfer | robustness | apps/private-transfer/src/PlayArea.tsx:295 | Memo silently truncated to 160 chars with no UI feedback | Add maxLength={160} to the memo input and a small live counter (e.g. memo.length/160) so the truncation is visible before sealing, keeping the displayed value and the sealed value  |
| profitanchor | robustness | apps/profitanchor/src/main.tsx:85 | No shared in-flight lock; stake/withdraw double-submittable across PlayArea and OperationPanel | Gate runAnchorAction on a shared in-flight flag: at the top do `if (anchor.isLoading.get()) return;` then `anchor.isLoading.set(true)` in a try/finally (or add a dedicated submitti |
| profitanchor-admin | wiring | apps/profitanchor-admin/src/PlayArea.tsx:30 | voteAgentId default seed is computed once and never updates after stats load | Derive the input's default from the bound stats observable instead of a one-shot useState seed, e.g. compute a `selectedAgentId` from str/val and use a useEffect to sync voteAgentI |
| quadratic-funding | correctness | apps/quadratic-funding/src/composables/useQuadraticContributions.ts:69 | Missing i18n key 'neoNoFractional' renders the raw key as the error message | Add neoNoFractional: { en: 'NEO is indivisible — enter a whole number.', zh: 'NEO 不可分割，请输入整数。' } to appMessages in apps/quadratic-funding/src/locale/messages.ts (or to shared base- |
| quadratic-funding | robustness | apps/quadratic-funding/src/PlayArea.tsx:195 | Create/Register/Contribute buttons show no in-flight state (no loading/disable) | Bind isCreatingRound/isRegisteringProject/isContributing in the main.tsx state record and pass loading={...} / disabled={...} to the three primary buttons (RoundForm already accept |
| recovery-guardian | wiring | apps/recovery-guardian/src/main.tsx:217 | `share*` actions silently duplicate copy and no copy/share/query emits any success feedback | Either (a) implement share via `navigator.share?.({url})` with copy as fallback, or (b) collapse the redundant Share buttons. In both copy and share handlers call ctx.services.noti |
| recovery-guardian | wiring | apps/recovery-guardian/src/main.tsx:22 | `isLoading` gate is dead — never set true, so part of canPrepareRecovery is inert | Remove the unused `isLoading` observable (and its `!isLoading` term in PlayArea.tsx:51) since nothing sets it, OR if an async link operation is intended, actually set isLoading tru |
| red-envelope | wiring | apps/red-envelope/src/composables/useRedEnvelope.ts:493 | NEO eligibility / wallet-gating apparatus is fully exported but never wired into the UI | Either wire checkEligibility into the claim flow (call it before dispatch and show eligibilityReason / disable Claim when !isEligible), or remove the unused eligibility exports + o |
| red-envelope | correctness | apps/red-envelope/src/composables/useRedEnvelope.ts:387 | createPool is never given the envelope funding amount or per-packet distribution | Pass the funding amount into the pool creation path (extend PoolConfig or include it in the create call payload) and include the amount in the deposit memo, so the pool's claimable |
| red-envelope | wiring | apps/red-envelope/src/main.tsx:66 | Opening-modal flow (dismissOpeningModal / openFromList / showOpeningModal) is registered/exported but unreachable | Remove the unused dismissOpeningModal action and the openFromList/showOpeningModal/openingEnvelope/openEnvelope exports, or wire the activity-list rows to openFromList if the stage |
| self-loan | robustness | apps/self-loan/src/composables/useSelfLoan.ts:484 | repay() accepts amounts exceeding outstanding debt with no cap | Validate against outstanding debt: if (Number(value) > loan.get().borrowed) throw new Error(t('...')) or clamp to the borrowed amount before deposit; optionally disable Repay when  |
| soulbound-certificate | robustness | apps/soulbound-certificate/src/composables/useSoulbound.ts:325 | connectWallet has no in-flight guard (double-submit) | Add a connecting observable and guard the body (`if (connecting.get()) return; connecting.set(true); try { ... } finally { connecting.set(false); }`), and disable/loading the conne |
| soulbound-certificate | wiring | apps/soulbound-certificate/src/composables/useSoulbound.ts:491 | Dead action openIssueModal / unconsumed soulbound:selectTemplate event | Remove openIssueModal from the composable return and its registerAction in main.tsx, or wire a real consumer if a cross-miniapp template-select hand-off is intended. |
| soulbound-certificate | correctness | apps/soulbound-certificate/src/PlayArea.tsx:114 | createFormValid omits the maxSupply upper bound enforced by the composable | Mirror the composable bound in createFormValid: add `&& Number(createForm.maxSupply) <= 100000` so the button stays disabled for out-of-range supply. |
| time-capsule | correctness | apps/time-capsule/src/composables/useTimeCapsule.ts:375 | category is collected and persisted but never read back - write-only dead field | Either add category to StoredCapsule/Capsule and surface it (e.g. a badge/label on each capsule item and/or use it for filtering), or remove the category select and its persistence |
| time-capsule | correctness | apps/time-capsule/src/components/CapsuleHero.tsx:31 | Hero/sidebar counts are app-wide (all users) not the current user's, and the list is capped at 50 with no pagination | Persist owner on each record at create time (use the wallet address instead of owner:"") and filter loadCapsules results to the current wallet so counts and the list reflect the us |
| timestamp-proof | wiring | apps/timestamp-proof/src/composables/useTimestampProof.ts:73 | yourProofs derive reads the unbound `address` observable, so it goes stale on wallet connect/switch | Either bind `address` in the state record returned from main.tsx setup() (add `address: proofContract.address`) so any address change triggers a host re-read, or pass `[proofs, add |
| timestamp-proof | robustness | apps/timestamp-proof/src/PlayArea.tsx:120 | Verify-input error message is not cleared while the user edits the proof ID | Clear the error on edit: in the verify input onChange do `onChange={(v) => { setVerifyId(v); if (verifyError) dispatch('clearVerifyError'); }}` after registering a `clearVerifyErro |
| trustanchor | wiring | apps/trustanchor/src/hooks/useTrustAnchor.ts:195 | stats.rps (rewardPerNeo) is parsed but never displayed | Either render rps (e.g. as a 'reward per NEO' stat chip with an i18n label) or drop the rps field from the TrustAnchorStats interface and loadStats() to keep the parsed shape hones |
| trustanchor | perf | apps/trustanchor/src/main.tsx:77 | ingressCount observable is created and bound to state but never read | Remove the ingressCount observable and its entry in the returned state record; if a vote-route count is meant to be shown, wire it into a stat chip / route card instead. |
| unbreakable-vault | correctness | apps/unbreakable-vault/src/composables/useVaultBreaker.ts:124 | normalizeAttemptFeeFixed8 double-scales small fixed8 fee values, overpaying | Decide one canonical unit. Since attemptFee is read as fixed8 raw, drop the <1_000_000 re-scaling branch entirely and just return String(Math.trunc(numeric)) for integer input; onl |
| wallet-health | perf | apps/wallet-health/src/composables/useWalletAnalysis.ts:56 | NEO and GAS balance reads run sequentially instead of in parallel | Run them with Promise.all: const [neoRaw, gasRaw] = await Promise.all([chain.read('balanceOf', [...], {scriptHash: NEO_HASH}), chain.read('balanceOf', [...], {scriptHash: GAS_HASH} |
| wallet-health | correctness | apps/wallet-health/src/composables/useHealthScore.ts:36 | Checklist persistence ignores injected OS StorageProxy and uses unprefixed global localStorage | Either actually persist through the injected StorageProxy (storage.get/set with an app-scoped key) and remove `void storage`, or, if runtime-cache is intended, namespace the key (e |
| wallet-health | correctness | apps/wallet-health/src/main.tsx:38 | errorKey 'refreshFailed' is undefined in locales and unreachable | Add a 'refreshFailed' entry (en/zh) to locale/messages.ts. This becomes meaningful once finding #1 is fixed so refresh errors actually propagate to the wrapper. |
| wallet-health | perf | apps/wallet-health/src/composables/useWalletAnalysis.ts:26 | BalanceService injected but unused; several derives are dead code | Remove the unused `balance` param (and its plumbing in useWalletHealth/main.tsx) and delete the unused isUnsupported/chainLabel/chainVariant derives, or wire balances through Balan |

