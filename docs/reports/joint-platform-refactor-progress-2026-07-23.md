# Joint Platform Refactor Progress — 2026-07-23

## 2026-07-24 current-state addendum

This addendum supersedes only the point-in-time test and framework counts in the historical sections below; the earlier evidence remains retained for chronology.

- Current local gates: `neo-os-web` deploy scripts **297/297**, framework **619/619**, and contract suite **633/633**; `neo-abstract-account` full local verification **219/219** contract tests, **416/416** frontend tests, **79/79** SDK tests, build and audit pass; `neo-morpheus-oracle` worker **470/470**, relayer **440/440**, control-plane **31/31**, scripts **92/92**, runtime matrix **6/6**, and web build/content checks pass. Both deployable Next workspaces now resolve **15.5.21**; root, Host, and Admin dependency audits report **0 vulnerabilities**, and Host production build plus desktop/mobile E2E pass **52/52** on that version.
- OneGate / NEP-21 validation is recorded at `docs/reports/onegate-nep21-validation-2026-07-24.md`: local current Host reload is **77/77**, **138** dAPI calls, and **21** wallet-interaction flows; the latest online `https://neomini.app` reload is **60/77** because 16 remote entries return 404 and `asset-factory` leaks the host shell. Previous deployed snapshot evidence remains **77/77**; SDK **70/70**, Host OneGate/Vault **42/42**, and shared OneGate/NEP-21 **45/45** pass. The official OneGate Browser Mock successfully loads the Unbreakable Vault reference in offline mode. This proves provider injection, encoded requests, resource integrity, and error boundaries, not paired-device approval or funded chain execution; one intentional review-only AA Relay warning remains.
- The shared `MiniAppCreditLedger` primitive now owns credit/liability mutations for PlatformRegistry, EngineBase, PlatformSocial, PlatformDeFi, PlatformAnchor, PlatformVesting, and PlatformEscrow without changing existing appId, asset, memo, or storage-key contracts; its shared `:credit` and `:fund` memo decoder accepts the String/ByteArray NEP-17 callback forms exercised by TestEngine. `PlatformRegistry` now requires reciprocal AA core configuration for engine-bound registration/attachment and materializes the unique shared account before external engine activation. Registry, Social, DeFi, Vesting, and Escrow artifacts regenerate successfully and the contract suite passes **633/633** against refreshed artifacts; no live deployment was changed.
- `MiniAppHouseGameBase.ValidateDeposit` now reuses the DevPack memo decoder, so the two current V2 house-game NEFs accept both String and ByteArray callback payloads without changing their fund/bet/stake grammar; CoinFlipV2/DiceV2 behavior tests pass **16/16**. The earlier complete non-V2 contract baseline remains **633/633**; the aggregate runner was not used as completion evidence after this isolated V2 change because the local runner stopped returning a process result under concurrent .NET workloads.
- The 2026-07-25 continuation applies the same String/ByteArray callback handling to `MiniAppMoneyBase.CreditGasDeposit`; its rebuilt reference NEF and TestEngine fixture pass **5/5**. OneGate coverage remains **77/77** with zero failures, while a source census still finds **30** legacy `(string)data` casts that require contract-by-contract migration, artifact refresh, deployment, and live read-back before they can be counted as complete.
- `PlatformEscrow` now supports a bounded M-of-N approver set for each milestone while preserving the single-creator `createEscrow` lane; threshold, duplicate-approval, pre-threshold claim, and post-threshold claim behavior pass **7/7** against the rebuilt NEF. The guarded framework encodes the new lane and its interface audit is **18/18**; this remains source/build/test evidence with no deployment or live binding.
- Fresh three-repository local gates also pass: AA contract tests **219/219**, frontend **416/416**, SDK **79/79**, frontend production build and high/critical audit clean under the `--skip-contract-build --skip-e2e` verification mode; Morpheus engine sync/parity **3/3**, worker, relayer **440/440**, control-plane **31/31**, Web **190/190**, production build, and content consistency all pass. The engine sync gate caught and then cleared a generated `merge` drift through the official sync script.
- Morpheus Web dependency verification now covers both the root workspace and the standalone `apps/web` lock: Next **15.5.21** and PostCSS **8.5.16** resolve with **0** high/critical audit findings; `bash scripts/verify_repo.sh` exits **0**, including worker **470/470**, relayer **440/440**, scripts **92/92**, Web **190/190**, Next **15.5.21** production build, and lint.
- Current framework coverage: PlatformRegistry **49/49**, PlatformDeFi **58/58**, PlatformSocial **36/36**, PlatformAnchor **38/38**, MiniAppFactory **13/13**, PlatformVesting **13/13**, and PlatformEscrow **17/17**. The Registry interface report was regenerated and its freshness check passes; the PlatformAnchor, PlatformVesting, and PlatformEscrow interface reports also pass.
- Standard reward-game rules now share the framework difficulty selector across **11/11** compatible games; each retains its own stakes, timing, target, payout, and status semantics. `zhuada-e` and `gas-lucky-pool` remain intentionally outside this model because their rule systems are structurally different. The framework gate is now **50/50 files, 619/619 tests**, and the 11-game engine regression is **11/11 files, 241/241 tests**.
- Post-change miniapp suites are **24/24**; `zhuada-e` now passes its 16 test files and 185 tests after material-key surface merging, shadow-caster capping, and a two-material zongzi leaf treatment. No shared-rule or OneGate/NEP-21 test failed.
- Shared-AA identity now has an explicit read-only prediction path: `getPredictedAbstractAccount` returns the same `(core, accountId, materialized)` tuple before registration in the AA core, while `app.platformAccount` exposes it as `predictedIdentity` without conflating it with a materialized account. Stable V2 derivation is independent of mutable app-admin ownership, with legacy AA methods retained for compatibility; Registry/AA source tests cover pre-materialization admin rotation and engine-bound core enforcement. Contract coverage is **633/633**, framework coverage is **619/619**, and no chain write was performed.
- Fresh read-only checks at 2026-07-24 14:29 UTC keep the deployment boundary explicit: shared-AA preflight remains `phase=upgrade_contracts`, `safe_to_materialize=false`, with nine missing Registry methods and eleven missing AA registrar/account methods, now including stable account derivation/registration; PlatformGame remains **0/11** runtime-complete and **0/11** funded-lifecycle-proven, while its five future-local-only candidates remain **0/5** runtime-ready. No chain write occurred.
- Current read-only testnet state: **77/77** Registry directory app rows are active; platform artifacts are **2/9** local matches, **4** drifted, and **3** without a deployment record.
- Namespace policy is now explicit in the local Registry source: permissionless `RegisterApp` rejects the platform-owned `miniapp-*` prefix while `RegisterAppByPlatform` remains the controlled lane; custom appIds remain permissionless and the residual custom-namespace policy is still documented as an open design boundary. Registry build has **0 errors**, and the focused registration suite passes **15/15**. The live Registry still predates this rule, so it remains source-fixed/pending the separately authorized Registry upgrade.
- The cohort registration and PlatformRegistry full-loop helpers now mirror that policy: `miniapp-*` rows use the fee-exempt platform lane without unnecessary credit top-ups, while custom ids retain the prepaid permissionless lane; the Go registration gate passes.
- Shared-AA state remains incomplete: preflight phase `upgrade_contracts`, `safe_to_materialize=false`; live Registry lacks **9** shared-AA methods and live UnifiedSmartWallet lacks **11** registrar/account methods, including stable account derivation/registration; predicted roster is **77/77 unique**, but materialization is **0/77**.
- AA provenance audit: the tracked `neo-abstract-account` Git history contains only **1** `UnifiedSmartWalletV3.nef` revision, checksum **2765313802**; it does not match the live checksum **1086937376**, so exact live source provenance remains unproven rather than merely unsearched.
- PlatformGame migration remains **0/11** funded-complete; the ledger now separates the **11** absorption-cohort apps from **5** `future-local-only` candidates that remain contractless and are not runtime-ready. The cross-repository duplication audit remains complete as an audit, with the **11** legacy clone contracts intentionally retained for recovery and rollback.
- The four non-user-owned zero-drain candidates (`arrow-escape`, `bead-workshop`, `fruit-funnel`, and `screw-sort`) now omit the misleading source-level `contract: { mode: "custom" }` declaration; their public manifests were already contractless, so the local-only boundary is represented consistently until a reviewed PlatformGame engine wrapper and descriptor exist. `zhuada-e` remains outside this edit because its worktree is concurrently user-owned.
- Current UI/UX audits: layout **77/77** with no failures, PlayArea catalog **77/77** with no gaps, and media **77 manifests / 16 definitions** with no failures. Coverage classification is **36 testnet+mainnet, 12 source-only, 24 frontend-only, and 5 testnet-only**, with no RPC-unknown rows; the five testnet-only apps require an explicit mainnet release or defer decision.
- Live-harness audit is now persisted from the command itself and reports **77/77** active apps, **42** live-chain flows, **12** server-backed flows, **3** shared-runtime flows, **20** UI-only flows, and **0** missing or blocked harnesses. FogPlay, GasBox, Last Survivor, and SelfLoan now route to their standalone-contract harnesses; GasBox has a dedicated create/configure/fund/prepay/pull/withdraw lifecycle validator, so the retired PlatformGame flagship path is no longer counted as evidence for those contracts.
- The regenerated aggregate goal report is currently **4 pass / 8 fail** with **0** direct testnet transactions: Host/Admin and Oracle local gates now pass structurally, while the remaining failures include five release/defer decisions, live contract drift, 6/33 mainnet and 32/33 testnet stale feeds, 38 configured-pair drift/staleness blockers, and missing live monitor/control-plane/transaction evidence; this remains an explicit incomplete-evidence boundary.
- The shared framework AA boundary is now truthful: the default `apps/shared` `AAService` no longer advertises a fake `createSessionKey` result, so `app.aa.sessionKey.create` correctly raises a typed capability error until a generic verifier adapter is injected; the dedicated `aa-session-key-lab` remains the authoritative network-specific session-key implementation and proof path.
- The PlatformGame migration freshness gate now normalizes recursively embedded live-state timestamps instead of treating every read-only evidence refresh as a semantic ledger change; its focused regression passes **4/4**. The shared Vitest gate now passes **398/398 files and 4446/4446 tests** under default concurrency after standardizing a 30-second global test timeout and retaining higher limits for the two intentionally heavy cases.
- Morpheus game-engine single-source verification is now part of the standard local gates: `npm run test:game-engine-sync` performs a fresh sibling-repo generation check, Sudoku data semantic parity, and the checked-in behavioral goldens; the focused run passes **3/3**, the verification-entrypoint regression passes **4/4**, and both `verify_repo.sh` and `run_local_validation_gates.sh` invoke it. This is reproducible worktree evidence, not proof of a committed or deployed runtime.
- PlatformVesting now provides the in-repo phase-3 vesting/streams lane: native GAS/NEO credit-only funding, `(appId,asset,payer)` credit isolation, liability counters, linear/cliff-compatible release parameters, pause-immune claim/cancel/credit exits, and a guarded `app.platformVesting` surface. Local contract tests pass **3/3**, source/build acceptance is **8/8**, and the framework ABI audit is **13/13**. This is source/build/test evidence only: NeoPay remains a separate live reference, while PlatformVesting has no deployment record, Registry binding, funded lifecycle, or migration read-back.
- NeoPay now contains a compatibility consumer path that prefers `app.platformVesting` when the host injects a valid shared binding: reads, atomic native-funding create, claim/cancel, and durable pending-operation recovery are covered by the full app suite (**44/44**). The existing NeoPay manifest remains on its legacy custom contract because no PlatformVesting deployment/hash is available; this proves local routing and recovery only, not deployment, binding, funding, or migration.
- The typed miniapp manifest permission surface now includes `invoke:platform-vesting` and `invoke:platform-escrow`, matching the framework's existing guarded write lanes; the wiring regression passes **1/1**. This closes a source-level consumer contract gap, but does not create a deployment or grant either permission in a live host.
- PlatformEscrow now provides the in-repo phase-3 conditional-release slice: native GAS/NEO credit-only funding, `(appId,asset,payer)` credit isolation, tenant/global liability counters, milestone approval/claim/cancel/timeout-recovery exits, pause-immune exits, and a guarded `app.platformEscrow` surface. The focused contract tests pass **6/6**, source/build acceptance is **9/9**, and the framework ABI audit is **17/17**. `milestone-escrow` now has a compatibility route that activates only when the host injects the shared hash; its legacy ABI and live manifest remain unchanged. This is source/build/test evidence only: PlatformEscrow has no deployment record, Registry binding, funded lifecycle, or migration read-back, and no chain write occurred.
- The shared React runtime now derives all platform surfaces from composable `manifest.platformBindings` without consuming the primary `contract` slot, while retaining normalized `contract.mode=shared` fallbacks for legacy manifests. `defineMiniApp` passes Registry, Game, Social, Anchor, DeFi, Vesting, Escrow, and network-indexed Factory bindings through `MiniAppRoot` → `createMiniAppFramework`; explicit definition overrides remain supported. The focused composition regression passes **2/2**, and the full shared gate passes **398/398 files and 4446/4446 tests**. No manifest was switched to a nonexistent deployment, so this proves configuration propagation rather than live binding or migration.
- Fresh 2026-07-24 12:23 UTC verification keeps the source boundary green: PlatformSocial audit **36/36**, cross-repo duplication audit `complete=true` with only the intentionally retained `legacy-clone-contracts` unresolved item, root dependency audits **0 vulnerabilities**, shared tests **398/398 files and 4446/4446 tests**, Admin **55/55 files and 405/405 tests** with typecheck/build pass, and Host Jest **174/174 suites and 1147/1147 tests** with production build pass. The Host Playwright last-run artifact reports `status=passed` and no failed tests. The Social audit now recognizes the composable `platformBindings` wiring instead of requiring the retired single-module string pattern; no chain write occurred.
- Fresh read-only checks at **2026-07-24 12:29–12:30 UTC** remain fail-closed: shared-AA preflight is `phase=upgrade_contracts`, `safe_to_materialize=false`, `next_action=upgrade_aa_then_registry_and_rerun_preflight`; PlatformGame is **0/11** runtime-complete and funded-lifecycle-proven, with **9/11** current live-state rows and **0/5** future candidates runtime-ready. The new shared `platformBindings` source detector is covered by deployment-script regression tests, so future Game/DeFi/Social consumers cannot be silently classified as legacy or unbound; no chain write occurred.
- The shared React runtime now derives all platform surfaces from composable `manifest.platformBindings` without consuming the primary `contract` slot, while retaining normalized `contract.mode=shared` fallbacks for legacy manifests. `defineMiniApp` passes Registry, Game, Social, Anchor, DeFi, Vesting, Escrow, and network-indexed Factory bindings through `MiniAppRoot` → `createMiniAppFramework`; explicit definition overrides remain supported. The focused composition regression passes **2/2**, and the full shared gate passes **398/398 files and 4446/4446 tests**. No manifest was switched to a nonexistent deployment, so this proves configuration propagation rather than live binding or migration.
- The wallet SDK now preserves the same manifest contract model: typed custom `contract.hash` resolves before legacy `contracts`/registry fallbacks, while `platformBindings` is typed as additive framework configuration and never masquerades as the app's primary contract. The focused wallet/config suite passes **13/13**; this closes a local resolution gap without asserting any deployment or live binding.
- The platform-engine base-conformance audit is now reproducible via `npm run -s audit:platform:engine-base`: `PlatformGame` adopts `MiniAppEngineBase` and removes its duplicate `AppKey` kit without changing its existing storage prefixes or ABI; `PlatformVesting` and `PlatformEscrow` already adopt it. `PlatformSocial`, `PlatformDeFi`, and `PlatformAnchor` retain only ABI-compatible wrappers over the shared `MiniAppStorageKeys` algorithm; `PlatformAnchor` and `PlatformSocial` now lock their external transfer paths, and Anchor's new app-scoped credit lane tracks NEO/GAS liabilities by tenant. All six engines pass the source conformance audit with **0** unresolved capabilities; live artifact drift and deployment compatibility remain separate boundaries.
- The cross-repository duplication freshness gate was rerun after the Morpheus sync work changed the measured `apps/shared/services` surface; the regenerated report now passes `check:cross-repo:duplication`, records **12/12** generated engine ports and **0** reviewed handwritten ports, and keeps only the expected `legacy-clone-contracts` unresolved boundary.
- This addendum records no WIF use, no signed transaction, no chain write, and no commit/push. The three worktrees remain mixed; the Morpheus worktree contains pre-existing staged changes.

## Executive status

The three repositories have strong local test baselines, and this pass fixed concrete manifest-safety, locale, stale-routing, deployment-verifier, mobile-E2E, lint, engine-integrity, Factory consumer-routing, production dependency-audit, cross-repository abstract-account byte-order failures, PlatformDeFi cross-tenant credit custody, over-broad shared-module invocation permissions, lazy MiniApp contract-address initialization, and PlatformAnchor app-scoped credit custody. Fresh credential-free, signer-bound testnet reads confirm all 77 directory rows are active, 11 are engine-attached, **0 have shared AA identities materialized**, and **0 have optional deployed AppAccount shims**. The former plan to deploy 77 shims simulated successfully but costs about 794.39 GAS, so the architecture now defaults to one shared UnifiedSmartWallet core with a unique virtual account per app; `AppAccount` remains an opt-in treasury-isolation lane. Registry and AA source/runtime tests prove registrar gating, app-admin ownership, uniqueness, reverse indexing, canonical address derivation, deterministic pre-materialization address prediction, and a timelocked Registry-core disable that preserves existing identities. A fresh reciprocal-state preflight confirms that both live contracts predate the required shared-AA ABI. It also identifies a heightened bootstrap boundary: the live AA core has `update` but no `proposeUpdate`, so the first AA transition is a legacy direct-admin update; the exact candidate simulation HALTs, but no authorization or transaction exists. The tracked historical AA artifact is only a semantic ABI/storage proxy: its checksum does not match the live NEF, exact deployed-source provenance remains unknown, and upgrade compatibility is therefore conditional rather than fully proven. The framework now exposes complete local-ABI surfaces for PlatformRegistry (49 non-control-plane methods plus GAS credit prepayment), PlatformSocial (36 methods plus native GAS/NEO prepayment, including tenant Notary and scoped liability reads), PlatformAnchor (38 methods plus native NEO staking and app-scoped NEO/GAS credit liabilities), PlatformDeFi v1.3 (58 methods plus native NEO/GAS prepaid deposits), and MiniAppFactory (13 methods), plus `app.platformAccount` as the unified directory/shared-AA/optional-treasury snapshot. Each shared surface now requires its own `invoke:platform-*` permission, and `invoke:primary` no longer authorizes writes to shared platform modules. PlatformSocial, PlatformDeFi, and PlatformAnchor direct credits are `(appId,payer)`-scoped with exact memo routing and tenant/global liability accounting; every affected business payout re-checks aggregate direct-credit solvency after assert-wrapped GAS/NEO transfers, and withdrawals remain available while a tenant is paused. SelfLoan is now the named PlatformDeFi first-tenant source migration: profile 1 enforces one active loan and disables liquidation/abandonment, exact artifact/profile attestation fails closed, atomic repay is framework-owned, and the standalone path remains available. TrustAnchor and ProfitAnchor use `app.platformAnchor` for all user reads/writes while preserving durable confirmation recovery; Timestamp Proof has a guarded Notary dual path but retains its legacy zero-GAS anchor because no shared binding exists. All three Factory products route deployment writes through the guarded `app.platformFactory` allowlist. PlatformRegistry remains at zero production bindings because the live artifact exposes only 41/49 tenant methods and has checksum drift; PlatformSocial remains undeployed with zero bindings, PlatformDeFi records zero shared bindings, PlatformAnchor's new app-scoped ABI is local-only with live artifact drift, and the live Factory lacks the new artifact-deployment ABI. The deployed PlatformDeFi stores payer-global credits under the same `0x14/0x15` prefixes that the local tenant-scoped generation rekeys to `(appId,payer)`. Its exact-snapshot recovery bridge remains held from chain use until the public payer arrays/hash, `1.34226336 GAS` deficit resolution, exact artifact simulation, and all payer exits are separately approved and proven; with zero bindings a fresh v1.3 deployment is preferred. These source/API completions are not deployment proof. Configuration and materialization remain closed. PlatformGame remains 11/11 locally routed and 0/11 funded-complete. All 77 MiniApps were rebuilt from the current worktree, synchronized into the Host runtime, and the comprehensive desktop/mobile surface suite passed **186/186**. The broader objective is therefore **partially complete**, not complete: 34 legacy per-app projects remain, live Registry/AA upgrades and 77 shared-account materializations remain undone, SelfLoan still needs a fresh exact PlatformDeFi deployment/profile registration/funded lifecycle before manifest cutover, Factory artifact deployment remains gated, and real-wallet, funded-lifecycle, physical-device, and live-service UX evidence remains incomplete.

## Evidence classification

The new local shared-AA roster preflight derives 77/77 deterministic account IDs with 77/77 unique predictions from the active Registry/app-admin snapshot. This is address-planning evidence only; shared identities remain 0/77 materialized on-chain.

| Objective | Status | Current evidence | Boundary |
| --- | --- | --- | --- |
| Three-repository local quality gates | Verified | Miniapps shared/contracts/integration/fuzz pass; AA repository verifier passes with 416 frontend tests, production build, and 1/1 browser smoke; Morpheus canonical verifier, contracts, shared package, lint, audits, and production build pass | Does not prove live services, physical-device behavior, or chain writes |
| Platform contract library breadth | Partial | Contract suite passes 630 tests, including 6 PlatformSocial credit-isolation/solvency, 4 PlatformDeFi tenant-isolation/solvency, 3 PlatformDeFi legacy-recovery, and 4 Notary TestEngine cases; the generated acceptance ledger reports 9/9 platform contracts and 2/2 Factory templates source/build/test accepted; creator-unique NEF/manifest generation, artifact digests, and exact six-argument calls are covered; live read-only checks identify five deployed contracts and one active Registry artifact; all four drifted artifacts resolve to exact Git revisions | Only 2/9 current testnet platform artifacts match local NEFs; PlatformDeFi has a breaking legacy-credit key-schema migration, the live Factory lacks the six-argument ABI and funded lifecycle certification, Anchor has ABI removals, and PlatformSocial has no deployment record |
| Per-miniapp abstract account | Partial | Registry and UnifiedSmartWallet source/runtime tests prove timelocked reciprocal configuration, registrar-only creation, app-admin ownership, two-app uniqueness, idempotent backfill, reverse indexes, a core-disable rollback, and one SDK/frontend/framework address vector; 77/77 live directory rows are active | Both live contracts lack the reciprocal ABI; the live AA bootstrap update is legacy-direct; 0/77 shared identities and 0/77 optional shims are materialized; no chain write occurred |
| Framework duplication reduction | Partial | 11 manifests use `moduleId: platform-game` and `mode: shared`; all 11 historical engine attachments and 99 descriptor writes are recorded successful; all 11 game runtimes now route through one tested PlatformGame adapter; `app.registry`, `app.platformSocial`, `app.platformAnchor`, `app.platformDeFi`, and `app.platformFactory` cover 49/49, 36/36, 38/38, 58/58, and 13/13 local non-control/tenant ABI methods; each shared surface has a distinct `invoke:platform-*` capability and rejects `invoke:primary` as a substitute; `app.platformAccount` composes directory/shared-AA/optional-treasury state; Social, DeFi, Registry, and Anchor native credit lanes use exact appId-attributed memo semantics; SelfLoan, Timestamp Proof, Trust/Profit Anchor, and all three Factory products have guarded framework routes | The scoped host/client capabilities are not on-chain authorization; contract witness and tenant checks remain authoritative. Registry production bindings remain 0 because the live artifact is only 41/49 tenant-compatible with checksum drift; Jump matches 0/9 and Sheep 3/9 of the new local descriptor values, funded completion is 0/11, PlatformSocial is not deployed or bound, PlatformDeFi has zero live shared bindings and a breaking legacy-credit key migration, PlatformAnchor has live artifact drift and no new app-scoped lane on-chain, Factory artifact deployment remains live-ABI gated, 34 legacy per-app contract projects remain, and the five zero-drain candidates are 0/5 runtime-ready |
| Morpheus private-compute/game runtime | Verified locally | Current worker rerun passes 470 tests; relayer passes 440, control plane 31, scripts 92, runtime matrix 6, shared package 95, contracts 62, lint, audit, and Next production build remain green | Local verification does not prove deployed Nitro/relayer/control-plane health |
| UI/UX quality | Partial | 24 of 24 app test suites, 77 of 77 current-source app builds, host 1,147 tests, current PlayArea/layout/media audits, 52 core E2E flows, and the complete 186-case Host surface suite pass across Desktop Chrome and Pixel 7; all 77 MiniApps render in both viewport classes without captured runtime errors, mutating requests, broken internal links, image failures, or horizontal overflow | The surface suite uses deterministic stubs for volatile read-only dependencies and does not prove physical-device interaction, real-wallet confirmation, live external-service health, or funded business lifecycles |
| NEO N3 testnet deployment | Verified read-only + dry-run | Fresh Registry verification confirms network magic, admin, active artifact, engine row, pause/fee/liability state, and 77/77 directory rows; four exact update/schedule simulations HALT under their live public admin identities | No chain write occurred; two distinct admin domains require separate signer authorization, Factory still requires live ABI/governed-artifact/funded-lifecycle certification, Anchor retains an ABI gate, and descriptor/funding after a simulated attachment remain unverified because independent RPC simulations do not persist state |

## Repository state

### neo-os-web

- Branch: `master` tracking `origin/master`.
- Current `HEAD` is `031f3f9a2`; the contract-address initialization, DataFeed fuzz/config, and this report remain uncommitted. Concurrent `zhuada-e`/GDD changes are user-owned and excluded from this refactor lane.
- This pass updates production-safety tests, the generated contract registry, one host regression test, the locale catalog, PlatformSocial payout-solvency enforcement, current platform audit reports, current PlayArea reports, and this joint progress report.
- Current inventory: 77 `neo-manifest.json` files, 11 `platform-game` modules, 11 shared-mode manifests, 64 manifests retaining a `contracts` field, 34 legacy per-app `MiniApp*` contract projects, and one shared `MiniApp.DevPack` source library. The active contract build and selected deployment helper now exclude the 11 shared-game clones by default; `BUILD_LEGACY_CLONES=1` and `MINIAPP_DEPLOY_INCLUDE_LEGACY_CLONES=1` are separate explicit recovery/test opt-ins, with no legacy manifest writeback.
- The Phase 0–2 implementation plan still contains unchecked tasks. It is planning/history, not completion proof.

### neo-abstract-account

- Branch: `main` tracking `origin/main`.
- Worktree now contains the focused platform-registrar partial, mock fixture, runtime tests, SDK/frontend byte-order correction, and documentation update; it is not committed.
- The current AA gate passes **219/219** contract tests, **416/416** frontend tests, and **79/79** SDK tests; AA-01–06, AA-09, AA-12–15, AA-18, and AA-19 are fixed in source/documentation, the tracked `contracts/build` manifests/NEFs are refreshed, and deployment remains pending. Stable V2 platform-account methods preserve legacy derivation ABI while making Registry predictions independent of future app-admin rotation; AA-07/08/10/11/16 are source/test verified, AA-19 keeps legacy read/delete fallback for the pre-renumbered layout, and AA-17 is safe only while raw forwarding remains disabled.
- Previously required `scripts/dotnet_env.sh` and `shared/registrationAccountId.mjs` remain tracked, and no newline-junk paths remain.

### neo-morpheus-oracle

- Branch: `chore/solc-0.8.35-upgrade` with a large pre-existing staged worktree.
- This pass also adds a 63-byte DataFeed storage-key guard, regenerates its test artifact, and adds a TestEngine regression; the pre-existing staged refactor remains user-owned and was neither reset nor committed by this pass.
- The existing staged refactor remains user-owned and was neither reset nor committed by this pass.

## Contract migration ledger

- **Attached and manifest-bound to `platform-game` on testnet (11), funded completion 0/11:** `miniapp-aim-master`, `miniapp-color-clash`, `miniapp-curve-arrow`, `miniapp-flappy-dash`, `miniapp-game-2048`, `miniapp-jump-rush`, `miniapp-merge-kingdom`, `miniapp-pet-potion`, `miniapp-sheep-solitaire`, `miniapp-snake-bounty`, and `miniapp-sudoku`. Registry evidence proves 11 historical engine attachments and 99 descriptor writes, but exact target comparison shows Jump matches 0/9 and Sheep 3/9 of the new local descriptors. All 11 runtimes now inherit the shared adapter from their manifest binding; tests lock appId-first calls, prepaid-only start, Finalizing-to-Solved polling, tenant event isolation, and restart recovery. The Sheep wrapper change produces a new local engine hash and therefore requires a reviewed runtime update before live use. All 11 still lack a retained funded lifecycle proof and live shared-AA identity.
- **Next zero-drain candidates (5), 0/5 ready:** `miniapp-arrow-escape`, `miniapp-bead-workshop`, `miniapp-fruit-funnel`, `miniapp-screw-sort`, and `miniapp-zhuada-e`. They have no standalone contract hash to drain, but none has a reviewed descriptor set, shared binding, engine attachment, Morpheus wrapper, or `app.platformGame` runtime adapter. They must remain guest-only until those gates and a funded lifecycle proof exist.
- **Deliberately standalone pending product decision:** the healthy wager contracts are not forced into the engine, and `MiniAppCredits` remains a separate deploy-versus-fold decision. This is an explicit policy boundary, not migration completion.
- **Finance/Social cohorts:** remain unproven beyond source/tests; each needs a named first tenant, deployed engine row, ABI-fidelity check, money-path invariant suite, and live rollback/drain proof before manifest rebinding.
- **Deletion gate:** the 34 legacy per-app projects are retained. Even for the 11 rebound games, witness-gated exits and historical pool recovery must remain available until the drain/expiry/rollback ledger proves deletion safe. Their standalone build/deployment path is now recovery-only and explicitly gated.

## Fixes made in this pass

### Miniapps

- Added caller-`appId` fallback to wallet contract resolution, exposed `resolveContractAddress()` through `ChainService` and `app.chain`, and pre-resolved contract bindings before MiniApp setup. Dev Tipping now performs the same strict network/hash/ABI attestation against a resolved address instead of treating the framework's initial `null` observable as a binding mismatch.
- Rebuilt and synchronized all 77 MiniApp runtime bundles from the current worktree. The standard builder passed 76/76; `zhuada-e` used its existing asset/type/Vite gates without invoking the source-generating `prebuild`, preserving concurrent user files.
- Restored `docs/superpowers/` as versioned architecture authority so the current cross-repository design and Phase 0–5 runbooks are no longer hidden by a broad ignore rule.
- Removed five tracked `.workbuddy/` memory/output files from the Git index while preserving their ignored local copies, and added a repository-hygiene gate that rejects tracked-ignored files, hidden canonical documents, or exposed local agent state.
- Replaced the stale hand-maintained cross-repository duplication snapshot with a reproducible audit/check command. The current evidence records 11 retained clone contracts at 8,914 logical LOC, 12 Morpheus engine sources plus 5 enclave adapters, a 12-generated/0-reviewed-handwritten split, and a canonical Morpheus confidential-envelope source generated into the AA browser artifact with source-hash and exact-suffix drift checks. The active contract build and selected deployment helper exclude those shared-game clones by default and keep separate explicit no-writeback recovery lanes; the Morpheus sibling worktree remains uncommitted.
- Added `app.badge` as the complete platform badge facade with capability-safe fallbacks, migrated AA Account Lab and Soulbound Certificate to `app.storage.remote`, and migrated Soulbound Certificate and Flashloan to `app.badge`. Direct production-code `ctx.os.*` calls are now 0; the remaining 12 text matches are comments/tests and host compatibility remains intentionally available.
- Updated the SelfLoan production static gate to verify the current tracked-run helper (`persistBroadcast` in the transaction-sent callback plus returned-txid fallback) instead of requiring the retired inline callback syntax; focused SelfLoan tests and the complete shared suite now pass.
- Updated Aim Master, Color Clash, and Curve Arrow production-safety tests to assert the shared registry/engine architecture instead of obsolete standalone contract hashes or an empty `contracts` object.
- Regenerated the canonical MiniApp contract registry by removing ten obsolete testnet standalone hashes left behind by the shared-game migration.
- Updated the host PlayArea regression test to assert that shared Game 2048 has no standalone contract route while remaining a local guest experience.
- Removed the duplicate `sessionAccountIdle` locale key from AA Session Key Lab.
- Decoupled read-only Registry verification and dry-run cohort reconciliation from private WIF loading by accepting a public address/hash and using an invoker or watch-only actor.
- Preserved fail-closed write controls: non-dry-run actions still require both a WIF and the exact chain-write confirmation phrase.
- Fixed the full-loop dry-run false negative: descriptor and funding steps now report that they are planned but unverified after simulated registration, rather than assuming independent RPC simulations share state.
- Added explicit cohort metrics for active rows, engine attachments, and materialized AppAccounts so directory registration cannot be misreported as per-app account completion.
- Corrected the platform-admin mint economics: the administrator lane no longer plans an unnecessary 10 GAS prepaid-credit transfer when `MintAccount` is fee-exempt.
- Added a dry-run-first `materialize-accounts` cohort action that requires the platform admin, simulates every mint, records per-app predicted hashes and system fees, rejects duplicate hashes, and verifies both Registry indexes after any separately approved write.
- Rejected that expensive fleet-wide shim lane as the default after its 77-app dry-run estimated `771.29012379` GAS system fee plus `23.10000000` GAS network fees.
- Added a timelocked `abstractAccountCore` to PlatformRegistry, automatic shared-AA creation during registration, idempotent `materializeAbstractAccount` backfill, and core-scoped reverse indexing.
- PlatformRegistry now fail-closes both `proposeAbstractAccountCore` and `setAbstractAccountCore` unless the candidate AA core's `getPlatformRegistrar()` reciprocally points to the Registry, preventing a one-sided activation during or after the Registry timelock; the runtime regression is covered in `PlatformRegistryAbstractAccountTests`.
- Added a seven-day-timelocked platform registrar to UnifiedSmartWallet. Only the configured Registry may create platform accounts, while each app admin remains backup owner and verifier/hook authority. Matured Registry app-admin rotation now calls the registrar-only `rotatePlatformAccountOwner` lane against the same stored `(Registry, appId)` binding, preserving the virtual account id while synchronizing the new backup owner; active market escrow and escape state fail closed.
- Updated the platform TestEngine counterparty to mirror the UnifiedSmartWallet registration domain, Hash160 byte order, zero-plugin fields, and uint32 little-endian timelock encoding; the Registry runtime suite now verifies the full Registry-to-AA binding order.
- Added `framework.registry.getAbstractAccount`, reverse lookup, deterministic verification-script/address derivation, and a byte-exact vector shared by the miniapps framework, AA SDK, and AA frontend. This also fixed two frontend callers and the SDK that previously mixed display and NeoVM byte order.
- Added `materialize-abstract-accounts` to the cohort tool. It is dry-run-first, platform-admin gated, uniqueness/reverse-index checked, and fails explicitly with “PlatformRegistry upgrade required” against the current testnet ABI.
- Added a generated joint Registry/AA ABI audit with fail-closed tests and retained JSON/Markdown reports.
- Added a credential-free shared-AA upgrade preflight that reads both live manifests plus current/pending/ETA state, classifies AA upgrade governance, simulates the exact local AA update under the live public admin identity, enforces “AA registrar first, Registry core second,” and emits exactly one safe next action without signing or broadcasting.
- Added `app.platformSocial`, a typed shared-engine surface covering envelope, bounded range-pool, heritage trust, vault commit/reveal, immutable tenant Notary, and `(appId,payer)` GAS/NEO credit lifecycle. It auto-threads `appId`, emits exact native prepayment memos, resolves the configured shared hash from `ContractBinding`, defaults account arguments to the connected wallet, retains transaction-sent/event-wait recovery hooks, and centralizes guest/permission guards. Timestamp Proof now has a durable Notary/legacy dual path. A generated 36/36 ABI ledger plus one native-transfer lane fails closed on missing/extra operations and records zero production bindings while retaining the current `no-deployment-record` boundary.
- Hardened every PlatformSocial business payout against direct-credit under-collateralization: Envelope and RangePool GAS exits, Trust NEO exits, and Vault GAS exits re-check the global liability after transfer; Trust/Vault fee transfers are now assert-wrapped; TestEngine cases prove one tenant's payout/cancellation leaves another tenant's GAS/NEO credit fully covered.
- Added `app.platformAnchor`, covering all 32 local tenant ABI methods plus the native NEO `stake:<appId>` deposit lane. TrustAnchor and ProfitAnchor now route every user read/write through it without weakening pending-transaction durability or exact event/readback confirmation; the generated ledger retains the live-artifact-drift boundary and performs no chain write.
- Added `app.platformDeFi`, covering all 49 local tenant ABI methods plus native NEO/GAS prepaid deposits, transaction-sent recovery hooks, appId/hash binding, wallet defaults, and centralized write guards. The generated ledger records zero shared bindings and blocks any claim that the incompatible standalone SelfLoan/FlashLoan/TimeCapsule apps were migrated.
- Added `app.platformFactory`, covering all 13 non-admin MiniAppFactory methods with per-network hashes and a strict three-operation deployment-call allowlist. Asset Factory, NFT Factory, shared Factory runtime, and MiniApp Factory's durable registration path now route deployment writes through framework guards; direct RPC remains read-only for contract-state and signer-aware fee probes.
- Expanded `app.registry` from a read-only directory helper to all 49 local non-control-plane Registry methods plus native GAS credit prepayment. Self-registration, shared-AA materialization, deterministic shared-AA prediction, descriptors, app governance, credit recovery, and role-bound treasury exits now share one guarded surface; platform-admin control methods stay excluded. The generated ledger records 0 production bindings because the live Registry is only 41/49 tenant-compatible and checksum-drifted.
- Replaced the shared modules' over-broad `invoke:primary` write gate with six module-specific capabilities: `invoke:platform-registry`, `invoke:platform-game`, `invoke:platform-social`, `invoke:platform-anchor`, `invoke:platform-defi`, and `invoke:platform-factory`. Seven production adopters now declare only the narrow capability they require in both manifest sources, shared PlatformGame apps remain free of `invoke:primary`, and generated interface auditors fail closed if a surface regresses to the broad policy.
- Added a 24-hour-timelocked Registry core disable. It preserves existing app identity indexes while returning future registrations to directory-only mode, providing a rollback before any AA registrar rotation.
- Added Pixel 7 to the default host Playwright gate and made the shared-runtime wallet/invoke scenario use the real mobile action sheet instead of asserting against the hidden desktop rail.
- Expanded the platform-contract presence gate from the legacy four-contract set to all seven current platform contracts.
- Fixed PlatformRegistry registration observability: `RegisterApp` now emits the generated shared abstract-account hash in `AppRegistered` instead of the stale zero placeholder; the TestEngine regression compares the event identity with the directory identity.
- Added a generated platform-contract acceptance ledger covering ABI size, permissions, money callback, update path, source conventions, test evidence, and deployment-evidence class; wired its staleness check into `verify:repo`.
- Added a generated PlatformGame migration-completeness ledger and repository staleness gate that separates on-chain attachment, shared manifest binding, exact descriptor-value parity, framework routing, direct legacy calls, and funded lifecycle proof. It reports 11/11 locally routed through the shared adapter, 0/11 retaining direct legacy reward calls, Jump at 0/9 and Sheep at 3/9 descriptor parity, 0/11 funded-complete, and 0/5 zero-drain candidates ready.
- Migrated Jump Rush and Sheep Solitaire from pre-kernel bind/signature settlement to `app.game.reward`; added cached live-view/op-count recovery to the generic Morpheus session start response, authoritative Sheep board/tray views, and undo replay that preserves accepted tool operations.
- Added the framework PlatformGame reward adapter and automatic shared-manifest injection. The adapter preserves the existing `app.game.reward` API while enforcing prepaid-only starts, appId-first shared calls, Finalizing polling, tenant-filtered events, shared stats, and engine-hash-bound recovery; custom contracts remain on the legacy backend.
- Added a credential-free live testnet artifact verifier that rejects wrong-network RPC endpoints, derives targets from retained reports and consumer manifests, compares local/on-chain NEF checksums and ABI method sets, and distinguishes deployed contracts, the Registry-hosted AppAccount artifact, and an undeployed contract.
- Migrated shared Asset/NFT Factory planning from the legacy `deployFromTemplate` target to `deployArtifactFromTemplate`; added governed NEP-17 and NEP-11 template contracts, deterministic creator-unique manifest generation, complete six-argument calls, artifact-bound digests, checksum-valid address handling, and exact live-ABI probing. App-owned production gates still keep execution, fee estimation, and broadcast closed until the deployed Factory, governed artifacts, and recovery lifecycle are certified.
- Added a generated upgrade-readiness ledger that resolves deployed NEF checksums to exact Git commits, compares ABI methods, storage-prefix bytes, and serialized record layouts, records intentional semantic changes, and defines an ordered update-or-hold plan.
- Hardened the platform updater so credential-free dry-runs use a public watch-only identity, Registry is routed to `scheduleUpdate` instead of unsafe direct update, MiniAppFactory is included, local/on-chain checksums are pinned, and post-write checksum mismatch would fail closed.
- Re-audited the historical miniapps findings against the current source and TestEngine evidence: findings H1/H2/H3, the threshold/pause/env guards, RewardGame callback/event/asset/body-limit fixes are now marked `fixed_in_source_verified`; the escape comment remains documentation-only, scope-less keys remain a production-mitigated compatibility boundary, and permissionless appId claiming remains an open design risk.

- Restored the DataFeed cross-repository fuzz lane to the current `getLatest` ABI and canonical testnet hash. The live contract currently faults on 64- and 256-byte resource IDs at the NeoVM storage-key boundary; the local MorpheusDataFeed now returns a default six-field record for oversized reads and rejects oversized writes before storage access.

### Morpheus

- Removed 11 unused imports/destructured values left by the fulfillment and game-wrapper splits, restoring zero-warning lint.
- Updated Sheep Solitaire and Jump Rush engine SHA-256 pins to match the verified current wrapper bytes.
- Overrode vulnerable `fast-uri` releases with `3.1.4` and `sharp` with `0.35.3` in both root and web-package audit scopes.
- Regenerated affected lock resolution; production audits now report zero vulnerabilities.

## Validation ledger

| Repository | Command/scope | Result |
| --- | --- | --- |
| Miniapps | All shared production-safety tests | 45 passed |
| Miniapps | Full miniapp aggregate + shared Vitest suite | All 24 per-miniapp suites passed; the current shared rerun passes 397 files and 4,441/4,441 tests, including scoped platform-module manifest coverage, framework badge consumers, and tracked SelfLoan transaction recovery guard |
| Miniapps | AA Session Key Lab targeted suite | 25 passed |
| Miniapps | AA Session Key Lab production build | Passed; upstream Sass deprecation warnings only |
| Miniapps | Framework Vitest suite + typecheck | 616 passed across 49 files and `tsc --noEmit` passed, including complete PlatformRegistry 49/49, PlatformSocial 36/36, PlatformAnchor 38/38, PlatformDeFi 58/58, MiniAppFactory 13/13, six distinct shared-module permissions, unified `app.platformAccount`, the capability-safe `app.badge` surface, and explicit contract-address resolution |
| Miniapps | Contract-address initialization regression | 74/74 focused shared/framework/Dev Tipping tests passed; Host typecheck and Dev Tipping production build passed; targeted desktop/mobile surface passed 2/2 |
| Host App | Complete current-source surface validation | Host production build passed after synchronizing 77 runtime bundles; Playwright passed 186/186 across Desktop Chrome and Pixel 7 |
| Miniapps | Deploy-script Node suite | 286 passed; AA Relay Console static coverage now follows the canonical `safeReadStorage`/`safeWriteStorage`/`safeRemoveStorage` helpers |
| Miniapps | Repository structure hygiene | 3 focused tests passed; all canonical `docs/superpowers` specifications/plans are versionable, no tracked file matches `.gitignore`, and `.workbuddy`/`.superpowers` runtime state remains local-only |
| Three-repo duplication census | `audit:cross-repo:duplication` and freshness check pass; all 12 Morpheus engines now generate from explicit miniapp TypeScript sources, leaving one residual class: retained legacy clone contracts; the 11 shared-game clones are excluded from both active build and selected deployment paths; AA now consumes a generated artifact from the Morpheus canonical envelope source, and direct production `ctx.os` calls are 0 |
| Morpheus engine single-source pipeline | 3/3 passed: generated engines match a fresh miniapps TypeScript sync, generated `sudoku-data.js` is semantically identical to its TS twin, and all checked-in behavioral parity goldens reproduce; sibling changes remain dirty/uncommitted |
| AA Account Lab, Soulbound Certificate, Flashloan framework migration | 10 focused files / 173 tests passed using the canonical `apps/shared` Vitest entrypoint; an earlier root-directory direct Vitest attempt failed before collection because it omitted the shared alias config and is not counted as a product failure |
| Miniapps | PlatformRegistry contract/framework interface | 49/49 non-control-plane ABI methods plus native GAS credit prepayment covered; generated freshness check passes; live availability is 41/49 with checksum drift, bindings remain 0, and no chain write occurred |
| Miniapps | PlatformSocial contract/framework interface | 36/36 user-facing ABI methods covered plus one guarded native-transfer lane, including Notary and tenant/global credit liabilities; generated JSON/Markdown freshness check passes; Timestamp Proof dual-path audit passes; live status remains `no-deployment-record`, configured consumers are 0, and no app binding changed |
| Miniapps | PlatformAnchor contract/framework interface | 38/38 tenant ABI methods plus native NEO staking and app-scoped NEO/GAS credit liabilities covered; TrustAnchor/ProfitAnchor user runtimes migrated; focused Anchor boundary tests pass 12/12 and framework interface freshness passes; live status remains `live-artifact-drift` and no chain write occurred |
| Miniapps | PlatformDeFi contract/framework interface | 58/58 tenant ABI methods plus exact `${appId}:credit` NEO/GAS deposits covered; SelfLoan profile/single-position/forced-exit and fee-liquidity invariants plus generated JSON/Markdown freshness checks pass; bindings remain 0, live status remains `live-artifact-drift`, and no chain write occurred |
| Miniapps | MiniAppFactory contract/framework interface | 13/13 non-admin ABI methods covered; three Factory products configured; both shared and durable registration writes use the guarded allowlist; 106 focused Factory tests and all three production builds pass; live artifact deployment remains gated and no chain write occurred |
| Miniapps | Shared-AA framework vector + Registry surfaces | 27 focused tests passed |
| Miniapps | PlatformRegistry shared-AA TestEngine runtime | 6 focused tests passed, including exact Registry-to-AA binding order, stable account identity during owner rotation, and timelocked disable with identity retention |
| Miniapps | Cohort tool single-file Go compile + security invariants | Passed; 8 security tests passed |
| Miniapps | Joint Registry/AA ABI audit | Passed; 2 fail-closed interface tests passed |
| Miniapps | Shared-AA upgrade state/compatibility machine + live preflight | 7 tests passed; live phase is `upgrade_contracts`, AA route is `legacy_direct`, compatibility is conditional because exact deployed-source provenance is unknown, the exact admin simulation HALTs at 32,763,615 datoshi, the non-admin simulation FAULTs, safe-to-materialize is false, and no write occurred |
| Abstract Account | UnifiedSmartWallet + registrar mock nccs builds | Passed with zero warnings in the focused builds |
| Abstract Account | Platform registrar TestEngine runtime | 4 focused tests passed |
| Abstract Account | SDK unit suite after canonical byte-order fix | 79 passed |
| Abstract Account | Frontend unit suite after canonical byte-order fix | 416 passed |
| Miniapps | Platform contract acceptance ledger | 7/7 platform contracts and 2/2 Factory templates source/build/test accepted; generated artifact hashes fresh; 4/7 retained platform deployment reports, 2/7 partial operational evidence, 1/7 no deployment record |
| Miniapps | Platform contract live testnet ledger | 5 deployed contracts found plus 1 active Registry artifact; 2/7 local artifacts match, 4 drift, 1 has no deployment record |
| Miniapps | Platform upgrade-readiness ledger | 4/4 drifted artifacts resolved to Git; 4/4 serialized record layouts unchanged; 0 storage-prefix values changed; 3 additive/equal ABIs; 1 ABI-breaking contract; 1 breaking storage-key schema (PlatformDeFi `0x14/0x15`); the live DeFi scan finds 3 legacy GAS-credit rows totaling 1,600,000,002 datoshi against 1,465,773,666 datoshi native GAS, a 134,226,336 datoshi deficit; only Registry remains a direct staged candidate |
| Miniapps | PlatformGame migration-completeness ledger | 11/11 attached and shared-bound; 11/11 route through the framework adapter and 0/11 retain direct legacy reward calls; exact descriptor parity is 9/9 for nine apps, 0/9 for Jump, and 3/9 for Sheep; 0/11 have funded lifecycle proof; zero-drain candidates 0/5 ready |
| Miniapps | Nine framework-routed game builds | Aim Master, Color Clash, Curve Arrow, Flappy Dash, Game 2048, Merge Kingdom, Pet Potion, Snake Bounty, and Sudoku passed; chunk-size warnings only |
| Miniapps | Exact update preflights | 4/4 HALT under two public admin identities, with exact candidate NEF/manifest payloads and 0 transactions broadcast |
| Miniapps | Host Jest suite | 174 suites, 1,147 tests passed; existing expected console noise remains |
| Miniapps | Per-app test runner | 24 of 24 suites passed |
| Miniapps | Full MiniApp build matrix | 77 built, 0 failures |
| Miniapps | Host production build + core desktop/mobile E2E | Build passed; 52 of 52 homepage/navigation/platform/list/detail/wallet flows passed across Desktop Chrome and Pixel 7 with WIF disabled |
| Miniapps | Contract xUnit suite | 610 passed, including the Registry `AppRegistered` shared-account identity, Registry-to-AA binding, and app-admin owner-rotation regressions |
| Miniapps | Current RewardGame regression | 21 passed, including pool-funding memo routing, callback module/requester binding, late-expiry idempotence, and truthful `Solved` events |
| Miniapps | Edge shared guards/body cap | 21 Deno tests passed with `--allow-env`, covering production-default environment, admin metadata, API-key scopes, and streamed request-size enforcement |
| Miniapps | Cross-repository integration | 33 passed, including live read-only testnet hash/ABI checks and canonical MorpheusDataFeed resolution |
| Miniapps | Cross-repository fuzz | 410 iterations passed with 0 runner errors; 20 repeated findings show the current deployed DataFeed faults on oversized resource IDs, while the local guard is covered by Morpheus VM tests and is not yet deployed |
| Abstract Account | Contract-only verification plus prior broader verifier | Current contracts 218 passed after retaining the legacy two-argument `finalizeEscape` overload; frontend 416 tests, production build, audit, and browser smoke 1/1 passed |
| Abstract Account | Historical finding re-audit | AA-01–06, AA-09, AA-12–15, AA-18, and AA-19 fixed in source/documentation with tracked bundle refreshed, pending deployment; AA-07/08/10/11/16 verified; AA-19 retains legacy prefix fallback; AA-17 default-off mitigation |
| Morpheus | Contracts | 62 passed, including oversized DataFeed read/write boundary regression |
| Morpheus | Shared package | 95 passed |
| Morpheus | Worker/relayer/control/scripts local gates | Current worker 470, relayer 440, control plane 31, scripts 92, and runtime matrix 6; web/content consistency, lint/types, and Next build remain separately recorded as passing |
| Morpheus | Root production audit gate | 0 vulnerabilities |
| Morpheus | Web package audit | 0 vulnerabilities |

Current static/product audits also pass: 77 active apps have complete live-harness classifications, 77 have no catalog-level PlayArea gaps, the layout audit reports no failures, the media audit reports no failures across 77 manifests and 16 definitions, and the unified-layer audit reports 0 errors, 0 warnings, and 0 informational findings. The opaque-origin `forever-album` host storage bridge now uses the shared `apps/shared/utils/embedded-storage-client.ts` protocol client; its app-specific adapter retains only the legacy key and album serialization rules, and its wire protocol remains parity-tested against the host bridge. The refreshed static runtime UI audit covers all 77 apps at desktop and mobile viewports (`154/154` checks), captures 154 PNG screenshots, and reports 0 failures and 0 warnings; the audit now derives its catalog size, accepts hidden custom form inputs correctly, and writes a clean Markdown report.

The generated acceptance artifacts are `docs/reports/platform-contract-acceptance-latest.json` and `docs/reports/platform-contract-acceptance-latest.md`; the read-only chain snapshot is retained in `docs/reports/platform-contract-testnet-live-latest.json` and `docs/reports/platform-contract-testnet-live-latest.md`; artifact provenance and upgrade gates are retained in `docs/reports/platform-contract-upgrade-readiness-latest.json` and `docs/reports/platform-contract-upgrade-readiness-latest.md`. Source/build/test acceptance is 7/7, but current testnet equality is only 2/7: PlatformGame and the active AppAccount artifact match local NEFs; PlatformRegistry, MiniAppFactory, PlatformAnchor, and PlatformDeFi have checksum drift; PlatformSocial has no deployment record. Retained deployment reports remain historical evidence and are not treated as current bytecode proof.

The repository-wide lint command remains red on pre-existing debt: 2,156 errors and 292 warnings across unrelated legacy files. Linting the files changed in this pass reports no errors; the generated registry retains its generator-emitted unused-disable warning.

One earlier full Miniapps shared-suite run observed a transient public-RPC timeout, and the affected real-chain file passed immediately in isolation. A later default-concurrency rerun at 07:24 exposed timeout amplification in dynamic imports, JSDOM rendering, and deterministic board checks; explicit 15-second bounds were added only to those tests, with no assertion changes. The fresh complete run now passes **397/397 files and 4,441/4,441 tests**. This closes the local shared-suite reliability regression while retaining the earlier external-RPC variance as historical evidence.

## Testnet evidence boundary

Fresh 2026-07-23 evidence was generated under `/tmp` so repository deployment history was not mutated:

- `/tmp/platform-registry-verify-2026-07-23.json` is a signer-bound, read-only verification of network magic `894710606` and Registry `0x5ec036efaa1fbde3ff7d1587d790768bc098cb2b`. The Registry admin matches the supplied public identity; AppAccount artifact version `1` and checksum `2055764547` match the local NEF; `platform-game` is active at `0xc75b181b4561462903bb27d8d9e0b32b637bec12`; global pause is false; and that capture observed no pending timelock at its capture time.
- Dry-runs for `deploy`, `execute-timelocks`, and `wire-engine` completed without a WIF or chain-write confirmation. The latest Registry update preflight uses the current local NEF checksum `579281221`, confirms the deployed hash and admin, and HALTs through `scheduleUpdate` with zero transactions. It is an update candidate, not proof that the new bytecode is live.
- `/tmp/platform-registry-full-loop-dry-run-2026-07-23.json` confirms the artifact/engine gates, existing engine binding, GAS-credit simulation, `mintAccount` eligibility, and `attachEngine` eligibility. Descriptor and funding are explicitly skipped as planned-after-registration because separate RPC dry-runs do not persist the simulated attachment; those two steps are not claimed verified.
- `/tmp/cohort0-registration-testnet-2026-07-23.json` reconciles all 77 current manifests: 77/77 rows registered and active, 11/77 engine-attached, 0/77 materialized AppAccounts, zero failed probes, and zero planned registrations.
- `/tmp/cohort0-account-materialization-testnet-2026-07-23.json` simulates `mintAccount` for all 77 active rows under the public platform-admin identity. All 77 simulations HALT, produce 77 distinct predicted account hashes with zero collisions, and estimate `771.29012379` GAS total system fee plus approximately `23.10000000` GAS network fees. This proves current eligibility and deterministic address uniqueness for the reviewed signer/artifact state; it does not prove materialization because no transaction was broadcast.
- A new credential-free `materialize-abstract-accounts` dry-run against the same Registry stops before planning with `PlatformRegistry upgrade required ... method not found: abstractAccountCore/0` and persists the current blocked evidence at `deploy/config/cohort0-abstract-account-materialization-testnet-2026-07-23.json` (with the normalized mirror at `docs/reports/shared-aa-materialization-public-dry-run-latest.json`); it records `77` pending roster rows, `transactions=[]`, and `chain_writes_performed=false`. This is the expected fail-closed boundary: the operational tool is ready, but the deployed Registry cannot create shared identities until a reviewed upgrade and reciprocal AA registrar configuration complete.
- `docs/reports/shared-aa-upgrade-preflight-latest.json` reads both live contracts and now reports phase `upgrade_contracts`: the live Registry lacks nine shared-AA management/read methods and the live AA core lacks eleven platform-registrar/account methods, including stable account derivation/registration. The AA core is update counter `4`, checksum `1086937376`, and exposes `update` without `proposeUpdate`; an exact candidate update simulation under its public admin identity HALTs at `34102221` datoshi, while a non-admin simulation FAULTs with `Not admin`. The tracked historical manifest matches the live method/event/permission/trust surface. The candidate adds the new governance/registrar/binding prefixes, moves `VerifyScopeTarget` from `0x12` to `0x1C`, and proves a `LegacyStoragePrefix12` read fallback, active `0x1C` write, and legacy-key cleanup; stored-record layouts are unchanged. Its only live-ABI removal is the intentionally eliminated instant `transferAdmin(Hash160)` path. The historical NEF checksum is `2765313802`, so it remains a semantic proxy rather than exact deployed-source provenance; compatibility is now conditional on the recorded ABI plus the explicit legacy-prefix migration proof. This proves the current direct bootstrap remains admin-gated, not that it is authorized or fully provenance-reconstructed. The prescribed order is this separately reviewed AA bootstrap, Registry upgrade with core disabled, registrar confirmation, Registry-core activation, then the 77-app dry-run.
- `/Users/jinghuiliao/git/r3e/neo-abstract-account/scripts/upgrade_testnet_unified_smart_wallet.js` is now a testnet-only, dry-run-first helper for that first legacy-direct AA bootstrap. It loads the local `UnifiedSmartWalletV3` artifact, requires an explicit public signer for simulation, fail-closes unless the exact update HALTs, and requires separate confirmation plus a configured WIF before broadcast. Its four offline tests pass; no WIF was read and no transaction was signed or broadcast.
- The first live read-only run of that helper under the retained public AA admin identity reached testnet magic `894710606`, simulated the exact candidate `update` as `HALT` with `34102221` datoshi, and recorded candidate SHA-256 values `0x896abf80e810b35012f0ef2f560a77872ab5d880cf6e13add4b91885d1312c22` (NEF) and `0x228395c90076483411e26b55d70cac5d83b861782db2bd99aa96a07bba328ca4` (manifest). The live core remains update counter `4`, checksum `1086937376`, with zero of nine registrar methods; the credential-free result is retained at `/Users/jinghuiliao/git/r3e/neo-abstract-account/docs/reports/testnet-unified-smart-wallet-upgrade-latest.json` and does not authorize execution.
- A fresh read-only mainnet method verification selected `https://mainnet2.neo.coz.io:443`, covered **77** miniapps and **29** unique deployed contracts, attempted **646/646** safe reads, and found **61** blockers: **53** live ABI drifts against canonical platform/AA/Morpheus manifests and **8** contracts whose canonical build artifacts are still missing. The regenerated goal report preserves this as `contracts.mainnet-readiness=fail`; it is not converted into a pass by the zero `contractStateErrors` count.
- `docs/reports/platform-contract-testnet-live-latest.json` is the retained credential-free artifact snapshot and now records machine-readable `read_only=true` and `chain_writes_performed=false`. PlatformGame checksum `2377918952` and AppAccount artifact checksum `2055764547` match local builds. PlatformRegistry is missing 11 local ABI methods (8 from the 48-method tenant surface), MiniAppFactory is missing `deployArtifactFromTemplate`, PlatformDeFi is missing 44 local ABI methods, and PlatformAnchor has checksum drift despite retaining every current local method. PlatformSocial remains undeployed. These are deployment gaps, not source-test failures.
- `docs/reports/platform-defi-legacy-credit-snapshot-latest.json` is a credential-free `findstorage` and native-balance snapshot at RPC block count `17924262`. Legacy NEO prefix `0x14` is empty. Legacy GAS prefix `0x15` contains 3 payer-global rows totaling `1600000002` datoshi (`16.00000002 GAS`), while the contract holds `1465773666` datoshi (`14.65773666 GAS`), leaving a `-134226336` datoshi (`-1.34226336 GAS`) backing gap. This is current read-only evidence that the v1.2 recovery bridge requires a separately approved deficit top-up before activation; no transaction was signed or broadcast.
- `docs/reports/platform-contract-upgrade-readiness-latest.json` resolves PlatformRegistry to `960d43f67e55`, MiniAppFactory to `16e6fa6abb6e`, PlatformAnchor to `15873071cf47`, and PlatformDeFi to `5e4d20cdc65f`. No serialized record layout changed and no existing prefix byte was reassigned, but the ledger no longer mistakes byte-prefix stability for storage compatibility: deployed DeFi stores payer-global credit at `0x14/0x15`, while v1.2 stores `(appId,payer)` at those prefixes and introduces liability counters. The local candidate now has a tested auto-paused recovery bridge, but PlatformDeFi remains gated on exact snapshot/hash reconciliation, deficit authorization, exact update simulation, and all payer withdrawals; a fresh deployment is still preferred while bindings are zero. Factory creator-artifact generation and consumer cutover are complete locally, but live ABI, governed-artifact registration, and funded lifecycle certification remain required; Anchor is held for an explicit `setAgentAccounts`/`setAgentWeight` ABI-deprecation decision.
- `docs/reports/platform-update-registry-preflight-latest.json` and `docs/reports/platform-update-shared-admin-preflight-latest.json` retain exact candidate simulations. Registry `scheduleUpdate` consumes `560736` datoshi in test invocation; Factory, Anchor, and DeFi direct update simulations consume `7818730`, `14953972`, and `29132042` respectively. All four HALT, all admin checks match, and no transaction was signed or broadcast.
- A fresh 2026-07-24 public-identity dry-run now reads the Registry upgrade storage directly. The live pending hash is `0xaf5042150f37525720d41820f01c02770e9672a12c5a6596421c010621abb1d9`, with execute-after `2026-07-19T08:00:45Z`; Git history identifies it as the older `e1acf9f55` artifact, not the current AA-enabled candidate checksum `579281221`. That old manifest has 65 methods versus the current 74 and lacks all nine shared-AA methods (`abstractAccountCore`, `pendingAbstractAccountCore`, `abstractAccountCoreAvailableAt`, `proposeAbstractAccountCore`, `setAbstractAccountCore`, `cancelAbstractAccountCore`, `materializeAbstractAccount`, `getAppAbstractAccount`, `appIdOfAbstractAccount`). The updater now refuses to overwrite this matured, conflicting timelock and persists `deploy/config/platform-registry-update-latest.json` with `chain_writes_performed=false`; cancellation or execution remains separately authorized work.
- A follow-up public-identity `cancelUpdate` dry-run now verifies the live Registry admin `0x13ef519c362973f9a34648a9eac5b71250b2a80a` matches the signer identity and the cancellation test-invoke returns `HALT`; `deploy/config/platform-registry-update-latest.json` records `chain_writes_performed=false`. This proves cancellation eligibility only; it does not cancel the matured update or authorize any subsequent Registry write.
- The Registry updater's default read-only `status` action now reads the pending-update storage directly and fails closed if that read errors; it never recommends scheduling when the current timelock state is unknown. Its isolated regression tests and the full deployment-script suite pass.
- The same read-only status report now computes the candidate `sha256(nef‖manifest)` (`0x25339ed07a55b410b3e85fdf546af4f1562ebee1a8a665f5e7457cafa817db3e`) and records `pending_update_matches_candidate=false` against the live pending hash. This makes the old timelock conflict machine-verifiable before any cancellation decision.
- Added `deploy/scripts/configure_shared_aa_testnet.go` with one-action-per-run, dry-run-first governance lanes for AA registrar and Registry core proposal/confirmation/cancellation. `npm run -s test:shared-aa-governance` passes; the fresh public-identity `status` read records the live administrators but six missing reciprocal governance methods in `deploy/config/shared-aa-governance-latest.json`. A `propose-registrar` dry-run exits `1` at the ABI gate with `chain_writes_performed=false` and no transaction list, so the helper cannot accidentally write against the legacy live artifacts.
- Fresh read-only reruns at `2026-07-24T00:18Z–00:19Z` keep the migration boundary unchanged: the shared-AA preflight remains `phase=upgrade_contracts`, `safe_to_materialize=false`, and `next_action=upgrade_aa_then_registry_and_rerun_preflight`; the exact required reciprocal gap remains nine Registry methods plus nine AA registrar/account methods. The roster remains `77/77` derived, `77` unique, and `0` materialized. PlatformGame reads remain contract-state-ready but only `9/11` live-state-ready; `miniapp-jump-rush` and `miniapp-sheep-solitaire` are the two descriptor blockers, with eligible public-admin preflights and `chain_writes_performed=false`.
- A fresh read-only PlatformGame/Registry live-state audit now covers all 11 absorption apps at engine `0xc75b181b4561462903bb27d8d9e0b32b637bec12`: contract admin/oracle/AA/Registry reads complete, global pause is false, and all 11 apps report `gameType=5`, active, unpaused, expected app admin, and consistent `poolBalance=reservedPool+freePool=heldForApp=42000000` datoshi. Registry descriptors fully match the local manifest for **9/11** apps; `miniapp-jump-rush` matches **0/9** and `miniapp-sheep-solitaire` **3/9**, so those two remain configuration blockers despite healthy pool state. A public app-admin `setDescriptor` test-invoke HALTs for all 15 mismatched keys, but no transaction was signed or broadcast. The attach tool now supports a watch-only public-signer dry-run without WIF; the retained `docs/reports/platform-game-attach-public-dry-run-latest.json` records 11/11 existing attachments, 84 descriptors already at target, 15 planned descriptor updates, 0 failures, and an empty transaction list. The retained live-state evidence is `docs/reports/platform-game-live-state-latest.json` and `docs/reports/platform-game-live-state-latest.md`, with `chain_writes_performed=false`; these are current state/planning proofs only and do not replace the funded start/finalize/settle/withdraw lifecycle gate.
- Fresh public-symbol feed reads now persist `neo-morpheus-oracle/docs/reports/feed-registry-drift-latest.json` with machine-readable `read_only=true` and `chain_writes_performed=false`: mainnet has **28/33** fresh pairs and testnet **1/33**; registry coverage has **0** missing pairs but **38** configured stale-pair blockers. Six extra legacy on-chain pairs are retained as non-blocking because no safe delete method exists; no provider credential or backfill transaction was used.
- BetterStack heartbeat read was not completed because `BETTERSTACK_UPTIME_API_TOKEN` is absent from the local environment; no monitor API mutation was attempted.

The earlier `deploy/config/cohort0-registration-testnet-2026-07-18.json` 76/77 result was a transient `getApp` timeout for `miniapp-wallet-health`, not evidence that the app was absent. The fresh 77/77 read resolves that historical discrepancy. A separate credential-free coverage audit also completed current read-only RPC checks for all 77 manifests: 36 classify as testnet+mainnet, 12 source-only, 24 frontend-only, and 5 testnet-only, with no RPC-unknown rows. The five testnet-only rows still require an explicit mainnet release/defer decision.

No private key was printed or written. No chain-write confirmation variable was set, no transaction was signed, and no chain transaction was attempted.

## Remaining high-priority work

1. Review and approve the exact UnifiedSmartWallet legacy-direct bootstrap artifact and its distinct admin domain. First cancel the conflicting matured Registry timelock under the verified Registry admin, then separately schedule/review the current PlatformRegistry upgrade artifact. The implementation now enforces the post-upgrade safe order and rollback, but no cancellation, update, registrar confirmation, core activation, or materialization write is authorized by this report.
2. Treat `materialize-accounts` as optional treasury isolation only. Name the apps that actually require a deployed shim before accepting its ~794.39 GAS fleet cost; do not use shim count as the default account-completeness metric.
3. Review the new local Registry hash and migration/rollback plan. Execute no batch update yet: Registry, DeFi, Factory, Anchor, and PlatformSocial retain independent upgrade/deployment gates and admin domains. PlatformDeFi is a confirmed hold: 3 payer-global GAS-credit rows total `16.00000002 GAS`, exceeding native backing by `1.34226336 GAS`. Review the local v1.2 recovery bridge, exact payer arrays/hash, separate deficit top-up, and all withdrawal simulations; prefer a fresh deployment while bindings are zero. Name a first tenant only after an exact standalone-to-shared ABI and money/recovery migration is approved; the new framework surface is not permission to rebind incompatible apps.
4. Review and deploy the updated Sheep wrapper/session-host pin, update its PlatformGame engine attachment, then run a funded start/finalize/settle/recovery/withdraw lifecycle for all routed apps. Only then define descriptors/Morpheus wrappers and attach the five zero-drain candidates; delete standalone code only after rollback and drain evidence. Until then, use the explicit legacy-clone recovery opt-in rather than the normal selected deployment path.
5. Extend the now-green 186/186 deterministic desktop/mobile surface gate with physical-device interaction, real-wallet confirmation, live dependency health, and funded business-lifecycle evidence. Keep those operational proofs separate from the automated render/navigation result.
6. Upgrade the deployed MorpheusDataFeed with the reviewed 63-byte key guard, then rerun the restored fuzz lane until the live 64/256-byte resource-ID findings disappear. The local source/artifact and 13/13 focused VM regression are green; no chain write has been authorized or performed.
7. Land the Morpheus engine-sync/parity work as its own reviewed change before claiming single-source completion. The AA envelope algorithm is now single-sourced through a generated canonical artifact; replace that artifact with a supported cross-repository package link only when the package distribution path is available. Direct app-level `ctx.os` calls are closed, but remove the host compatibility layer only after its composition and test consumers are independently retired.

## Completion decision

Do not mark the joint platform refactor complete. Shared-AA source architecture, focused cross-repository tests, and the 186/186 automated full-fleet desktop/mobile surface gate are implemented, but live Registry/AA upgrades, reciprocal configuration, 77 shared-account materializations, funded game lifecycles, physical-device/real-wallet/live-service UX proof, and every reviewed chain write remain incomplete.
