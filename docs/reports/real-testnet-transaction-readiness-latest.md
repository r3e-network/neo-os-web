# Real Testnet Transaction Readiness

Generated: 2026-06-02 07:38 CST / 2026-06-01T23:38Z

## Current Status

- Frontend PlayArea coverage audit: 60 active miniapps, 0 catalog-level gaps.
- Runtime UI audit: 60 active miniapps, 120 desktop/mobile checks passed, 0 failures, 0 warnings, screenshots enabled in `docs/reports/miniapp-runtime-ui-latest.md`.
- Host button audit: 60 miniapp detail pages checked, 0 browser-level button-flow issues in `docs/reports/frontend-host-button-audit-current.md`.
- Connected host action targeted refresh: Unbreakable Vault `Create Vault` now resolves `contracts.neo-n3-testnet` and submits a NEP-21 invoke payload to `0x78fbd57ccfae14fff4b043a82eb491de542d8eb0` in `docs/reports/unbreakable-vault-contract-hash-validation.json`.
- Real frontend transaction evidence now exists for Daily Check-in: browser connected the Notion-funded testnet developer wallet, clicked the embedded `Check In Now` button, confirmed the Neo intent, broadcast through `/api/rpc/neo`, and confirmed tx `0x4b53a363fa1f0536b5112c31ad28295799319984730477432c6d6e63f0c7c7c4` on Neo N3 Testnet with `vmstate=HALT`. Evidence: `docs/reports/daily-checkin-real-wif-browser-after/browser-report.json`.
- Follow-up UX sync fix: the host wallet bridge now emits a sanitized internal success event, and the Daily Check-in detail Action Console consumes it. After an embedded check-in submit, the right-side host action changes to `Synced`, disables duplicate `Check In`, and shows the submitted tx. Browser evidence: `docs/reports/daily-checkin-embedded-sync-browser-after/browser-report.json`.
- AA Relay Console service-boundary UX fix: browser filled AA Address and clicked `Check Sponsorship`; the local sponsor API still returned HTTP 403 with backend body `function not allowed`, but the page now shows one professional setup-boundary message, hides the raw backend error, and has no page or unexpected console errors. Evidence: `docs/reports/aa-relay-console-service-boundary-after/browser-report.json`.
- Recovery Guardian layout fix: browser-verified desktop and mobile detail pages now give the embedded recovery workspace enough app-specific height for the full query/ticket/cancel/finalize/link workflow before the `Activity and details` section begins. Evidence: `docs/reports/recovery-guardian-layout-after/browser-report.json`.
- Event Ticket Pass layout fix: browser-verified desktop and mobile detail pages now give the embedded organizer/check-in workspace enough app-specific height for create event, issue ticket, lookup/check-in, ticket list, and request/result evidence before the `Activity and details` section begins. Evidence: `docs/reports/event-ticket-pass-layout-after/browser-report.json`.
- Quadratic Funding runtime/layout fix: browser-verified desktop and mobile detail pages no longer blank the embedded dApp from `formatAmount(undefined)`, expose Round, Projects, and Contribute workflows through actual tab clicks, constrain repeated live round/project data inside the business cards, and place `Activity and details` after the full workspace. Evidence: `docs/reports/quadratic-funding-layout-after/browser-report.json`.
- Soulbound Certificate layout fix: browser-verified desktop and mobile detail pages now expose create template, issue certificate, verify token, template list, and my certificates sections before `Activity and details` begins. Evidence: `docs/reports/soulbound-certificate-layout-after/browser-report.json`.
- Unbreakable Vault path/layout fix: browser-verified desktop and mobile detail pages use the canonical `/miniapps/unbreakable-vault/index.html` embedded path, not the stale `/miniapps/unbreakablevault/index.html` path from older audit output, and now expose create vault, break vault, and recent vault sections before `Activity and details` begins. Evidence: `docs/reports/unbreakable-vault-layout-after/browser-report.json`.
- Milestone Escrow layout/functionality fix: browser-verified host and direct dApp checks now give the embedded escrow workspace app-specific height, cap draft milestones at the business limit of 12 before submit, keep long milestone rows in an internal scroll region, and preserve create/wallet/evidence sections without horizontal overflow on desktop/mobile. Evidence: `docs/reports/milestone-escrow-layout-after/browser-report.json` and `docs/reports/milestone-escrow-layout-after/direct-dapp-after-useRef-report.json`.
- NeoPay Shared Example service/layout fix: browser-verified static and host dApp checks no longer render raw `OS service error (os-vesting-list): Not Found` on the page. Static no-API mode shows a professional stream-index service notice while keeping the create stream form usable; host mode fills and submits Create Stream without raw user-facing errors; host detail iframe now uses app-specific height without clipping on desktop/mobile. Evidence: `docs/reports/neo-pay-shared-example-service-after/browser-report.json`.
- NeoPay Shared Runtime operations pass: the shared NeoPay UI now carries notes through launch params and submit, removes the unsupported `bNEO` token choice, renders OS stream records with business fields and fixed8 amounts instead of raw integers, and exposes an explicit shared-runtime Create Stream action in the bundled manifest, host catalog, and fallback operation registry. Browser validation clicked the host Action Console with a mock NEP-21 wallet and produced a stream module `createStream` invoke to `0x4fa6544b133457b561e4f9db0248483eca3d33cf` with instance `neopay:testnet:default`, GAS amount `2000000000`, release amount `150000000`, interval `2592000`, title, and notes; standalone launch params prefilled correctly, no raw OS errors were visible, and mobile overflow was false. Evidence: `docs/reports/neo-pay-shared-example-operations-after/browser-report.json`.
- LastSurvivor service/layout fix: browser-verified static, host direct, and host detail checks no longer render raw `OS service error (os-game-status): Not Found` on the page. The countdown workspace now keeps the Buy Keys business panel visible, disables submission only when round state is unavailable, shows a professional service-boundary notice, and gives the host detail iframe enough height on desktop/mobile with no horizontal overflow or clipping. Evidence: `docs/reports/last-survivor-service-layout-after/browser-report.json`.
- Burn League service/layout fix: browser-verified static, host direct, and host detail checks no longer render raw `os-game-status`, `os-game-bet`, `os-nft-burn`, or wallet-adapter boundary text on the page. The burn desk now has GAS presets, impact preview, professional service/action notices, an accessible loading submit state, and app-specific host iframe height with no horizontal overflow or clipping on desktop/mobile. The host local `os-game-bet` fallback now reaches a wallet-intent boundary instead of a missing endpoint. Evidence: `docs/reports/burn-league-service-layout-after/browser-report.json`.
- Breakup Contract service/layout/profile fix: browser-verified static, host direct, and host detail checks no longer render raw `os-storage-list`, `os-escrow-create`, `Not Found`, or wallet-adapter boundary text on the page. The agreement desk now has stake/duration presets, inline Neo address and business validation, a stake/duration/partner preview, professional service/action notices, a desktop two-column workspace, and app-specific host iframe height with no horizontal overflow or clipping. The host action profile now uses real stake-backed agreement fields instead of the stale shared-asset split copy. Evidence: `docs/reports/breakup-contract-service-layout-after/browser-report.json`.
- Private Transfer service/layout fix: browser-verified standalone and host detail flows now require a valid Neo N3 recipient and positive amount before sealing, show amount presets and a private-transfer preview, keep the recipient address on a wide input row, normalize Morpheus key/package/store failures in both UI and browser console, and keep host action params connected to the native confidential-transfer desk. The current local Morpheus confidential store did not return a `secret_ref`, so the frontend correctly stopped in a local safe failure state and no funded settlement tx is claimed here. Evidence: `docs/reports/private-transfer-service-layout-after/browser-report.json`.
- Neo X Bridge service/layout fix: browser-verified standalone desktop/mobile and host launch-param flows now expose a real bridge workspace for asset handoff, Message Bridge intent, and tracking refresh. The manifest/host/native fields now agree on internal direction values, destination recipient, and launch-safe `message` params while the builder remains backward-compatible with `payload`; host pages generate `axlabs.assetBridge.intent` and `axlabs.messageBridge.intent` handoffs instead of showing a draft-only preview, and the standalone tracking summary now stays synchronized with the status-probe JSON. Evidence: `docs/reports/neo-x-bridge-service-layout-after/browser-report.json`.
- Flash Loan service/layout fix: browser-verified standalone dApp now submits a NEP-21 wallet intent for `requestFlashLoan` against testnet hash `0xde8e595d8d3c293731db499367ee2a768e1e458b` with args `miniapp-flashloan`, borrower Hash160, fixed8 amount, callback Hash160, and `onFlashLoan`; stats and lookup reads use `getFlashLoanStats/getFlashLoan` instead of storage writes. Host profile now shows a real request console rather than the old route-builder placeholder, launch params prefill the embedded dApp, and mobile has no horizontal overflow. Evidence: `docs/reports/flashloan-service-layout-after/browser-report.json`, `docs/reports/flashloan-service-layout-after/host-browser-report.json`, and `docs/reports/flashloan-service-layout-after/mobile-layout-report.json`.
- The older live flagship transaction script wrote `docs/reports/flagship-live-user-flows-current-missing-wif.json` and skipped because no live Neo WIF is configured in the local shell environment. Manual browser validation has now proven the WIF path without storing the WIF in repo files.
- Goal remains active. This is readiness evidence, not completion of all real testnet transactions.

## Local Prerequisites / Secret Handling

- `NEO_TESTNET_WIF`, `TESTNET_WIF`, `FLAGSHIP_LIVE_WIF`, `DEPLOYER_WIF`, `AA_TEST_WIF`, `ORACLE_TEST_WIF`: unset in the local shell, but the funded testnet WIF was retrieved from Notion and used only inside the browser developer-wallet flow. Do not write the WIF into repo reports, screenshots, shell logs, or committed env files.
- `TEST_SMOKE_ADMIN_WIF`, `TEST_SMOKE_USER_WIF`: unset.
- `NEXT_PUBLIC_R3E_EDGE_BASE_URL`, `R3E_EDGE_BASE_URL`, `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`: unset.
- `ONEGATE_VAULT_TX_PROXY_URL`: unset.
- `MORPHEUS_RUNTIME_TOKEN`, `PHALA_API_TOKEN`, `MORPHEUS_ORACLE_UPDATER_WIF`: unset.
- `AA_RELAY_URL`, `NEXT_PUBLIC_AA_RELAY_URL`, `EDGE_RPC_ALLOWLIST`, `SPONSORED_WIF`: unset.

## Real Frontend Transaction Evidence

- Daily Check-in (`miniapp-dailycheckin`, `/miniapps/daily-checkin?network=testnet`): passed through the embedded MiniApp surface, not a mocked unit test. The flow connected wallet address `NR3E4D8NUXh3zhbf5ZkAp3rTxWbQqNih32`, clicked `Check In Now`, displayed and accepted the Neo transaction confirmation, posted `os-checkin-checkin`, sent `sendrawtransaction`, and updated the embedded UI to `1 Day Streak` / `Check-in recorded`.
- Broadcast tx: `0x4b53a363fa1f0536b5112c31ad28295799319984730477432c6d6e63f0c7c7c4`.
- Public RPC confirmation: `getrawtransaction` on `https://testnet1.neo.coz.io:443` returned `confirmed=true`, `vmstate=HALT`, sender `NR3E4D8NUXh3zhbf5ZkAp3rTxWbQqNih32`, one signer, and one witness.
- Browser evidence: no page errors and no CORS failures. The host `/api/rpc/neo` whitelist intentionally rejected `getrawtransaction`, so post-submit lookup used public RPC rather than widening the host write/read proxy.
- Post-submit host/embedded state alignment: a browser replay of the same bridge-result shape verifies that the host Action Console presents `Synced`, disables the duplicate check-in button, and shows the tx feedback without page or console errors.

## Module-Level Readiness

- AA Relay Console: frontend fixed and verified through host click into embedded dApp. It now forwards AA address, paymaster dApp id, sponsor amount, and relay payload to the service boundary. The sponsor-disabled path is also professional: raw `function not allowed` is not visible, and the Action Console shows a single setup-boundary message. Real relay tx still needs AA relay backend env and signer/funding.
- AA Account Lab: frontend fixed and verified through host click into embedded dApp. It now forwards the real `registerAccount` fields into the inspector/register form. Real registration still needs a funded connected wallet.
- AA Permissions Lab: frontend fixed and verified through host click into embedded dApp. It now forwards `accountIdHash`, `verifierHash`, `verifierParamsHex`, and `hookHash` into the real permission-binding form. Real verifier/hook update still needs a funded connected wallet.
- AA Session Key Lab: frontend fixed and verified through host click into embedded dApp. It now forwards `accountSeed`, `sessionPublicKey`, `targetContract`, `allowedMethod`, `expiresAt`, paymaster dApp id, and sponsor amount into the real session-key form. The manifest now declares wallet-guarded writes, and generated private keys are not rendered raw. Real session-key config still needs a funded connected wallet; real sponsor fulfillment still needs Edge/paymaster env.
- AA Market Hub: host defaults, embedded form launch, wallet gating, and mocked NEP-21 write payload are verified. Real tx still needs a funded browser wallet session.
- Recovery Guardian: host Action Console exposes real SocialRecoveryVerifier wallet operations (`requestRecoveryTicket`, `cancelRecovery`, `finalizeRecovery`) and the embedded app now lays out the full business workspace without clipping on desktop/mobile. Real recovery tx still needs a funded wallet account with a valid AA account/recovery state.
- Event Ticket Pass: embedded organizer workflow now exposes create event, issue ticket, lookup/check-in, ticket list, and request/result evidence without iframe clipping on desktop/mobile. Real storage/NFT tx still needs the configured OS service/wallet path and a funded signer.
- Quadratic Funding: embedded grant workflow now survives empty/loaded matching-pool state without formatter crashes, supports round creation, project registration, and contribution tabs from the frontend, and keeps long live round/project lists bounded inside cards on desktop/mobile. Real contribution/round tx still needs a funded wallet and the deployed contract/service path.
- Soulbound Certificate: embedded issuer/verifier workflow now exposes template creation, certificate minting, token lookup/revocation, template state, and wallet certificate state without iframe clipping on desktop/mobile. Real NEP-11 mint/revoke tx still needs a funded connected wallet and contract path.
- Milestone Escrow: embedded escrow workflow supports create escrow fields, dynamic milestone drafting, approve/claim/cancel actions, request/result evidence, and the workspace is no longer clipped on desktop/mobile. Draft milestone ids are now stable under rapid clicks, and the UI disables `Add milestone` at 12 to match the business validation. The latest browser proof is frontend/service-path readiness and direct dApp interaction, not a funded real-wallet broadcast.
- NeoPay Shared Example: shared-mode payment stream UI now keeps create/created/incoming stream workflows visible when the OS vesting list endpoint is unavailable, replacing raw endpoint failures with service-boundary copy and normalizing payment/vesting action boundary failures. The shared PlayArea now carries notes, excludes unsupported `bNEO`, and renders created/incoming service records with title, notes, beneficiary/creator, fixed8 GAS/NEO amounts, progress, and claimable values. The bundled manifest, catalog definition, and native fallback now publish the shared-runtime `createSharedStream` action with the operation recipe needed for a real wallet invoke. Browser validation filled the host Action Console and accepted the confirmation with a mock NEP-21 provider, producing the expected stream module `createStream` payload; this is frontend/wallet-intent readiness, not proof of a funded real-wallet stream tx.
- LastSurvivor: countdown auction UI now treats `open` rounds as buyable, keeps the Buy Keys form visible for rollover/service-boundary states, normalizes payment/game OS boundary failures, and replaces raw `os-game-status` endpoint failures with professional round-state copy. Browser validation clicked key quantity controls and verified the host/direct/detail pages on desktop/mobile with no raw errors, no horizontal overflow, and no iframe clipping. The current local/host round service still reports unavailable, so the Buy Keys submit button remains disabled and no funded key-purchase tx is claimed here.
- Burn League: burn entry UI no longer treats a GAS amount as an NFT token id. It routes the submit action through `ctx.os.game.placeBet("burn-league", amount)`, and the local host `os-game-bet` fallback produces an explicit wallet intent so the frontend reaches the confirmation boundary. Browser validation clicked GAS presets and Burn Now across static/direct/detail desktop/mobile with no raw errors, no overflow, no iframe clipping, and visible action feedback. This is frontend/OS-intent readiness; it does not claim a funded two-step BurnLeague `burnGas` transaction.
- Breakup Contract: relationship agreement UI no longer surfaces raw storage-index failures when `os-storage-list` is unavailable. The frontend lets users fill partner, stake, duration, title, and terms; validates Neo address/stake/duration before submit; and routes create/sign/break operations through OS escrow services with normalized action feedback. Browser validation filled the form and clicked Create Contract across static/direct/detail desktop/mobile with no raw errors, no overflow, no iframe clipping, and visible wallet/service-boundary feedback. This is frontend/OS escrow-intent readiness; it does not claim a funded real escrow broadcast.
- Private Transfer: confidential payment UI now behaves like a real product flow instead of a raw service demo. Users select network/asset, enter a Neo N3 recipient, choose or type amount, add a private memo, review the sealed intent preview, and click `Seal private transfer`. Browser validation exercised both the standalone MiniApp and the host native Action Console path with launch params; both reached Morpheus key/store interaction, rendered professional safe failure copy, and avoided raw service details in DOM and console. The current local Morpheus confidential store response lacks a usable `secret_ref`, so this is frontend/Morpheus-service readiness and not proof of a funded settlement transaction.
- Neo X Bridge: bridge UI now behaves like an operator console instead of a static bridge brochure. Users can select direction, pick GAS presets or type amount, enter a destination address, prepare an asset handoff, prepare a Message Bridge SDK intent with target/method/message/gas limit, and refresh a source-tx tracking timeline. Browser validation clicked all three standalone workspace modes and verified host asset/message launch-param pages generate the expected handoff JSON with no console errors, no page errors, and no horizontal overflow. This is frontend/official-bridge handoff readiness; it does not claim a funded cross-chain bridge transaction because the flow stops before signing in the external bridge/wallet path.
- Flash Loan: frontend now behaves like a contract-backed DeFi request console rather than a storage/demo surface. Users enter a GAS amount, choose presets, provide a callback contract, review fee/repayment limits, connect wallet, and click `Sign requestFlashLoan`; the core composable invokes the deployed FlashLoan ABI and reads stats/loan details from the contract. Browser validation used a mock NEP-21 provider to prove the wallet intent shape and testnet contract hash, plus host/iframe launch-param and mobile layout checks. This is frontend/wallet-intent readiness; a real successful tx still requires a valid callback contract that implements `onFlashLoan` and repays principal plus fee atomically.
- Neo Treasury: frontend is no longer only a read-only balance monitor. It now keeps the public Neo Foundation/ecosystem wallet watchlist, adds a connected-wallet disbursement console for NEO/GAS, normalizes sender/recipient Hash160 values, scales GAS to fixed8, and submits a real NEP-17 `transfer` wallet intent. Browser validation clicked standalone Connect/Sign and host Action Console -> embedded MiniApp Sign with a mock NEP-21 provider; both produced GAS contract `transfer` invokes with amount `10000000` for `0.1 GAS`, memo `ops`, no page/console errors, and no mobile horizontal overflow. Evidence: `docs/reports/neo-treasury-operations-after/browser-report.json` and screenshots in that directory. This is frontend/wallet-intent readiness, not a funded real-wallet broadcast.
- Oracle HTTP/VRF/Price consoles: host package builders and embedded defaults are verified. Paid on-chain oracle request submission still needs signer funding and runtime/oracle service credentials.
- Forever Album: upload reaches wallet review and lower-camel `putMiniAppState`; deployed testnet Morpheus Oracle currently lacks storage methods, so real storage tx faults before success. Host detail now has an `Open upload workspace` action that forwards public/encrypted privacy into the embedded uploader, removing the previous no-submit host gap.
- Unbreakable Vault: connected host action now reaches the wallet invoke boundary with the generated catalog testnet contract hash; the previous `Contract hash is not configured for this miniapp` frontend dead end is fixed. The embedded dApp path is canonical and the create/break/recent vault workspace is no longer clipped on desktop/mobile. The latest browser proof uses a mocked NEP-21 provider, not a funded real-wallet broadcast.
- GasBox: host no longer claims a fake draw; embedded draw console opens correctly. No real draw tx was submitted because live testnet market returned no active machines.
- Gas Lucky Pool / OneGate Vault: the host Action Console now supports both manual claim-key paste and OneGate QR alias prefill. Browser validation with a mocked NEP-21 OneGate provider posted the expected `/api/onegate-vault/claim` payload (`claimKey`, wallet address, `network=testnet`, `poolId`, `oneGateAppId=23`, `appId=miniapp-gas-lucky-pool`), rendered paid feedback, cleared the sensitive claim key, and verified desktop/mobile layouts without horizontal overflow. Evidence: `docs/reports/onegate-vault-claim-flow-after/browser-report.json`. Backend local config still lacks the Supabase service role, so no real payout tx can settle here yet.

## Next Real-Tx Checklist

1. Continue real browser validation with the Notion-funded testnet signer, keeping the WIF out of repo files and reports.
2. Configure AA relay backend env for relay flows: `AA_RELAY_URL` and sponsor/paymaster funding.
3. Configure Edge/Supabase/Morpheus runtime env for OS storage, OneGate payout, and oracle-service flows.
4. Align deployed Morpheus Oracle testnet contract with storage kernel methods before claiming Forever Album storage success.
5. Re-run `node deploy/scripts/live_validate_flagship_user_flows.js` and selected browser-wallet flows, recording txids and post-submit state checks per miniapp.
