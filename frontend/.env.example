# Frontend (Vercel/Vite) environment variables
# Copy to `frontend/.env` for local development.

# Base URL for the Service Layer Gateway API.
# In production this should point at your deployed gateway.
VITE_API_URL=http://localhost:8080/api/v1
